import{EventEmitter}from'node:events';
import{spawn,spawnSync,type ChildProcess}from'node:child_process';
import{createHash,randomBytes}from'node:crypto';
import{createConnection}from'node:net';
import{accessSync,constants,existsSync,mkdirSync,readFileSync,renameSync,writeFileSync,copyFileSync,cpSync,readdirSync,statSync,unlinkSync,rmSync}from'node:fs';
import path from'node:path';
import{parse as parseYaml,stringify as stringifyYaml}from'yaml';
import type{ApiRun,ApiWorkspace,AppState,BackupInfo,BranchEnvironment,ComponentPack,DatabaseInfo,DatabaseQueryResult,DatabaseSchemaDiff,DatabaseTable,DatabaseUser,Diagnostic,EngineeringCheck,EngineeringReport,FrameworkAction,InspectorEvent,LogEntry,MailAddress,OperationRecord,PluginInfo,PortBindingInfo,PortConflictInfo,PreflightReport,ProductionParityReport,Project,ProjectBlueprint,ProjectHealthCheck,ProjectHealthReport,ProjectProfile,ProjectTask,ReleaseReadiness,RepairAction,ReplayCapture,ResourceUsage,RuntimeComponent,RuntimeMode,RuntimePackageInfo,SafeChangeRecord,SecurityFinding,SecretMetadata,ServiceDependency,ServiceId,ServiceState,ShareSession,SnapshotInfo,StartProfile,TestLabReport,UpdateState,WebhookEndpoint,WebhookEvent,WorkerProcessInfo}from'./types.js';

const APP_VERSION='1.1.0-test.1';
const OFFICIAL_STABLE_UPDATE_FEED='https://localdeck.nl/downloads/windows.json';
const OFFICIAL_BETA_UPDATE_FEED='https://localdeck.nl/downloads/beta.json';
const defaultUpdateFeedUrl=()=>APP_VERSION.includes('-')?OFFICIAL_BETA_UPDATE_FEED:OFFICIAL_STABLE_UPDATE_FEED;
const detectedLanguage=():AppState['settings']['language']=>{const locale=Intl.DateTimeFormat().resolvedOptions().locale||process.env.LANG||'';return locale.toLowerCase().startsWith('nl')?'nl':'en'};
export interface SecretCodec{available():boolean;encrypt(value:string):string;decrypt(value:string):string;name:'windows-dpapi'|'test-codec'}
interface VaultRecord extends SecretMetadata{encryptedValue:string}
interface WindowsPortOwner{pid?:number;name?:string;path?:string}
const PHP_EXTENSIONS=[
 {id:'mysqli',label:'MySQLi',description:'MySQL-databaseverbindingen'},
 {id:'pdo_mysql',label:'PDO MySQL',description:'PDO-driver voor MySQL'},
 {id:'mbstring',label:'Mbstring',description:'Unicode- en multibyte-tekst'},
 {id:'openssl',label:'OpenSSL',description:'TLS en cryptografie'},
 {id:'curl',label:'cURL',description:'HTTP- en API-verzoeken'},
 {id:'zip',label:'ZIP',description:'ZIP-archieven'},
 {id:'gd',label:'GD',description:'Afbeeldingsbewerking'},
 {id:'intl',label:'Intl',description:'Internationalisatie'},
 {id:'xdebug',label:'Xdebug',description:'Debuggen en code coverage'}
];

const SERVICES:ServiceState[]=[
 {id:'apache',name:'Apache',description:'HTTP-webserver',status:'stopped',port:80,version:'2.4.68',installed:false,health:'unknown'},
 {id:'php',name:'PHP',description:'FastCGI runtime per project',status:'stopped',port:9000,version:'8.5',installed:false,health:'unknown'},
 {id:'mysql',name:'MySQL',description:'Relationele database',status:'stopped',port:3306,version:'8.4.10',installed:false,health:'unknown'},
 {id:'phpmyadmin',name:'phpMyAdmin',description:'Databasebeheer in de browser',status:'stopped',port:8080,version:'5.2.3',installed:false,health:'unknown'},
 {id:'mail',name:'Mailpit',description:'Lokale e-mail inbox',status:'stopped',port:8025,version:'1.30.7',installed:false,health:'unknown'},
 {id:'redis',name:'Redis',description:'Cache & queues',status:'stopped',port:6379,version:'5.0.14.1',installed:false,health:'unknown'}
];

const DEFAULT_PROFILE:ProjectProfile={webServer:'apache',phpExtensions:['mysqli','pdo_mysql','mbstring','openssl','curl','zip'],environment:'development',databaseEngine:'mysql',databaseVersion:'8.4',mailMode:'capture',debugToolbar:true,isolated:true,runtimeLocked:true,xdebugMode:'off',schedulerEnabled:false,env:{APP_ENV:'local',APP_DEBUG:'true'}};
const BUILT_IN_PLUGINS:PluginInfo[]=[
 {id:'wordpress',name:'WordPress',description:'WordPress-projecten met database en lokale mail.',category:'template',version:'latest',enabled:true,installed:true,builtIn:true},
 {id:'laravel',name:'Laravel',description:'Laravel, Artisan, queues en Composer-taken.',category:'template',version:'latest',enabled:true,installed:true,builtIn:true},
 {id:'symfony',name:'Symfony',description:'Symfony CLI-workflow en Composer-taken.',category:'template',version:'latest',enabled:true,installed:true,builtIn:true},
 {id:'drupal',name:'Drupal',description:'Drupal-projecttemplate via Composer.',category:'template',version:'latest',enabled:true,installed:true,builtIn:true},
 {id:'nginx',name:'Nginx-profielen',description:'Projectprofielen voorbereiden voor een optionele Nginx-runtime.',category:'service',version:'1',enabled:false,installed:false,builtIn:true},
 {id:'caddy',name:'Caddy-profielen',description:'Automatische HTTPS-profielen met een optionele Caddy-runtime.',category:'service',version:'1',enabled:false,installed:false,builtIn:true},
 {id:'real-mail',name:'SMTP/IMAP-server',description:'Koppeling voor een externe of optionele lokale mailserver.',category:'service',version:'1',enabled:false,installed:false,builtIn:true}
];
const COMPONENT_PACKS:ComponentPack[]=[
 {id:'mariadb',name:'MariaDB',description:'Alternatieve relationele database als afzonderlijk, controleerbaar offline pakket.',category:'database',status:'planned',requiresAdmin:false,license:'GPL-2.0'},
 {id:'postgresql',name:'PostgreSQL',description:'PostgreSQL-runtime en beheerprofiel als afzonderlijk offline pakket.',category:'database',status:'planned',requiresAdmin:false,license:'PostgreSQL'},
 {id:'nginx',name:'Nginx',description:'Alternatieve webserver voor projecten die Apache niet gebruiken.',category:'webserver',status:'planned',requiresAdmin:false,license:'BSD-2-Clause'},
 {id:'caddy',name:'Caddy',description:'Moderne webserver met lokaal HTTPS-profiel.',category:'webserver',status:'planned',requiresAdmin:false,license:'Apache-2.0'},
 {id:'mail-server',name:'Lokale SMTP/IMAP-server',description:'Volwaardige ontwikkelmailserver naast de veilige Mailpit-opvangmodus.',category:'mail',status:'planned',requiresAdmin:false,license:'Nog vast te leggen'}
];
const SERVICE_DEPENDENCIES:ServiceDependency[]=[
 {serviceId:'mysql',requires:[],usedBy:['phpmyadmin'],reason:'Databaseserver voor phpMyAdmin en projecten.'},
 {serviceId:'php',requires:[],usedBy:['apache','phpmyadmin'],reason:'FastCGI-runtime voor websites en phpMyAdmin.'},
 {serviceId:'apache',requires:['php'],usedBy:[],reason:'Webserver gebruikt de actieve PHP-pools.'},
 {serviceId:'phpmyadmin',requires:['mysql','php'],usedBy:[],reason:'Databasebeheer vereist MySQL en PHP.'},
 {serviceId:'mail',requires:[],usedBy:[],reason:'Zelfstandige SMTP-opvang en webinbox.'},
 {serviceId:'redis',requires:[],usedBy:[],reason:'Zelfstandige cache- en queue-service.'}
];
const defaultStartProfiles=():StartProfile[]=>{const updatedAt=new Date().toISOString();return[
 {id:'full-stack',name:'Volledige stack',description:'Alle lokale services voor dagelijks ontwikkelwerk.',services:['mysql','php','apache','phpmyadmin','redis','mail'],projectIds:[],builtIn:true,active:true,updatedAt},
 {id:'web',name:'Webontwikkeling',description:'Apache, PHP, database en mail zonder Redis of phpMyAdmin.',services:['mysql','php','apache','mail'],projectIds:[],builtIn:true,active:false,updatedAt},
 {id:'api',name:'API & queues',description:'PHP, MySQL, Redis en mail voor API- en workerwerk.',services:['mysql','php','redis','mail'],projectIds:[],builtIn:true,active:false,updatedAt},
 {id:'minimal',name:'Minimaal',description:'Alleen Apache en de vereiste PHP-runtime.',services:['php','apache'],projectIds:[],builtIn:true,active:false,updatedAt}
]};
const DEFAULT_TASKS:ProjectTask[]=[
 {id:'composer-install',name:'Composer install',command:'composer',args:['install','--no-interaction'],category:'composer',safe:true},
 {id:'composer-audit',name:'Composer audit',command:'composer',args:['audit','--format=plain'],category:'composer',safe:true},
 {id:'npm-install',name:'npm install',command:'npm',args:['install'],category:'node',safe:true},
 {id:'npm-test',name:'npm test',command:'npm',args:['test','--','--run'],category:'test',safe:true},
 {id:'phpunit',name:'PHPUnit',command:'php',args:['vendor/bin/phpunit'],category:'test',safe:true},
 {id:'artisan-migrate',name:'Artisan migrate',command:'php',args:['artisan','migrate','--force'],category:'framework',safe:false}
];
const normalizeProject=(project:Project):Project=>({...project,secure:true,profile:{...DEFAULT_PROFILE,...project.profile,env:{...DEFAULT_PROFILE.env,...project.profile?.env}}});

export class RuntimeManager extends EventEmitter{
 private state:AppState;private processes=new Map<ServiceId,ChildProcess>();private phpProcesses=new Map<string,ChildProcess>();private workerProcesses=new Map<string,ChildProcess>();private cancelledOperations=new Set<string>();private dataDirectory:string;private stateFile:string;private vaultFile:string;private sessionFile:string;private allowSimulation:boolean;private bundledPackagesDirectory?:string;private projectsDirectory:string;private webRootDirectory:string;private folderMode:boolean;private secretCodec?:SecretCodec;private vaultRecords:VaultRecord[]=[];private serviceIntent:Partial<Record<ServiceId,boolean>>={};private componentPacksLoaded=false;private windowsPortSnapshot?:{checkedAt:number;owners:Map<number,WindowsPortOwner>};private diagnosticsQueue:Promise<void>=Promise.resolve();
 constructor(dataDirectory:string,options:{allowSimulation?:boolean;bundledPackagesDirectory?:string;projectsDirectory?:string;webRootDirectory?:string;folderMode?:boolean;secretCodec?:SecretCodec}={}){super();this.dataDirectory=path.resolve(dataDirectory);this.stateFile=path.join(this.dataDirectory,'state.json');this.vaultFile=path.join(this.dataDirectory,'vault.json');this.sessionFile=path.join(this.dataDirectory,'session.json');this.allowSimulation=options.allowSimulation??true;this.secretCodec=options.secretCodec;this.bundledPackagesDirectory=options.bundledPackagesDirectory;this.folderMode=options.folderMode??false;this.projectsDirectory=path.resolve(options.projectsDirectory??path.join(this.dataDirectory,'www'));this.webRootDirectory=path.resolve(options.webRootDirectory??path.join(this.dataDirectory,'www'));mkdirSync(this.dataDirectory,{recursive:true});mkdirSync(this.projectsDirectory,{recursive:true});mkdirSync(this.webRootDirectory,{recursive:true});const native=this.runtimeComplete();const marker=existsSync(path.join(this.dataDirectory,'runtime.json')),saved=this.loadSaved(),savedProjectsDirectory=typeof saved?.settings?.projectsDirectory==='string'?saved.settings.projectsDirectory:undefined;this.serviceIntent=saved?.serviceIntent??{};this.vaultRecords=this.loadVault();const previousSession=this.loadSession(),unclean=Boolean(previousSession&&!previousSession.cleanExit);
  this.state={services:SERVICES.map(s=>({...s,...saved?.services?.find((x:ServiceState)=>x.id===s.id),status:'stopped',health:'unknown',installed:this.binaryExists(s.id)})),projects:(saved?.projects??[]).map((project:Project,index:number)=>normalizeProject({...this.relocateFolderProject(project,savedProjectsDirectory),profile:{...DEFAULT_PROFILE,...project.profile,httpPort:project.profile?.httpPort??10080+index*2,httpsPort:project.profile?.httpsPort??10081+index*2}})),blueprints:[],branchEnvironments:saved?.branchEnvironments??[],components:[],componentPacks:COMPONENT_PACKS,runtimePackages:[],projectHealth:[],productionParity:[],preflightReports:[],testLabReports:[],secretVault:this.vaultMetadata(),portConflicts:[],portBindings:[],serviceDependencies:SERVICE_DEPENDENCIES,startProfiles:saved?.startProfiles??defaultStartProfiles(),actionOperations:(saved?.actionOperations??[]).map((item:OperationRecord)=>item.status==='running'||item.status==='queued'?{...item,status:'failed',progress:Math.min(item.progress,99),message:'Onderbroken doordat LocalDeck werd afgesloten.',updatedAt:new Date().toISOString(),finishedAt:new Date().toISOString(),canCancel:false}:item),safeChanges:saved?.safeChanges??[],runtimeLifecycle:[],engineeringReports:saved?.engineeringReports??[],apiWorkspaces:saved?.apiWorkspaces??[],apiRuns:saved?.apiRuns??[],webhookEndpoints:saved?.webhookEndpoints??[],webhookEvents:saved?.webhookEvents??[],workers:(saved?.workers??[]).map((item:WorkerProcessInfo)=>({...item,status:'stopped',pid:undefined,stoppedAt:new Date().toISOString()})),replayCaptures:saved?.replayCaptures??[],devDrive:{supported:false,drive:path.parse(this.projectsDirectory).root,isDevDrive:false,status:'unsupported',detail:'Nog niet gecontroleerd.',securityMode:'Windows Defender standaardbeleid',checkedAt:new Date().toISOString()},crashRecovery:{sessionId:crypto.randomUUID(),uncleanShutdownDetected:unclean,recoveredServices:[],status:unclean?'detected':'clean',detail:unclean?'De vorige LocalDeck-sessie is niet netjes afgesloten.':'Vorige sessie is netjes afgesloten.',checkedAt:new Date().toISOString()},atomicUpdate:{status:'idle',lastKnownGoodVersion:saved?.atomicUpdate?.lastKnownGoodVersion??APP_VERSION,checkedAt:new Date().toISOString(),detail:'Geen update in behandeling.',...saved?.atomicUpdate,currentVersion:APP_VERSION},releaseReadiness:{score:0,status:'warning',checkedAt:new Date().toISOString(),checks:[]},inspectorEvents:[],resourceUsage:{capturedAt:new Date().toISOString(),appMemoryMb:0,serviceMemoryMb:0,totalMemoryMb:0,processes:[]},shares:[],databases:saved?.databases??[],mailAddresses:saved?.mailAddresses??[],backups:[],snapshots:saved?.snapshots??[],tasks:(saved?.tasks??DEFAULT_TASKS).map((task:ProjectTask)=>task.id==='npm-dev'?{...task,id:'npm-test',name:'npm test',args:['test','--','--run'],category:'test'}:task),securityFindings:[],plugins:saved?.plugins??BUILT_IN_PLUGINS,repairActions:[],offlineRuntime:{status:'not-prepared',packages:0,requiredPackages:13},phpVersions:['8.2','8.3','8.4','8.5'].map(version=>({version,installed:existsSync(path.join(this.dataDirectory,`php-${version}`,'php.exe'))||version==='8.5'&&existsSync(path.join(this.dataDirectory,'php','php.exe')),active:(saved?.settings?.defaultPhpVersion??'8.5')===version})),phpExtensions:[],installation:{status:native?'installed':'not-installed',progress:native?100:0,message:native?'Windows-runtime gereed':marker?'Runtime-installatie is onvolledig. Klik op Nu installeren om te herstellen.':'Runtime nog niet geïnstalleerd'},diagnostics:[],update:{status:'idle',currentVersion:APP_VERSION},settings:{startWithWindows:false,minimizeToTray:true,telemetry:false,privacyMode:false,language:detectedLanguage(),defaultPhpVersion:'8.5',localDomainSuffix:'localhost',autoBackup:true,backupRetention:7,runtimeMode:'application',serviceAutoStart:true,autoRestart:true,restoreServicesAfterCrash:true,atomicUpdates:true,autoUpdateCheck:false,updateChannel:APP_VERSION.includes('-')?'beta':'stable',updateFeedUrl:defaultUpdateFeedUrl(),firstRunComplete:false,backupSchedule:'daily',webControlEnabled:true,webControlPort:7331,dnsMode:'hosts',mailMode:'capture',mailMaxMessages:1000,mailMaxAgeDays:30,smtpHost:'127.0.0.1',smtpPort:1025,smtpUsername:'',requireSignedUpdates:false,trustedUpdatePublisher:'',trustedPackPublisher:'',combinedLogs:true,securityScanOnStart:true,resourceSaver:false,idleStopMinutes:0,lanSharingEnabled:false,mcpEnabled:true,offlineFirst:true,...saved?.settings,simulationMode:this.allowSimulation&&!native,dataDirectory:this.dataDirectory,projectsDirectory:this.projectsDirectory,folderMode:this.folderMode},logs:[]};
  if([OFFICIAL_STABLE_UPDATE_FEED,OFFICIAL_BETA_UPDATE_FEED].includes(this.state.settings.updateFeedUrl))this.state.settings.updateFeedUrl=this.state.settings.updateChannel==='beta'?OFFICIAL_BETA_UPDATE_FEED:OFFICIAL_STABLE_UPDATE_FEED;
  this.state.settings.interfaceMode=this.state.settings.interfaceMode??'simple';
  this.state.services.find(service=>service.id==='php')!.version=this.state.settings.defaultPhpVersion;
  const projectsChanged=this.syncFolderProjects();this.configurePhpMyAdminAutoLogin();this.refreshPhpExtensions();this.refreshBackups();this.refreshSnapshots();this.refreshOfflineRuntime();this.refreshComponents();this.refreshComponentPacks();this.refreshBlueprints();this.refreshRuntimeLifecycle();this.refreshResources();this.writeLandingMetadata();if(this.folderMode){this.writeVhosts();this.persist()}else if(projectsChanged)this.persist();this.runProjectDoctor();this.assessReleaseReadiness();
  this.log('info','Systeem',native?'Native Windows-runtime gevonden.':'Veilige simulatiemodus actief.');void this.runDiagnostics();
 }
 private runtimeComplete(){const r=this.dataDirectory;return[path.join(r,'runtime.json'),path.join(r,'apache','bin','httpd.exe'),path.join(r,'php','php.exe'),path.join(r,'php','php-cgi.exe'),path.join(r,'mysql','bin','mysqld.exe'),path.join(r,'phpmyadmin','index.php'),path.join(r,'mailpit','mailpit.exe'),path.join(r,'redis','redis-server.exe')].every(existsSync)}
 private loadSaved():any{try{return JSON.parse(readFileSync(this.stateFile,'utf8').replace(/^\uFEFF/,''))}catch{return null}}
 private loadVault():VaultRecord[]{try{const value=JSON.parse(readFileSync(this.vaultFile,'utf8').replace(/^\uFEFF/,''));return Array.isArray(value)?value.filter(item=>item&&typeof item.key==='string'&&typeof item.encryptedValue==='string'):[]}catch{return[]}}
 private loadSession():any{try{return JSON.parse(readFileSync(this.sessionFile,'utf8').replace(/^\uFEFF/,''))}catch{return null}}
 private vaultMetadata():SecretMetadata[]{return this.vaultRecords.map(({encryptedValue,...item})=>item)}
 private saveVault(){const tmp=`${this.vaultFile}.tmp`;writeFileSync(tmp,JSON.stringify(this.vaultRecords,null,2),{encoding:'utf8',mode:0o600});renameSync(tmp,this.vaultFile);this.state.secretVault=this.vaultMetadata()}
 private publicEnvironment(environment:Record<string,string>={}){return Object.fromEntries(Object.entries(environment).filter(([key])=>!/password|passwd|secret|token|api[_-]?key|private[_-]?key/i.test(key)))}
 private runtimeEnvironment(projectId?:string){const values:Record<string,string>={};if(!this.secretCodec?.available())return values;for(const item of this.vaultRecords)if(!item.projectId||item.projectId===projectId)try{values[item.key]=this.secretCodec.decrypt(item.encryptedValue)}catch{this.log('warning','Secret Vault',`${item.key} kon niet voor het proces worden ontsleuteld.`)}return values}
 private relocateFolderProject(project:Project,savedProjectsDirectory?:string){if(!this.folderMode||!savedProjectsDirectory)return project;const relative=path.relative(path.resolve(savedProjectsDirectory),path.resolve(project.path));if(!relative||relative.startsWith('..')||path.isAbsolute(relative))return project;const projectPath=path.join(this.projectsDirectory,relative);if(!existsSync(projectPath))return project;const oldRoot=path.resolve(project.path),oldDocumentRoot=path.resolve(project.documentRoot||project.path),documentRelative=path.relative(oldRoot,oldDocumentRoot),documentRoot=!documentRelative.startsWith('..')&&!path.isAbsolute(documentRelative)?path.join(projectPath,documentRelative):projectPath;return{...project,path:projectPath,documentRoot}}
 private localDomain(slug:string){return`${slug}.${this.state.settings.localDomainSuffix}`}
 private certificatePaths(project:Project){const directory=path.join(this.dataDirectory,'certs','projects',project.id);return{directory,certificateFile:path.join(directory,'certificate.pem'),keyFile:path.join(directory,'private-key.pem')}}
 private refreshProjectCertificates(){for(const project of this.state.projects){const files=this.certificatePaths(project),active=project.secure&&existsSync(files.certificateFile)&&existsSync(files.keyFile);project.certificate={status:project.secure?active?'active':'missing':'disabled',...(active?{certificateFile:files.certificateFile,keyFile:files.keyFile}:{})}}}
 refreshProjectConfiguration(){this.refreshProjectCertificates();this.writeVhosts();this.runProjectDoctor();this.persist();this.emit('change',this.snapshot());return this.snapshot()}
 private syncFolderProjects(){if(!this.folderMode||!existsSync(this.projectsDirectory))return false;let changed=false;for(const entry of readdirSync(this.projectsDirectory,{withFileTypes:true})){if(!entry.isDirectory()||entry.name.startsWith('.'))continue;const projectPath=path.join(this.projectsDirectory,entry.name),key=path.resolve(projectPath).toLowerCase();if(this.state.projects.some(project=>path.resolve(project.path).toLowerCase()===key))continue;const detected=this.detectProject(projectPath),hasEntry=['index.php','index.html','composer.json','wp-config.php'].some(file=>existsSync(path.join(projectPath,file)))||existsSync(path.join(projectPath,'public','index.php'));if(!hasEntry)continue;const baseSlug=entry.name.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||'website';let slug=baseSlug,suffix=2;while(this.state.projects.some(project=>project.domain===this.localDomain(slug)))slug=`${baseSlug}-${suffix++}`;let httpPort=10080;const used=new Set([...this.state.services.map(service=>service.port),...this.state.projects.flatMap(project=>[project.profile?.httpPort,project.profile?.httpsPort])].filter((port):port is number=>Boolean(port)));while(used.has(httpPort)||used.has(httpPort+1))httpPort+=2;this.state.projects.push(normalizeProject({id:crypto.randomUUID(),name:entry.name,path:projectPath,documentRoot:detected.documentRoot,domain:this.localDomain(slug),phpVersion:this.state.settings.defaultPhpVersion,secure:false,createdAt:new Date().toISOString(),template:detected.template,profile:{...DEFAULT_PROFILE,isolated:false,httpPort,httpsPort:httpPort+1}}));changed=true}return changed}
 private persist(){const clean={services:this.state.services.map(({status,health,pid,error,...s})=>s),serviceIntent:this.serviceIntent,projects:this.state.projects,branchEnvironments:this.state.branchEnvironments,databases:this.state.databases,mailAddresses:this.state.mailAddresses,snapshots:this.state.snapshots,tasks:this.state.tasks,plugins:this.state.plugins,atomicUpdate:this.state.atomicUpdate,startProfiles:this.state.startProfiles,actionOperations:this.state.actionOperations.slice(0,100),safeChanges:this.state.safeChanges.slice(0,100),engineeringReports:this.state.engineeringReports.slice(0,100),apiWorkspaces:this.state.apiWorkspaces,apiRuns:this.state.apiRuns.slice(0,100),webhookEndpoints:this.state.webhookEndpoints,webhookEvents:this.state.webhookEvents.slice(0,200),workers:this.state.workers.map(({pid,...worker})=>({...worker,status:'stopped' as const})),replayCaptures:this.state.replayCaptures.slice(0,100),settings:this.state.settings};const tmp=`${this.stateFile}.tmp`;writeFileSync(tmp,JSON.stringify(clean,null,2));renameSync(tmp,this.stateFile)}
 snapshot():AppState{this.refreshProjectCertificates();return structuredClone(this.state)}
 reloadPersistedState(){const saved=this.loadSaved();if(!saved)return this.snapshot();for(const service of this.state.services){const persisted=saved.services?.find((item:ServiceState)=>item.id===service.id);if(persisted)Object.assign(service,{port:persisted.port,version:persisted.version,installed:this.binaryExists(service.id)})}this.serviceIntent=saved.serviceIntent??this.serviceIntent;this.state.projects=(saved.projects??this.state.projects).map((project:Project,index:number)=>normalizeProject({...this.relocateFolderProject(project,saved.settings?.projectsDirectory),profile:{...DEFAULT_PROFILE,...project.profile,httpPort:project.profile?.httpPort??10080+index*2,httpsPort:project.profile?.httpsPort??10081+index*2}}));this.state.branchEnvironments=saved.branchEnvironments??this.state.branchEnvironments;this.state.databases=saved.databases??this.state.databases;this.state.mailAddresses=saved.mailAddresses??this.state.mailAddresses;this.state.snapshots=saved.snapshots??this.state.snapshots;this.state.tasks=saved.tasks??this.state.tasks;this.state.plugins=saved.plugins??this.state.plugins;this.state.startProfiles=saved.startProfiles??this.state.startProfiles;this.state.actionOperations=saved.actionOperations??this.state.actionOperations;this.state.safeChanges=saved.safeChanges??this.state.safeChanges;this.state.engineeringReports=saved.engineeringReports??this.state.engineeringReports;this.state.apiWorkspaces=saved.apiWorkspaces??this.state.apiWorkspaces;this.state.apiRuns=saved.apiRuns??this.state.apiRuns;this.state.webhookEndpoints=saved.webhookEndpoints??this.state.webhookEndpoints;this.state.webhookEvents=saved.webhookEvents??this.state.webhookEvents;this.state.replayCaptures=saved.replayCaptures??this.state.replayCaptures;this.state.atomicUpdate={...this.state.atomicUpdate,...saved.atomicUpdate,currentVersion:APP_VERSION};this.vaultRecords=this.loadVault();this.state.secretVault=this.vaultMetadata();const native=this.runtimeComplete();this.state.settings={...this.state.settings,...saved.settings,simulationMode:this.allowSimulation&&!native,dataDirectory:this.dataDirectory,projectsDirectory:this.projectsDirectory,folderMode:this.folderMode};const projectsChanged=this.syncFolderProjects(),phpService=this.state.services.find(service=>service.id==='php');if(phpService)phpService.version=this.state.settings.defaultPhpVersion;if(this.state.installation.status!=='installing')this.state.installation={status:native?'installed':'not-installed',progress:native?100:0,message:native?'Windows-runtime gereed':'Runtime ontbreekt of is onvolledig. Klik op Nu installeren.'};this.state.phpVersions.forEach(version=>version.active=version.version===this.state.settings.defaultPhpVersion);this.refreshPhpExtensions();this.refreshBackups();this.refreshSnapshots();this.refreshOfflineRuntime();this.refreshComponents();this.refreshComponentPacks();this.refreshBlueprints();this.refreshRuntimeLifecycle();this.refreshResources();this.runProjectDoctor();this.assessReleaseReadiness();if(projectsChanged){this.writeVhosts();this.persist()}this.emit('change',this.snapshot());return this.snapshot()}
 private log(level:LogEntry['level'],source:string,message:string){if(!message)return;this.state.logs.unshift({id:crypto.randomUUID(),at:new Date().toISOString(),level,source,message:message.slice(0,3000)});this.state.logs=this.state.logs.slice(0,500);this.emit('change',this.snapshot())}
 private binaryPath(id:ServiceId){const r=this.dataDirectory,selectedPhp=this.state?.settings?.defaultPhpVersion??'8.5',phpDir=existsSync(path.join(r,`php-${selectedPhp}`))?path.join(r,`php-${selectedPhp}`):path.join(r,'php');return id==='apache'?path.join(r,'apache','bin','httpd.exe'):id==='php'?path.join(phpDir,'php-cgi.exe'):id==='mysql'?path.join(r,'mysql','bin','mysqld.exe'):id==='phpmyadmin'?path.join(phpDir,'php.exe'):id==='mail'?path.join(r,'mailpit','mailpit.exe'):path.join(r,'redis','redis-server.exe')}
 private phpDirectory(version:string){const versioned=path.join(this.dataDirectory,`php-${version}`);return existsSync(path.join(versioned,'php-cgi.exe'))?versioned:version===this.state.settings.defaultPhpVersion?path.join(this.dataDirectory,'php'):versioned}
 private phpPort(version:string){const base=this.state.services.find(service=>service.id==='php')?.port??9000;if(version===this.state.settings.defaultPhpVersion)return base;const versions=['8.2','8.3','8.4','8.5'];return base+10+Math.max(0,versions.indexOf(version))}
 private binaryExists(id:ServiceId){if(id==='phpmyadmin')return existsSync(this.binaryPath(id))&&existsSync(path.join(this.dataDirectory,'phpmyadmin','index.php'));return existsSync(this.binaryPath(id))}
 invalidatePortInspection(){this.windowsPortSnapshot=undefined}
 private windowsPortOwner(port:number){if(process.platform!=='win32')return undefined;if(!this.windowsPortSnapshot||Date.now()-this.windowsPortSnapshot.checkedAt>3000){const script='$processes=@{};Get-CimInstance Win32_Process -ErrorAction SilentlyContinue|ForEach-Object{$processes[[int]$_.ProcessId]=$_};@(Get-NetTCPConnection -State Listen -ErrorAction SilentlyContinue|ForEach-Object{$p=$processes[[int]$_.OwningProcess];[pscustomobject]@{port=[int]$_.LocalPort;pid=[int]$_.OwningProcess;name=$p.Name;path=$p.ExecutablePath}})|ConvertTo-Json -Compress',result=spawnSync('powershell.exe',['-NoLogo','-NoProfile','-NonInteractive','-WindowStyle','Hidden','-Command',script],{encoding:'utf8',windowsHide:true,timeout:7000}),owners=new Map<number,WindowsPortOwner>();try{const parsed=JSON.parse(result.stdout.trim()),items=Array.isArray(parsed)?parsed:parsed?[parsed]:[];for(const item of items){const localPort=Number(item.port);if(localPort&&!owners.has(localPort))owners.set(localPort,{pid:Number(item.pid)||undefined,name:item.name||undefined,path:item.path||undefined})}}catch{}this.windowsPortSnapshot={checkedAt:Date.now(),owners}}return this.windowsPortSnapshot.owners.get(port)}
 private async portOccupancy(port:number){const owner=this.state.settings.simulationMode?undefined:this.windowsPortOwner(port);return{occupied:Boolean(owner)||await this.portReachable(port),owner}}
 private async portFree(port:number){return!(await this.portOccupancy(port)).occupied}
 private async portReachable(port:number){return new Promise<boolean>(resolve=>{let finished=false;const socket=createConnection({host:'127.0.0.1',port}),finish=(reachable:boolean)=>{if(finished)return;finished=true;socket.destroy();resolve(reachable)};socket.setTimeout(1200,()=>finish(false));socket.once('connect',()=>finish(true));socket.once('error',()=>finish(false))})}
 private async waitForPort(port:number,attempts=32){for(let attempt=0;attempt<attempts;attempt++){if(await this.portReachable(port))return true;await new Promise(resolve=>setTimeout(resolve,250))}return false}
 runDiagnostics(){const run=async()=>{this.invalidatePortInspection();const checks:Diagnostic[]=[];checks.push({id:'platform',label:'Windows x64',status:process.platform==='win32'&&process.arch==='x64'?'ok':'error',detail:`${process.platform} ${process.arch}`,fixable:false});checks.push({id:'runtime',label:'Native runtime',status:existsSync(path.join(this.dataDirectory,'runtime.json'))?'ok':'warning',detail:existsSync(path.join(this.dataDirectory,'runtime.json'))?'Geïnstalleerd':'Nog niet geïnstalleerd',fixable:true});for(const service of this.state.services){const info=await this.inspectPort(service.id),owner=[info.ownerName,info.ownerPid?`PID ${info.ownerPid}`:''].filter(Boolean).join(' · ');checks.push({id:`port-${service.id}`,label:`Poort ${info.port} (${service.name})`,status:info.status==='occupied'?'warning':'ok',detail:info.status==='free'?'Beschikbaar':info.status==='localdeck'?'In gebruik door LocalDeck':`Extern in gebruik${owner?` door ${owner}`:''}`,fixable:info.status==='occupied'})}this.state.diagnostics=checks;this.emit('change',this.snapshot());return this.snapshot()};const result=this.diagnosticsQueue.then(run,run);this.diagnosticsQueue=result.then(()=>undefined,()=>undefined);return result}
 async installRuntime(scriptPath:string,packageDirectory?:string){
  if(this.state.installation.status==='installing')throw new Error('Installatie is al bezig.');
  this.state.installation={status:'installing',progress:2,message:'Installer voorbereiden',startedAt:new Date().toISOString()};this.emit('change',this.snapshot());
  const args=['-NoLogo','-NoProfile','-NonInteractive','-WindowStyle','Hidden','-ExecutionPolicy','Bypass','-File',scriptPath,'-InstallDir',this.dataDirectory];if(packageDirectory)args.push('-PackageDir',packageDirectory);
  const child=spawn('powershell.exe',args,{windowsHide:true,shell:false,stdio:['ignore','pipe','pipe']});let step=5,lastMessage='';
  const consume=(d:unknown)=>{const lines=String(d).split(/\r?\n/).map(x=>x.trim()).filter(Boolean);for(const line of lines){lastMessage=line;step=Math.min(94,step+4);this.state.installation.progress=step;this.state.installation.message=line;this.log('info','Installer',line)}};
  child.stdout.on('data',consume);child.stderr.on('data',consume);
  return new Promise<AppState>((resolve,reject)=>{child.once('error',error=>{this.state.installation={status:'failed',progress:0,message:`Installer kon niet starten: ${error.message}`,finishedAt:new Date().toISOString()};reject(new Error(this.state.installation.message))});child.on('exit',async code=>{if(code===0&&this.runtimeComplete()){this.state.installation={status:'installed',progress:100,message:'Runtime geïnstalleerd',finishedAt:new Date().toISOString()};this.state.settings.simulationMode=false;this.state.services.forEach(s=>s.installed=this.binaryExists(s.id));this.configurePhpMyAdminAutoLogin();this.writeVhosts();this.writeLandingMetadata();this.persist();await this.runDiagnostics();this.log('success','Installer','Native Windows-runtime is gereed.');resolve(this.snapshot())}else{const detail=lastMessage?`: ${lastMessage}`:'';this.state.installation={status:'failed',progress:0,message:`Installatie afgebroken (code ${code??'onbekend'})${detail}`,finishedAt:new Date().toISOString()};this.log('error','Installer',this.state.installation.message);reject(new Error(this.state.installation.message))}})})
  }
 async toggleService(id:ServiceId){const service=this.state.services.find(s=>s.id===id);if(!service)throw new Error('Onbekende service');if(!service.installed&&!this.state.settings.simulationMode)throw new Error(`${service.name} ontbreekt. Installeer eerst de Windows-runtime via Installatie → Nu installeren.`);if(this.state.settings.runtimeMode==='windows-service'&&!this.state.settings.simulationMode)throw new Error('Gebruik Windows-servicebeheer voor deze service.');if(!this.state.settings.simulationMode){await this.toggleNative(service);this.serviceIntent[id]=service.status==='running';this.persist();return this.snapshot()}if(service.status==='running'){service.status='stopped';delete service.pid;this.log('warning',service.name,`${service.name} is gestopt.`)}else{service.status='starting';this.emit('change',this.snapshot());await new Promise(r=>setTimeout(r,350));service.status='running';service.health='healthy';service.pid=Math.floor(2000+Math.random()*20000);this.log('success',service.name,`${service.name} luistert op poort ${service.port}.`)}this.serviceIntent[id]=service.status==='running';this.persist();return this.snapshot()}
 private terminateProcess(child:ChildProcess){if(child.exitCode!==null)return;try{if(child.pid)spawnSync('taskkill.exe',['/PID',String(child.pid),'/T','/F'],{windowsHide:true,stdio:'ignore'})}catch{}try{child.kill()}catch{}}
 private stopNativeProcess(service:ServiceState,child:ChildProcess){if(service.id==='mysql'){const admin=path.join(this.dataDirectory,'mysql','bin','mysqladmin.exe'),env={...process.env,MYSQL_PWD:this.mysqlPassword()};spawnSync(admin,[`--defaults-file=${path.join(this.dataDirectory,'my.ini')}`,'-u','root','-h','127.0.0.1','-P',String(service.port),'shutdown'],{windowsHide:true,stdio:'ignore',env})}this.terminateProcess(child)}
 private async toggleNative(service:ServiceState){if(service.id==='php')return this.togglePhpPools(service);const current=this.processes.get(service.id);if(current&&current.exitCode===null){this.processes.delete(service.id);service.status='stopped';this.stopNativeProcess(service,current);await new Promise(r=>setTimeout(r,250));service.health='unknown';delete service.pid;this.log('warning',service.name,`${service.name} is gestopt.`);return this.snapshot()}const exe=this.binaryPath(service.id);if(!existsSync(exe))throw new Error(`${service.name} is niet geïnstalleerd.`);if(!(await this.portFree(service.port)))throw new Error(`Poort ${service.port} is al in gebruik. Kies een andere poort of gebruik Automatisch zoeken.`);this.validateServiceConfig(service);let args:string[]=[];const env:NodeJS.ProcessEnv={...process.env};if(service.id==='apache')args=['-f',path.join(this.dataDirectory,'apache','conf','httpd-localdeck.conf'),'-DFOREGROUND'];if(service.id==='mysql')args=[`--defaults-file=${path.join(this.dataDirectory,'my.ini')}`,`--port=${service.port}`,'--console'];if(service.id==='phpmyadmin'){const password=this.mysqlPassword();if(!password)throw new Error('Het MySQL-wachtwoord voor phpMyAdmin kon niet veilig worden gelezen.');env.LOCALDECK_MYSQL_PASSWORD=password;args=['-S',`127.0.0.1:${service.port}`,'-t',path.join(this.dataDirectory,'phpmyadmin')]}if(service.id==='mail'){args=['--listen',`127.0.0.1:${service.port}`,'--smtp',`127.0.0.1:${this.state.settings.smtpPort}`,'--database',path.join(this.dataDirectory,'mailpit.db'),'--disable-version-check','--max',String(this.state.settings.mailMaxMessages)];if(this.state.settings.mailMaxAgeDays>0)args.push('--max-age',`${this.state.settings.mailMaxAgeDays}d`)}if(service.id==='redis')args=['--bind','127.0.0.1','--port',String(service.port)];const child=spawn(exe,args,{cwd:path.dirname(exe),env,windowsHide:true,stdio:['ignore','pipe','pipe']});this.processes.set(service.id,child);service.status='starting';service.error=undefined;this.emit('change',this.snapshot());child.stdout?.on('data',d=>this.log('info',service.name,String(d).trim()));child.stderr?.on('data',d=>this.log('info',service.name,String(d).trim()));child.on('exit',code=>{const unexpected=this.processes.get(service.id)===child;if(!unexpected)return;this.processes.delete(service.id);service.status=code&&code!==0?'error':'stopped';service.health='unknown';service.error=code?`Proces stopte met code ${code}`:undefined;delete service.pid;this.emit('change',this.snapshot());if(code&&this.state.settings.autoRestart&&this.state.settings.runtimeMode==='application'&&(service.restartCount??0)<3){service.restartCount=(service.restartCount??0)+1;this.log('warning',service.name,`Onverwacht gestopt; herstelpoging ${service.restartCount}/3.`);setTimeout(()=>void this.toggleNative(service).catch(e=>this.log('error',service.name,String(e))),1500)}});const healthy=child.exitCode===null&&await this.waitForPort(service.port);if(child.exitCode!==null||!healthy){this.processes.delete(service.id);this.terminateProcess(child);service.status='error';service.health='unreachable';throw new Error(`${service.name} kon niet gezond starten. Bekijk het logboek.`)}service.status='running';service.health='healthy';service.pid=child.pid;service.restartCount=0;this.log('success',service.name,`${service.name} is gezond op poort ${service.port}.`);return this.snapshot()}
 private async togglePhpPools(service:ServiceState){if(this.phpProcesses.size){const pools=[...this.phpProcesses.values()];this.phpProcesses.clear();service.status='stopped';for(const child of pools)this.terminateProcess(child);await new Promise(resolve=>setTimeout(resolve,250));service.health='unknown';delete service.pid;this.log('warning','PHP','Alle PHP-versiepools zijn gestopt.');return this.snapshot()}const versions=[...new Set([this.state.settings.defaultPhpVersion,...this.state.projects.map(project=>project.phpVersion)])].sort(),missing=versions.filter(version=>!existsSync(path.join(this.phpDirectory(version),'php-cgi.exe')));if(missing.length)throw new Error(`PHP ${missing.join(', ')} ontbreekt. Installeer deze versie(s) eerst.`);for(const version of versions){const port=this.phpPort(version);if(!(await this.portFree(port)))throw new Error(`PHP ${version} kan niet starten: poort ${port} is bezet.`)}service.status='starting';this.emit('change',this.snapshot());for(const version of versions){const directory=this.phpDirectory(version),exe=path.join(directory,'php-cgi.exe'),port=this.phpPort(version),child=spawn(exe,['-b',`127.0.0.1:${port}`],{cwd:directory,env:{...process.env,PHPRC:directory,PHP_FCGI_CHILDREN:'2'},windowsHide:true,stdio:['ignore','pipe','pipe']});this.phpProcesses.set(version,child);child.stdout?.on('data',data=>this.log('info',`PHP ${version}`,String(data).trim()));child.stderr?.on('data',data=>this.log('warning',`PHP ${version}`,String(data).trim()));child.on('exit',code=>{this.phpProcesses.delete(version);if(code&&service.status==='running'){service.status='error';service.error=`PHP ${version} stopte met code ${code}`;this.emit('change',this.snapshot())}})}const healthy=(await Promise.all(versions.map(version=>this.waitForPort(this.phpPort(version))))).every(Boolean),failed=versions.find(version=>this.phpProcesses.get(version)?.exitCode!==null);if(failed||!healthy){for(const child of this.phpProcesses.values())this.terminateProcess(child);this.phpProcesses.clear();throw new Error(`PHP ${failed||versions.join(', ')} kon niet gezond starten.`)}service.status='running';service.health='healthy';service.pid=this.phpProcesses.get(this.state.settings.defaultPhpVersion)?.pid;service.restartCount=0;this.log('success','PHP',`${versions.length} geïsoleerde PHP-pool${versions.length===1?'':'s'} actief: ${versions.map(version=>`${version}:${this.phpPort(version)}`).join(', ')}.`);return this.snapshot()}
 private validateServiceConfig(service:ServiceState){if(service.id!=='apache')return;const r=spawnSync(this.binaryPath('apache'),['-t','-f',path.join(this.dataDirectory,'apache','conf','httpd-localdeck.conf')],{encoding:'utf8',windowsHide:true});if(r.status!==0)throw new Error(`Apache-configuratie is ongeldig: ${(r.stderr||r.stdout).trim()}`)}
 async toggleAll(start:boolean){const startOrder:ServiceId[]=['mysql','php','apache','phpmyadmin','redis','mail'],order=start?startOrder:[...startOrder].reverse();if(start&&!this.state.settings.simulationMode&&order.some(id=>!this.state.services.find(service=>service.id===id)?.installed))throw new Error('De Windows-runtime ontbreekt of is onvolledig. Open Installatie en klik op Nu installeren.');for(const id of order){const s=this.state.services.find(x=>x.id===id)!;if(s.installed||this.state.settings.simulationMode){if((s.status==='running')!==start)await this.toggleService(id);this.serviceIntent[id]=start}}this.persist();return this.snapshot()}
 setServiceIntent(id:ServiceId,running:boolean){this.serviceIntent[id]=running;this.persist();return this.snapshot()}
 setAllServiceIntent(running:boolean){for(const service of this.state.services)this.serviceIntent[service.id]=running;this.persist();return this.snapshot()}
 beginSession(){writeFileSync(this.sessionFile,JSON.stringify({sessionId:this.state.crashRecovery.sessionId,startedAt:new Date().toISOString(),cleanExit:false,desiredServices:this.serviceIntent},null,2));return this.snapshot()}
 async recoverFromCrash(){if(!this.state.crashRecovery.uncleanShutdownDetected||!this.state.settings.restoreServicesAfterCrash||!this.state.settings.autoRestart){this.state.crashRecovery={...this.state.crashRecovery,status:'clean',detail:this.state.crashRecovery.uncleanShutdownDetected?'Automatisch herstel is uitgeschakeld.':'Geen crashherstel nodig.',checkedAt:new Date().toISOString()};return this.snapshot()}this.state.crashRecovery={...this.state.crashRecovery,status:'recovering',detail:'Eerdere servicestatus wordt veilig hersteld.',checkedAt:new Date().toISOString()};const recovered:ServiceId[]=[];try{await this.refreshServiceStates();for(const service of this.state.services)if(this.serviceIntent[service.id]&&service.status!=='running'&&(service.installed||this.state.settings.simulationMode)){if(this.state.settings.runtimeMode==='application'||this.state.settings.simulationMode){await this.toggleService(service.id);recovered.push(service.id)}}this.state.crashRecovery={...this.state.crashRecovery,status:'recovered',recoveredServices:recovered,detail:recovered.length?`${recovered.length} service(s) na de onverwachte afsluiting hersteld.`:'Alle gewenste services draaiden al.',checkedAt:new Date().toISOString()}}catch(error){this.state.crashRecovery={...this.state.crashRecovery,status:'failed',recoveredServices:recovered,detail:error instanceof Error?error.message:String(error),checkedAt:new Date().toISOString()};this.log('error','Crashherstel',this.state.crashRecovery.detail)}this.emit('change',this.snapshot());return this.snapshot()}
 async shutdown(){const services=new Map(this.state.services.map(service=>[service.id,service]));const native=[...this.processes.entries()];this.processes.clear();for(const[id,child]of native){const service=services.get(id);if(service){service.status='stopped';service.health='unknown';delete service.pid;this.stopNativeProcess(service,child)}else this.terminateProcess(child)}const phpService=services.get('php');if(phpService){phpService.status='stopped';phpService.health='unknown';delete phpService.pid}const pools=[...this.phpProcesses.values()];this.phpProcesses.clear();for(const child of pools)this.terminateProcess(child);for(const child of this.workerProcesses.values())this.terminateProcess(child);this.workerProcesses.clear();for(const worker of this.state.workers){worker.status='stopped';delete worker.pid;worker.stoppedAt=new Date().toISOString()}writeFileSync(this.sessionFile,JSON.stringify({sessionId:this.state.crashRecovery.sessionId,cleanExit:true,closedAt:new Date().toISOString(),desiredServices:this.serviceIntent},null,2));this.persist()}
 setRuntimeMode(mode:RuntimeMode){this.state.settings.runtimeMode=mode;this.persist();this.log('success','Services',mode==='windows-service'?'Windows-servicemodus is geactiveerd.':'App-modus is geactiveerd; services stoppen met LocalDeck.');return this.snapshot()}
 async refreshServiceStates(){for(const service of this.state.services){const reachable=await this.portReachable(service.port),managed=service.id==='php'?this.phpProcesses.get(this.state.settings.defaultPhpVersion):this.processes.get(service.id);service.status=reachable?'running':'stopped';service.health=reachable?'healthy':'unknown';if(reachable&&managed?.pid&&managed.exitCode===null)service.pid=managed.pid;else delete service.pid}this.emit('change',this.snapshot());return this.snapshot()}
 async validateServicePort(id:ServiceId,port:number){if(!Number.isInteger(port)||port<1||port>65535)throw new Error('Een poort moet tussen 1 en 65535 liggen.');const service=this.state.services.find(item=>item.id===id);if(!service)throw new Error('Onbekende service');const reserved=[...this.state.services.filter(item=>item.id!==id).map(item=>({port:item.port,label:item.name})),{port:this.state.settings.webControlPort,label:'Webbeheer'},{port:this.state.settings.smtpPort,label:'Mailpit SMTP'},...this.state.projects.flatMap(project=>[{port:project.profile?.httpPort,label:`${project.name} HTTP`},{port:project.profile?.httpsPort,label:`${project.name} HTTPS`}])].find(item=>item.port===port);if(reserved)throw new Error(`Poort ${port} is gereserveerd voor ${reserved.label}.`);if(port!==service.port&&!await this.portFree(port))throw new Error(`Poort ${port} is al in gebruik door een ander programma. Kies een andere poort of gebruik Port Autopilot.`);return true}
 updateServicePort(id:ServiceId,port:number,allowObservedExternal=false){if(!Number.isInteger(port)||port<1||port>65535)throw new Error('Een poort moet tussen 1 en 65535 liggen.');const s=this.state.services.find(x=>x.id===id);if(!s)throw new Error('Onbekende service');const conflict=this.state.services.find(x=>x.id!==id&&x.port===port);if(conflict)throw new Error(`Poort ${port} wordt al gebruikt door ${conflict.name}.`);if(s.status==='running'&&!allowObservedExternal)throw new Error(`Stop ${s.name} voordat je de poort wijzigt.`);const old=s.port;if(old===port)return this.snapshot();s.port=port;s.status='stopped';s.health='unknown';s.error=undefined;delete s.pid;this.invalidatePortInspection();if(id==='apache'){if(!this.state.settings.simulationMode)this.replaceConfig(path.join(this.dataDirectory,'apache','conf','httpd-localdeck.conf'),/^Listen\s+\d+/m,`Listen ${port}`);this.writeVhosts()}if(!this.state.settings.simulationMode&&id==='php')this.replaceConfig(path.join(this.dataDirectory,'apache','conf','httpd-localdeck.conf'),/proxy:fcgi:\/\/127\.0\.0\.1:\d+\/?/g,`proxy:fcgi://127.0.0.1:${port}/`);if(!this.state.settings.simulationMode&&id==='mysql')this.replaceConfig(path.join(this.dataDirectory,'my.ini'),/^port=\d+/gm,`port=${port}`);this.writeLandingMetadata();this.persist();this.log('success',s.name,`Poort gewijzigd van ${old} naar ${port}.`);return this.snapshot()}
 async autoPort(id:ServiceId){const service=this.state.services.find(s=>s.id===id);if(!service)throw new Error('Onbekende service');if(service.status==='running')throw new Error(`Stop ${service.name} voordat je de poort wijzigt.`);for(let candidate=service.port;candidate<Math.min(65535,service.port+1000);candidate++){try{await this.validateServicePort(id,candidate);return this.updateServicePort(id,candidate)}catch{}}throw new Error('Er is geen vrije, ongereserveerde poort gevonden.')}
 async inspectPort(id:ServiceId){const service=this.state.services.find(item=>item.id===id);if(!service)throw new Error('Onbekende service');const inspectedPort=service.port,occupancy=await this.portOccupancy(inspectedPort),occupied=occupancy.occupied,ownerPid=occupancy.owner?.pid,ownerName=occupancy.owner?.name,ownerPath=occupancy.owner?.path;
  const managedPids=id==='php'?[...this.phpProcesses.values()].map(child=>child.pid).filter(Boolean):[this.processes.get(id)?.pid].filter(Boolean),ownedByPid=ownerPid!==undefined&&managedPids.includes(ownerPid),relativeOwner=ownerPath?path.relative(this.dataDirectory,path.resolve(ownerPath)):'',insideRuntime=Boolean(ownerPath)&&relativeOwner!==''&&!relativeOwner.startsWith('..')&&!path.isAbsolute(relativeOwner),expectedName=id==='apache'?'httpd.exe':id==='php'?'php-cgi.exe':id==='mysql'?'mysqld.exe':id==='phpmyadmin'?'php.exe':id==='mail'?'mailpit.exe':'redis-server.exe',ownedByWindowsService=this.state.settings.runtimeMode==='windows-service'&&insideRuntime&&path.basename(ownerPath!).toLowerCase()===expectedName;const owned=occupied&&(ownedByPid||ownedByWindowsService);
  let suggested=inspectedPort;for(let candidate=occupied?Math.min(65535,inspectedPort+1):Math.max(1,inspectedPort);candidate<Math.min(65536,inspectedPort+1000);candidate++)if(!this.state.services.some(item=>item.id!==id&&item.port===candidate)&&await this.portFree(candidate)){suggested=candidate;break}const info:PortConflictInfo={serviceId:id,serviceName:service.name,port:inspectedPort,status:owned?'localdeck':occupied?'occupied':'free',ownerPid,ownerName,ownerPath,suggestedPort:suggested,checkedAt:new Date().toISOString()};if(service.port===inspectedPort)this.state.portConflicts=[...this.state.portConflicts.filter(item=>item.serviceId!==id),info];this.emit('change',this.snapshot());return info}
 private replaceConfig(file:string,pattern:RegExp,value:string){if(existsSync(file))writeFileSync(file,readFileSync(file,'utf8').replace(pattern,value))}
 private configurePhpMyAdminAutoLogin(){const directory=path.join(this.dataDirectory,'phpmyadmin'),index=path.join(directory,'index.php');if(!existsSync(index))return;const file=path.join(directory,'config.inc.php'),existing=existsSync(file)?readFileSync(file,'utf8'):'',savedSecret=existing.match(/\$cfg\['blowfish_secret'\]\s*=\s*'([^']+)'/)?.[1],secret=savedSecret&&savedSecret.length>=32?savedSecret:createHash('sha256').update(`LocalDeck:${this.dataDirectory}`).digest('hex');writeFileSync(file,`<?php\n$cfg['blowfish_secret']='${secret.replace(/\\/g,'\\\\').replace(/'/g,"\\'")}';\n$localDeckState=json_decode((string) @file_get_contents(dirname(__DIR__).'/state.json'),true);\n$localDeckMySqlPort='3306';\nforeach(($localDeckState['services']??[]) as $localDeckService){if(($localDeckService['id']??'')==='mysql'){$localDeckMySqlPort=(string)($localDeckService['port']??3306);break;}}\n$i=1;\n$cfg['Servers'][$i]['auth_type']='config';\n$cfg['Servers'][$i]['user']='root';\n$cfg['Servers'][$i]['password']=(string)(getenv('LOCALDECK_MYSQL_PASSWORD')?:'');\n$cfg['Servers'][$i]['host']='127.0.0.1';\n$cfg['Servers'][$i]['port']=$localDeckMySqlPort;\n$cfg['Servers'][$i]['connect_type']='tcp';\n$cfg['Servers'][$i]['AllowNoPassword']=false;\n`)}
 refreshDatabases(){if(this.state.settings.simulationMode)return this.snapshot();const result=this.mysql(['-N','-e',"SELECT SCHEMA_NAME,DEFAULT_CHARACTER_SET_NAME,DEFAULT_COLLATION_NAME FROM information_schema.SCHEMATA WHERE SCHEMA_NAME NOT IN ('mysql','information_schema','performance_schema','sys')"]);this.state.databases=result.trim()?result.trim().split(/\r?\n/).map(line=>{const[n,charset,collation]=line.split('\t');const old=this.state.databases.find(d=>d.name===n);return{id:old?.id??crypto.randomUUID(),name:n,charset,collation,tables:old?.tables??0,size:old?.size??'—',createdAt:old?.createdAt??new Date().toISOString()}}):[];this.persist();return this.snapshot()}
 private mysqlPassword(){const secrets=path.join(this.dataDirectory,'secrets.json');if(!existsSync(secrets))return'';try{const saved=JSON.parse(readFileSync(secrets,'utf8').replace(/^\uFEFF/,''));if(saved.mysqlRoot)return String(saved.mysqlRoot);const script=path.join(this.dataDirectory,'automation','get-secret.ps1');if(!existsSync(script))return'';const result=spawnSync('powershell.exe',['-NoLogo','-NoProfile','-NonInteractive','-WindowStyle','Hidden','-ExecutionPolicy','Bypass','-File',script,'-SecretsFile',secrets],{encoding:'utf8',windowsHide:true});return result.status===0?result.stdout.trim():''}catch{return''}}
 getDatabasePassword(){const password=this.mysqlPassword();if(!password)throw new Error('Het MySQL-wachtwoord kon niet veilig worden gelezen.');return password}
 private mysql(args:string[]){const cli=path.join(this.dataDirectory,'mysql','bin','mysql.exe');const port=String(this.state.services.find(s=>s.id==='mysql')?.port??3306);const env={...process.env,MYSQL_PWD:this.mysqlPassword()};const r=spawnSync(cli,[`--defaults-file=${path.join(this.dataDirectory,'my.ini')}`,'-u','root','-h','127.0.0.1','-P',port,...args],{encoding:'utf8',windowsHide:true,maxBuffer:64*1024*1024,env});if(r.status!==0)throw new Error(r.stderr||'MySQL-opdracht mislukt. Start MySQL eerst.');return r.stdout}
 private mysqlInput(sql:string){const cli=path.join(this.dataDirectory,'mysql','bin','mysql.exe'),port=String(this.state.services.find(service=>service.id==='mysql')?.port??3306),env={...process.env,MYSQL_PWD:this.mysqlPassword()},result=spawnSync(cli,[`--defaults-file=${path.join(this.dataDirectory,'my.ini')}`,'-u','root','-h','127.0.0.1','-P',port,'--batch'],{input:sql,encoding:'utf8',windowsHide:true,maxBuffer:64*1024*1024,env});if(result.status!==0)throw new Error(result.stderr||'MySQL-opdracht mislukt. Start MySQL eerst.');return result.stdout}
 createDatabase(name:string,charset='utf8mb4',collation='utf8mb4_unicode_ci'){const clean=name.trim();if(!/^[a-zA-Z][a-zA-Z0-9_]{0,63}$/.test(clean))throw new Error('Gebruik letters, cijfers en underscores.');if(this.state.databases.some(d=>d.name.toLowerCase()===clean.toLowerCase()))throw new Error('Deze database bestaat al.');if(!this.state.settings.simulationMode)this.mysql(['-e',`CREATE DATABASE \`${clean}\` CHARACTER SET ${charset} COLLATE ${collation}`]);this.state.databases.push({id:crypto.randomUUID(),name:clean,charset,collation,tables:0,size:'0 B',createdAt:new Date().toISOString()});this.persist();this.log('success','MySQL',`Database ${clean} aangemaakt.`);return this.snapshot()}
 deleteDatabase(id:string){const db=this.state.databases.find(d=>d.id===id);if(db&&!this.state.settings.simulationMode){if(this.state.settings.autoBackup)this.exportDatabase(id,path.join(this.dataDirectory,'backups',`${db.name}-${Date.now()}.sql`));this.mysql(['-e',`DROP DATABASE \`${db.name}\``])}this.state.databases=this.state.databases.filter(d=>d.id!==id);this.persist();if(db)this.log('warning','MySQL',`Database ${db.name} verwijderd.`);return this.snapshot()}
 importDatabase(id:string,file:string){const db=this.state.databases.find(d=>d.id===id);if(!db)throw new Error('Database niet gevonden.');if(this.state.settings.simulationMode){this.log('success','MySQL',`${path.basename(file)} gesimuleerd geïmporteerd in ${db.name}.`);return this.snapshot()}const cli=path.join(this.dataDirectory,'mysql','bin','mysql.exe');const input=readFileSync(file),env={...process.env,MYSQL_PWD:this.mysqlPassword()};const r=spawnSync(cli,[`--defaults-file=${path.join(this.dataDirectory,'my.ini')}`,'-u','root','-h','127.0.0.1','-P',String(this.state.services.find(s=>s.id==='mysql')?.port??3306),db.name],{input,encoding:'buffer',windowsHide:true,maxBuffer:64*1024*1024,env});if(r.status!==0)throw new Error(String(r.stderr));this.log('success','MySQL',`${path.basename(file)} geïmporteerd in ${db.name}.`);return this.snapshot()}
 importDatabaseSanitized(id:string,file:string){const database=this.state.databases.find(item=>item.id===id);if(!database)throw new Error('Database niet gevonden.');this.backupDatabase(id);this.importDatabase(id,file);this.anonymizeDatabase(id,false);this.log('success','Privacy-import',`${path.basename(file)} geïmporteerd en direct geanonimiseerd in ${database.name}.`);return this.snapshot()}
 exportDatabase(id:string,file:string){const db=this.state.databases.find(d=>d.id===id);if(!db)throw new Error('Database niet gevonden.');mkdirSync(path.dirname(file),{recursive:true});if(this.state.settings.simulationMode)writeFileSync(file,`-- LocalDeck simulatieback-up voor ${db.name}\n`);else{const dump=path.join(this.dataDirectory,'mysql','bin','mysqldump.exe'),env={...process.env,MYSQL_PWD:this.mysqlPassword()};const r=spawnSync(dump,[`--defaults-file=${path.join(this.dataDirectory,'my.ini')}`,'-u','root','-h','127.0.0.1','-P',String(this.state.services.find(s=>s.id==='mysql')?.port??3306),'--single-transaction','--routines','--events',db.name],{encoding:'buffer',windowsHide:true,maxBuffer:256*1024*1024,env});if(r.status!==0)throw new Error(String(r.stderr));writeFileSync(file,r.stdout)}this.log('success','MySQL',`${db.name} geëxporteerd naar ${file}.`);return this.snapshot()}
 backupDatabase(id:string){const db=this.state.databases.find(d=>d.id===id);if(!db)throw new Error('Database niet gevonden.');const dir=path.join(this.dataDirectory,'backups');mkdirSync(dir,{recursive:true});const stamp=new Date().toISOString().replace(/[:.]/g,'-');this.exportDatabase(id,path.join(dir,`${db.name}-${stamp}.sql`));this.refreshBackups();this.pruneBackups();return this.snapshot()}
 restoreBackup(id:string){const backup=this.state.backups.find(b=>b.id===id);if(!backup)throw new Error('Back-up niet gevonden.');let db=this.state.databases.find(d=>d.name===backup.database);if(!db){this.createDatabase(backup.database);db=this.state.databases.find(d=>d.name===backup.database)!}this.importDatabase(db.id,backup.file);this.log('success','Back-up',`${backup.database} is hersteld.`);return this.snapshot()}
 refreshBackups(){const dir=path.join(this.dataDirectory,'backups');if(!existsSync(dir)){this.state.backups=[];return this.snapshot()}this.state.backups=readdirSync(dir).filter(f=>f.toLowerCase().endsWith('.sql')).map(file=>{const full=path.join(dir,file),stats=statSync(full),match=file.match(/^(.*?)-\d{4}-\d{2}-\d{2}T/);return{id:file,database:match?.[1]??path.basename(file,'.sql'),file:full,size:this.formatBytes(stats.size),createdAt:stats.mtime.toISOString()} satisfies BackupInfo}).sort((a,b)=>b.createdAt.localeCompare(a.createdAt));return this.snapshot()}
 private pruneBackups(){const keep=Math.max(1,this.state.settings.backupRetention);const grouped=new Map<string,BackupInfo[]>();for(const backup of this.state.backups){const list=grouped.get(backup.database)??[];list.push(backup);grouped.set(backup.database,list)}for(const list of grouped.values())for(const backup of list.slice(keep)){try{unlinkSync(backup.file)}catch{}}this.refreshBackups()}
 private formatBytes(bytes:number){if(bytes<1024)return`${bytes} B`;if(bytes<1024*1024)return`${(bytes/1024).toFixed(1)} KB`;return`${(bytes/1024/1024).toFixed(1)} MB`}
 addMailAddress(address:string,description:string,projectId?:string){const clean=address.trim().toLowerCase();if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(clean))throw new Error('Vul een geldig lokaal e-mailadres in.');if(this.state.mailAddresses.some(m=>m.address===clean))throw new Error('Dit e-mailadres bestaat al.');const item:MailAddress={id:crypto.randomUUID(),address:clean,description:description.trim(),projectId:projectId||undefined,createdAt:new Date().toISOString()};this.state.mailAddresses.push(item);this.persist();this.log('success','Mailpit',`${clean} toegevoegd. Mailpit vangt alle lokale berichten voor dit adres op.`);return this.snapshot()}
 deleteMailAddress(id:string){const item=this.state.mailAddresses.find(m=>m.id===id);this.state.mailAddresses=this.state.mailAddresses.filter(m=>m.id!==id);this.persist();if(item)this.log('warning','Mailpit',`${item.address} verwijderd uit het adresboek.`);return this.snapshot()}
 private refreshPhpExtensions(){const phpDir=existsSync(path.join(this.dataDirectory,`php-${this.state.settings.defaultPhpVersion}`))?path.join(this.dataDirectory,`php-${this.state.settings.defaultPhpVersion}`):path.join(this.dataDirectory,'php');const ini=path.join(phpDir,'php.ini'),content=existsSync(ini)?readFileSync(ini,'utf8'):'';this.state.phpExtensions=PHP_EXTENSIONS.map(ext=>{const dll=path.join(phpDir,'ext',`php_${ext.id}.dll`);const available=existsSync(dll)||['mysqli','pdo_mysql','mbstring','openssl','curl','zip'].includes(ext.id);const enabled=new RegExp(`^\\s*extension\\s*=\\s*(?:php_)?${ext.id}(?:\\.dll)?\\s*$`,'mi').test(content)||ext.id==='xdebug'&&/^\s*zend_extension\s*=.*xdebug/mi.test(content);return{...ext,available,enabled}})}
 togglePhpExtension(id:string){const extension=PHP_EXTENSIONS.find(e=>e.id===id);if(!extension)throw new Error('PHP-extensie niet gevonden.');const phpDir=existsSync(path.join(this.dataDirectory,`php-${this.state.settings.defaultPhpVersion}`))?path.join(this.dataDirectory,`php-${this.state.settings.defaultPhpVersion}`):path.join(this.dataDirectory,'php');const ini=path.join(phpDir,'php.ini');if(!existsSync(ini))throw new Error('Installeer eerst de PHP-runtime.');const dll=path.join(phpDir,'ext',`php_${id}.dll`);if(!existsSync(dll)&&id==='xdebug')throw new Error('De Xdebug-DLL ontbreekt voor deze PHP-versie. Installeer eerst een passende Xdebug-build.');let content=readFileSync(ini,'utf8');const line=id==='xdebug'?`zend_extension="${dll.replace(/\\/g,'/')}"`:`extension=${id}`;const active=id==='xdebug'?/^\s*zend_extension\s*=.*xdebug.*$/mi:new RegExp(`^\\s*extension\\s*=\\s*(?:php_)?${id}(?:\\.dll)?\\s*$`,'mi');if(active.test(content))content=content.replace(active,match=>`;${match}`);else{const disabled=id==='xdebug'?/^\s*;\s*zend_extension\s*=.*xdebug.*$/mi:new RegExp(`^\\s*;\\s*extension\\s*=\\s*(?:php_)?${id}(?:\\.dll)?\\s*$`,'mi');content=disabled.test(content)?content.replace(disabled,line):`${content.trimEnd()}\r\n${line}\r\n`}writeFileSync(ini,content);this.refreshPhpExtensions();this.log('success','PHP',`${extension.label} is ${this.state.phpExtensions.find(e=>e.id===id)?.enabled?'ingeschakeld':'uitgeschakeld'}.`);return this.snapshot()}
 async runComposer(projectId:string,command:'install'|'update'|'audit'){const project=this.state.projects.find(p=>p.id===projectId);if(!project)throw new Error('Project niet gevonden.');const php=path.join(this.phpDirectory(project.phpVersion),'php.exe'),composer=path.join(this.dataDirectory,'composer.phar');if(!existsSync(php)||!existsSync(composer))throw new Error(`PHP ${project.phpVersion} of Composer ontbreekt.`);this.log('info','Composer',`${command} gestart voor ${project.name} met PHP ${project.phpVersion}.`);const args=[composer,command,'--no-interaction'];if(command==='audit')args.push('--format=plain');return new Promise<AppState>((resolve,reject)=>{const child=spawn(php,args,{cwd:project.path,windowsHide:true,shell:false,stdio:['ignore','pipe','pipe'],env:{...process.env,...project.profile?.env,LOCALDECK_PHP_VERSION:project.phpVersion}});child.stdout.on('data',d=>this.log('info','Composer',String(d).trim()));child.stderr.on('data',d=>this.log('warning','Composer',String(d).trim()));child.once('error',reject);child.on('exit',code=>{if(code===0){this.log('success','Composer',`${command} voor ${project.name} is voltooid.`);resolve(this.snapshot())}else reject(new Error(`Composer ${command} stopte met code ${code}.`))})})}
 addProject(input:Omit<Project,'id'|'createdAt'>){const domain=input.domain.trim().toLowerCase(),projectPath=path.resolve(input.path),documentRoot=path.resolve(input.documentRoot||projectPath);if(!/^(?=.{1,253}$)(?:[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?\.)+(?:localhost|test)$/.test(domain))throw new Error('Gebruik een geldig lokaal .localhost- of .test-domein.');if(!existsSync(projectPath)||!existsSync(documentRoot))throw new Error('De projectmap of document root bestaat niet.');if(!this.state.phpVersions.some(item=>item.version===input.phpVersion))throw new Error('Deze PHP-versie wordt niet ondersteund.');if(this.state.projects.some(p=>p.domain===domain))throw new Error('Dit domein bestaat al');let httpPort=10080;const used=new Set([...this.state.services.map(service=>service.port),...this.state.projects.flatMap(project=>[project.profile?.httpPort,project.profile?.httpsPort])].filter((port):port is number=>Boolean(port)));while(used.has(httpPort)||used.has(httpPort+1))httpPort+=2;const requestedHttp=input.profile?.httpPort??httpPort,requestedHttps=input.profile?.httpsPort??httpPort+1;for(const port of[requestedHttp,requestedHttps])if(!Number.isInteger(port)||port<1024||port>65535)throw new Error('Geïsoleerde projectpoorten moeten tussen 1024 en 65535 liggen.');if(requestedHttp===requestedHttps||used.has(requestedHttp)||used.has(requestedHttps))throw new Error('Kies twee vrije, verschillende projectpoorten.');const p=normalizeProject({...input,path:projectPath,domain,documentRoot,id:crypto.randomUUID(),createdAt:new Date().toISOString(),lastOpenedAt:new Date().toISOString(),profile:{...input.profile,httpPort:requestedHttp,httpsPort:requestedHttps}as ProjectProfile});this.state.projects.push(p);try{this.writeProjectManifest(p.id);this.writeVhosts();this.refreshBlueprints();this.refreshComponents();this.runProjectDoctor(p.id);this.persist()}catch(error){this.state.projects=this.state.projects.filter(item=>item.id!==p.id);throw error}this.log('success','Projecten',`${p.name} toegevoegd op ${p.secure?'https':'http'}://${domain}.`);return this.snapshot()}
 projectRemovalPlan(id:string){
  const project=this.state.projects.find(item=>item.id===id);if(!project)throw new Error('Project niet gevonden.');const projectIds=new Set<string>([id]);let changed=true;while(changed){changed=false;for(const candidate of this.state.projects)if(candidate.parentProjectId&&projectIds.has(candidate.parentProjectId)&&!projectIds.has(candidate.id)){projectIds.add(candidate.id);changed=true}}
  const projects=this.state.projects.filter(item=>projectIds.has(item.id)),branches=this.state.branchEnvironments.filter(item=>projectIds.has(item.projectId)||projectIds.has(item.childProjectId)),databaseNames=[...new Set([...projects.map(item=>item.profile?.databaseName),...branches.map(item=>item.databaseName)].filter((name):name is string=>Boolean(name)))],sharedDatabases=databaseNames.filter(name=>this.state.projects.some(item=>!projectIds.has(item.id)&&item.profile?.databaseName===name));
  return{project,projectIds,projects,branches,databaseNames,sharedDatabases,snapshotCount:this.state.snapshots.filter(item=>projectIds.has(item.projectId)).length,mailAddressCount:this.state.mailAddresses.filter(item=>item.projectId&&projectIds.has(item.projectId)).length,workerCount:this.state.workers.filter(item=>projectIds.has(item.projectId)).length,certificateCount:projects.filter(item=>this.certificatePaths(item).certificateFile&&existsSync(this.certificatePaths(item).certificateFile)).length}
 }
 private inside(base:string,target:string){const relative=path.relative(path.resolve(base),path.resolve(target));return relative!==''&&!relative.startsWith('..')&&!path.isAbsolute(relative)}
 private validateProjectDeletionPath(target:string,projectIds:Set<string>){const resolved=path.resolve(target),root=path.parse(resolved).root,branchRoot=path.join(this.dataDirectory,'branches'),managedRoots=[this.projectsDirectory,this.webRootDirectory,branchRoot].map(item=>path.resolve(item)),exactForbidden=[this.dataDirectory,...managedRoots,process.env.USERPROFILE].filter((item):item is string=>Boolean(item)).map(item=>path.resolve(item)),systemRoots=[process.env.SystemRoot,process.env.ProgramFiles,process.env['ProgramFiles(x86)'],process.env.ProgramData].filter((item):item is string=>Boolean(item)).map(item=>path.resolve(item)),insideData=this.inside(this.dataDirectory,resolved),insideManagedRoot=managedRoots.some(item=>this.inside(item,resolved));if(resolved===root||exactForbidden.some(item=>item.toLowerCase()===resolved.toLowerCase())||systemRoots.some(item=>item.toLowerCase()===resolved.toLowerCase()||this.inside(item,resolved))||insideData&&!insideManagedRoot||[this.dataDirectory,...managedRoots].some(item=>this.inside(resolved,item)))throw new Error(`Veiligheidsstop: de projectmap is te breed om te verwijderen (${resolved}).`);const nested=this.state.projects.find(item=>!projectIds.has(item.id)&&this.inside(resolved,item.path));if(nested)throw new Error(`Veiligheidsstop: ${nested.name} staat ook binnen deze map. Verwijder of verplaats dat project eerst.`);return resolved}
 private removeInside(base:string,target:string,recursive=false){const resolved=path.resolve(target);if(!this.inside(base,resolved))return;if(existsSync(resolved))rmSync(resolved,{recursive,force:true,maxRetries:4,retryDelay:150})}
 removeProject(id:string,confirmation:string){
  const plan=this.projectRemovalPlan(id);if(confirmation!==plan.project.name)throw new Error(`Typ exact "${plan.project.name}" om volledige verwijdering te bevestigen.`);if(plan.sharedDatabases.length)throw new Error(`Verwijderen gestopt: database ${plan.sharedDatabases.join(', ')} is ook aan een ander project gekoppeld.`);
  const targets=[...new Set(plan.projects.map(project=>this.validateProjectDeletionPath(project.path,plan.projectIds)))].sort((left,right)=>right.length-left.length);for(const worker of this.state.workers.filter(item=>plan.projectIds.has(item.projectId)))if(worker.status==='running')this.stopWorker(worker.id);
  for(const environment of plan.branches){const parent=this.state.projects.find(item=>item.id===environment.projectId);if(!this.state.settings.simulationMode&&parent&&!plan.projectIds.has(parent.id)){const git=spawnSync('where.exe',['git.exe'],{encoding:'utf8',windowsHide:true});if(git.status===0)spawnSync(git.stdout.trim().split(/\r?\n/)[0],['-C',parent.path,'worktree','remove','--force',environment.path],{encoding:'utf8',windowsHide:true})}}
  for(const databaseName of plan.databaseNames){const database=this.state.databases.find(item=>item.name===databaseName);if(database)this.deleteDatabase(database.id)}
  const snapshots=this.state.snapshots.filter(item=>plan.projectIds.has(item.projectId));for(const snapshot of snapshots){this.removeInside(this.snapshotDirectory(),snapshot.file);this.removeInside(this.snapshotDirectory(),path.join(this.snapshotDirectory(),`${snapshot.id}.snapshot.json`))}
  for(const report of this.state.testLabReports.filter(item=>plan.projectIds.has(item.projectId)))if(report.screenshot)this.removeInside(path.join(this.dataDirectory,'testlab'),report.screenshot)
  this.refreshBackups();for(const backup of this.state.backups.filter(item=>plan.databaseNames.includes(item.database)))this.removeInside(path.join(this.dataDirectory,'backups'),backup.file);for(const report of this.state.engineeringReports.filter(item=>item.projectId&&plan.projectIds.has(item.projectId)))for(const artifact of report.artifacts)this.removeInside(path.join(this.dataDirectory,'reports'),artifact,statSync(artifact,{throwIfNoEntry:false})?.isDirectory()??false);
  for(const project of plan.projects){this.removeInside(path.join(this.dataDirectory,'certs','projects'),this.certificatePaths(project).directory,true);this.removeInside(path.join(this.dataDirectory,'dev-pages'),path.join(this.dataDirectory,'dev-pages',project.id),true);this.removeInside(path.join(this.dataDirectory,'xdebug'),path.join(this.dataDirectory,'xdebug',project.id),true);this.removeInside(path.join(this.dataDirectory,'logs'),path.join(this.dataDirectory,'logs',`${project.id}-access.log`));this.removeInside(path.join(this.dataDirectory,'logs'),path.join(this.dataDirectory,'logs',`${project.id}-error.log`))}
  for(const target of targets)if(existsSync(target))rmSync(target,{recursive:true,force:true,maxRetries:4,retryDelay:150});
  this.state.securityFindings=this.state.securityFindings.filter(item=>!item.projectId||!plan.projectIds.has(item.projectId));this.state.portBindings=this.state.portBindings.filter(item=>![...plan.projectIds].some(projectId=>item.id.startsWith(`project-${projectId}-`)));
  this.state.projects=this.state.projects.filter(item=>!plan.projectIds.has(item.id));this.state.branchEnvironments=this.state.branchEnvironments.filter(item=>!plan.projectIds.has(item.projectId)&&!plan.projectIds.has(item.childProjectId));this.state.projectHealth=this.state.projectHealth.filter(item=>!plan.projectIds.has(item.projectId));this.state.productionParity=this.state.productionParity.filter(item=>!plan.projectIds.has(item.projectId));this.state.preflightReports=this.state.preflightReports.filter(item=>!plan.projectIds.has(item.projectId));this.state.testLabReports=this.state.testLabReports.filter(item=>!plan.projectIds.has(item.projectId));this.state.engineeringReports=this.state.engineeringReports.filter(item=>!item.projectId||!plan.projectIds.has(item.projectId));this.state.inspectorEvents=this.state.inspectorEvents.filter(item=>!item.projectId||!plan.projectIds.has(item.projectId));this.state.mailAddresses=this.state.mailAddresses.filter(item=>!item.projectId||!plan.projectIds.has(item.projectId));this.state.tasks=this.state.tasks.filter(item=>!item.projectId||!plan.projectIds.has(item.projectId));this.state.workers=this.state.workers.filter(item=>!plan.projectIds.has(item.projectId));this.state.shares=this.state.shares.filter(item=>!plan.projectIds.has(item.projectId));this.state.replayCaptures=this.state.replayCaptures.filter(item=>!item.projectId||!plan.projectIds.has(item.projectId));const workspaces=this.state.apiWorkspaces.filter(item=>item.projectId&&plan.projectIds.has(item.projectId)).map(item=>item.id);this.state.apiWorkspaces=this.state.apiWorkspaces.filter(item=>!item.projectId||!plan.projectIds.has(item.projectId));this.state.apiRuns=this.state.apiRuns.filter(item=>!workspaces.includes(item.workspaceId));this.state.startProfiles=this.state.startProfiles.map(item=>({...item,projectIds:item.projectIds.filter(projectId=>!plan.projectIds.has(projectId))}));this.state.actionOperations=this.state.actionOperations.filter(item=>!item.projectId||!plan.projectIds.has(item.projectId));this.state.safeChanges=this.state.safeChanges.filter(item=>!plan.projectIds.has(item.targetId));this.state.snapshots=this.state.snapshots.filter(item=>!plan.projectIds.has(item.projectId));this.vaultRecords=this.vaultRecords.filter(item=>!item.projectId||!plan.projectIds.has(item.projectId));this.saveVault();this.refreshBackups();this.writeVhosts();this.refreshBlueprints();this.refreshComponents();this.refreshRuntimeLifecycle();this.persist();this.log('warning','Projecten',`${plan.project.name} volledig verwijderd: ${plan.projects.length} projectmap(pen), ${plan.databaseNames.length} database(s), SSL, snapshots en gekoppelde LocalDeck-gegevens.`);return this.snapshot()
 }
 importXampp(xamppDirectory:string){const root=path.resolve(xamppDirectory),htdocs=path.join(root,'htdocs');if(!existsSync(htdocs)||!existsSync(path.join(root,'xampp-control.exe')))throw new Error('Dit lijkt geen geldige XAMPP-map te zijn.');const reserved=new Set(['dashboard','xampp','webalizer','img']);let added=0;for(const name of readdirSync(htdocs)){const projectPath=path.join(htdocs,name);if(reserved.has(name.toLowerCase())||!statSync(projectPath).isDirectory())continue;const files=readdirSync(projectPath);if(!files.some(file=>['index.php','composer.json','wp-config.php'].includes(file.toLowerCase())))continue;let slug=name.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||`xampp-${added+1}`,domain=this.localDomain(slug),suffix=2;while(this.state.projects.some(project=>project.domain===domain))domain=this.localDomain(`${slug}-${suffix++}`);const isFramework=existsSync(path.join(projectPath,'public','index.php'));this.state.projects.push({id:crypto.randomUUID(),name,domain,path:projectPath,documentRoot:isFramework?path.join(projectPath,'public'):projectPath,phpVersion:this.state.settings.defaultPhpVersion,secure:true,createdAt:new Date().toISOString(),template:existsSync(path.join(projectPath,'wp-config.php'))?'wordpress':existsSync(path.join(projectPath,'artisan'))?'laravel':existsSync(path.join(projectPath,'bin','console'))?'symfony':'custom'});added++}this.writeVhosts();this.runProjectDoctor();this.persist();this.log(added?'success':'warning','XAMPP',added?`${added} projecten uit XAMPP geïmporteerd. Exporteer databases als SQL en importeer ze via Databases.`:'Geen geschikte projecten in XAMPP\\htdocs gevonden.');return this.snapshot()}
 private writeVhosts(){
  const file=path.join(this.dataDirectory,'apache','conf','localdeck-vhosts.conf'),baseConfig=path.join(this.dataDirectory,'apache','conf','httpd-localdeck.conf');if(!existsSync(path.dirname(file)))return;if(existsSync(baseConfig)){const content=readFileSync(baseConfig,'utf8');let updated=content;if(!/^LogFormat\s+.*\scombined\s*$/m.test(updated))updated=updated.replace(/^ServerName\s+/m,'LogFormat "%h %l %u %t \\"%r\\" %>s %b" combined\r\nServerName ');if(!/^LogFormat\s+.*\slocaldeck\s*$/m.test(updated))updated=updated.replace(/^ServerName\s+/m,'LogFormat "%h %l %u %t \\"%r\\" %>s %b %D" localdeck\r\nServerName ');if(!/^LoadModule actions_module /m.test(updated))updated=updated.replace(/^LoadModule dir_module/m,'LoadModule actions_module modules/mod_actions.so\r\nLoadModule cgi_module modules/mod_cgi.so\r\nLoadModule dir_module');updated=updated.replace(/\r?\n<FilesMatch "\\\.php\$">[\s\S]*?<\/FilesMatch>/,'');if(updated!==content)writeFileSync(baseConfig,updated)}
  const defaultPort=this.state.services.find(service=>service.id==='apache')?.port??80,www=this.webRootDirectory.replace(/\\/g,'/'),logs=path.join(this.dataDirectory,'logs').replace(/\\/g,'/'),extraPorts=new Set<number>();mkdirSync(path.join(this.dataDirectory,'logs'),{recursive:true});
  const phpHandler=(version:string)=>{const directory=this.phpDirectory(version).replace(/\\/g,'/'),handler=`localdeck-php-${version.replace(/\./g,'-')}`;return`ScriptAlias /__localdeck_php__/ "${directory}/"\n <Directory "${directory}">\n  Options ExecCGI\n  Require all granted\n </Directory>\n AddHandler ${handler} .php\n Action ${handler} "/__localdeck_php__/php-cgi.exe"`};
  const home=`<VirtualHost *:${defaultPort}>\n ServerName localhost\n ServerAlias 127.0.0.1 ::1\n DocumentRoot "${www}"\n DirectoryIndex index.html index.php\n <Directory "${www}">\n  AllowOverride All\n  Require all granted\n </Directory>\n ${phpHandler(this.state.settings.defaultPhpVersion)}\n</VirtualHost>`;
  const projects=this.state.projects.map(project=>{const root=(project.documentRoot||project.path).replace(/\\/g,'/'),profile={...DEFAULT_PROFILE,...project.profile},httpPort=profile.isolated&&profile.httpPort?profile.httpPort:defaultPort,httpsPort=profile.isolated&&profile.httpsPort?profile.httpsPort:443,phpPort=this.phpPort(project.phpVersion),certificate=this.certificatePaths(project),cert=certificate.certificateFile.replace(/\\/g,'/'),key=certificate.keyFile.replace(/\\/g,'/');if(httpPort!==defaultPort)extraPorts.add(httpPort);if(project.secure&&httpsPort!==443)extraPorts.add(httpsPort);const devDirectory=path.join(this.dataDirectory,'dev-pages',project.id);mkdirSync(devDirectory,{recursive:true});const devPath=devDirectory.replace(/\\/g,'/'),payload=JSON.stringify({project:project.name,domain:project.domain,php:project.phpVersion,phpPort,database:profile.databaseName||null,databaseEngine:profile.databaseEngine,environment:profile.environment,mail:profile.mailMode,branch:project.branch||null},null,2).replace(/</g,'\\u003c');writeFileSync(path.join(devDirectory,'index.php'),`<?php header('Content-Type: text/html; charset=utf-8'); $data = json_decode(<<<'JSON'\n${payload}\nJSON, true); ?><!doctype html><meta charset="utf-8"><title>LocalDeck · <?=htmlspecialchars($data['project'])?></title><style>body{font:15px system-ui;background:#0b0d12;color:#e9ecf3;padding:40px}main{max-width:720px;margin:auto;background:#151923;border:1px solid #34305b;border-radius:18px;padding:30px}b{color:#8f83ff}pre{background:#090b10;padding:18px;border-radius:10px;overflow:auto}a{color:#69dbca}</style><main><b>LOCALDECK DEVELOPER PAGE</b><h1><?=htmlspecialchars($data['project'])?></h1><p>Geïsoleerd projectprofiel en actuele ontwikkelgegevens.</p><pre><?=htmlspecialchars(json_encode($data, JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES))?></pre><a href="/">Terug naar project</a></main>`);const common=`ServerName ${project.domain}\n DocumentRoot "${root}"\n DirectoryIndex index.php index.html\n <Directory "${root}">\n  AllowOverride All\n  Require all granted\n  <FilesMatch "^\\.">\n   Require all denied\n  </FilesMatch>\n </Directory>\n ${phpHandler(project.phpVersion)}\n ErrorLog "${logs}/${project.id}-error.log"\n CustomLog "${logs}/${project.id}-access.log" localdeck${profile.debugToolbar?`\n Alias "/__localdeck" "${devPath}"\n <Directory "${devPath}">\n  AllowOverride None\n  Require all granted\n </Directory>`:''}`;const http=`<VirtualHost *:${httpPort}>\n ${common}\n</VirtualHost>`,https=project.secure&&existsSync(certificate.certificateFile)&&existsSync(certificate.keyFile)?`<VirtualHost *:${httpsPort}>\n ${common}\n SSLEngine on\n SSLCertificateFile "${cert}"\n SSLCertificateKeyFile "${key}"\n</VirtualHost>`:'';return`${http}\n${https}`});
  const listens=[...extraPorts].sort((a,b)=>a-b).map(port=>`Listen ${port}`).join('\n');writeFileSync(file,[listens,home,...projects].filter(Boolean).join('\n\n'))
 }
 private writeLandingMetadata(){if(!existsSync(this.webRootDirectory))return;const port=(id:ServiceId,fallback:number)=>this.state.services.find(s=>s.id===id)?.port??fallback;writeFileSync(path.join(this.webRootDirectory,'localdeck-runtime.json'),JSON.stringify({language:this.state.settings.language,apache:port('apache',80),php:port('php',9000),mysql:port('mysql',3306),phpmyadmin:port('phpmyadmin',8080)},null,2))}
 async repairRuntime(){mkdirSync(this.webRootDirectory,{recursive:true});mkdirSync(this.projectsDirectory,{recursive:true});mkdirSync(path.join(this.dataDirectory,'backups'),{recursive:true});this.syncFolderProjects();this.configurePhpMyAdminAutoLogin();this.writeVhosts();this.writeLandingMetadata();this.refreshPhpExtensions();this.refreshBackups();await this.runDiagnostics();this.persist();this.log('success','Herstel','Configuraties, startpagina en statusgegevens zijn opnieuw opgebouwd.');return this.snapshot()}
 exportDiagnostics(file:string){const report={generatedAt:new Date().toISOString(),version:APP_VERSION,platform:`${process.platform} ${process.arch}`,installation:this.state.installation,services:this.state.services.map(({pid,...service})=>service),diagnostics:this.state.diagnostics,settings:{...this.state.settings,dataDirectory:'[lokaal pad verwijderd]',updateFeedUrl:this.state.settings.updateFeedUrl?'[ingesteld]':''},recentLogs:this.state.logs.slice(0,100).map(log=>({...log,message:log.message.replace(/[A-Z]:\\[^\s]+/gi,'[pad]')}))};writeFileSync(file,JSON.stringify(report,null,2));this.log('success','Diagnose',`Veilig diagnoserapport opgeslagen in ${file}.`);return this.snapshot()}

 private snapshotDirectory(){const directory=path.join(this.dataDirectory,'snapshots');mkdirSync(directory,{recursive:true});return directory}
 refreshSnapshots(){const directory=this.snapshotDirectory(),items:SnapshotInfo[]=[];for(const file of readdirSync(directory).filter(name=>name.endsWith('.snapshot.json'))){try{const item=JSON.parse(readFileSync(path.join(directory,file),'utf8'))as SnapshotInfo;if(existsSync(item.file))items.push(item)}catch{}}this.state.snapshots=items.sort((a,b)=>b.createdAt.localeCompare(a.createdAt));return this.snapshot()}
 deleteSnapshot(id:string){const item=this.state.snapshots.find(snapshot=>snapshot.id===id);if(!item)throw new Error('Snapshot niet gevonden.');for(const file of[item.file,path.join(this.snapshotDirectory(),`${item.id}.snapshot.json`)])if(existsSync(file))unlinkSync(file);this.refreshSnapshots();this.persist();this.log('warning','Snapshots',`Snapshot van ${item.projectName} verwijderd.`);return this.snapshot()}
 registerSnapshot(metadataFile:string){if(!existsSync(metadataFile))throw new Error('Snapshotmetadata ontbreekt.');const item=JSON.parse(readFileSync(metadataFile,'utf8'))as SnapshotInfo;if(!existsSync(item.file))throw new Error('Snapshotarchief ontbreekt.');this.refreshSnapshots();this.persist();this.log('success','Snapshots',`Snapshot voor ${item.projectName} gemaakt.`);return this.snapshot()}

 private projectManifest(project:Project){const profile={...DEFAULT_PROFILE,...project.profile,env:this.publicEnvironment(project.profile?.env)},secretKeys=this.vaultRecords.filter(item=>item.projectId===project.id).map(item=>item.key).sort(),production=project.production??{php:project.phpVersion,database:`${profile.databaseEngine}:${profile.databaseVersion}`,webServer:profile.webServer,requiredEnv:[...new Set([...Object.keys(profile.env),...secretKeys])].sort()};return{schemaVersion:3,kind:'LocalDeckProjectCapsule',exportedBy:`LocalDeck ${APP_VERSION}`,name:project.name,domain:project.domain,documentRoot:path.relative(project.path,project.documentRoot||project.path)||'.',php:{version:project.phpVersion,extensions:profile.phpExtensions,xdebug:profile.xdebugMode},web:{server:profile.webServer,https:project.secure,httpPort:profile.httpPort,httpsPort:profile.httpsPort},database:{engine:profile.databaseEngine,version:profile.databaseVersion,name:profile.databaseName,branching:true},services:{mail:profile.mailMode,scheduler:profile.schedulerEnabled,redis:true},production,environment:profile.environment,env:profile.env,secrets:secretKeys,template:project.template??'custom',repository:{url:project.repositoryUrl,branch:project.branch},profile,tasks:this.state.tasks.filter(task=>!task.projectId||task.projectId===project.id).map(({lastRunAt,lastExitCode,...task})=>task)}}
 writeProjectManifest(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const manifest=this.projectManifest(project),serialized=JSON.stringify(manifest,null,2),yaml=`# LocalDeck Project Capsule — veilig deelbaar, zonder geheime waarden\n${stringifyYaml(manifest,{indent:2,lineWidth:110})}`,hash=createHash('sha256').update(yaml).digest('hex');writeFileSync(path.join(project.path,'.localdeck.json'),serialized);writeFileSync(path.join(project.path,'.localdeck.yml'),yaml);writeFileSync(path.join(project.path,'.localdeck.lock.json'),JSON.stringify({schemaVersion:3,capsuleSchemaVersion:3,blueprintHash:hash,localDeckVersion:APP_VERSION,lockedAt:new Date().toISOString(),runtime:{php:project.phpVersion,webServer:project.profile?.webServer,database:`${project.profile?.databaseEngine}:${project.profile?.databaseVersion}`}},null,2));this.refreshBlueprints();this.log('success','Project Capsule',`.localdeck.yml en controleerbare lockfile bijgewerkt voor ${project.name}.`);return this.snapshot()}
 updateProjectProfile(projectId:string,patch:Partial<ProjectProfile>){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');for(const port of[patch.httpPort,patch.httpsPort])if(port!==undefined&&(!Number.isInteger(port)||port<1024||port>65535))throw new Error('Geïsoleerde projectpoorten moeten tussen 1024 en 65535 liggen.');const nextHttp=patch.httpPort??project.profile?.httpPort,nextHttps=patch.httpsPort??project.profile?.httpsPort;if(nextHttp!==undefined&&nextHttp===nextHttps)throw new Error('HTTP en HTTPS moeten verschillende poorten gebruiken.');for(const[protocol,port]of[['HTTP',patch.httpPort],['HTTPS',patch.httpsPort]]as const)if(port!==undefined){if(this.state.services.some(service=>service.port===port))throw new Error(`Deze geïsoleerde ${protocol}-poort wordt al door ${this.state.services.find(service=>service.port===port)?.name} gebruikt.`);if(this.state.projects.some(other=>other.id!==projectId&&(other.profile?.httpPort===port||other.profile?.httpsPort===port)))throw new Error(`Deze geïsoleerde ${protocol}-poort wordt al door een ander project gebruikt.`)}project.profile={...DEFAULT_PROFILE,...project.profile,...patch,env:{...DEFAULT_PROFILE.env,...project.profile?.env,...patch.env}};this.writeProjectManifest(projectId);this.writeVhosts();this.persist();this.log('success','Projectprofiel',`Profiel van ${project.name} bijgewerkt.`);return this.snapshot()}
 setProjectPhpVersion(projectId:string,version:string){const project=this.state.projects.find(item=>item.id===projectId),php=this.state.services.find(item=>item.id==='php');if(!project)throw new Error('Project niet gevonden.');if(!this.state.phpVersions.some(item=>item.version===version))throw new Error('Deze PHP-versie wordt niet ondersteund.');if(php?.status==='running')throw new Error('Stop PHP eerst voordat je de projectversie wijzigt.');if(!this.state.settings.simulationMode&&!existsSync(path.join(this.phpDirectory(version),'php-cgi.exe')))throw new Error(`PHP ${version} is nog niet geïnstalleerd.`);project.phpVersion=version;this.writeProjectManifest(projectId);this.writeVhosts();this.refreshComponents();this.persist();this.log('success','Projectruntime',`${project.name} gebruikt nu PHP ${version}.`);return this.snapshot()}
 importProjectManifest(file:string){const source=readFileSync(file,'utf8').replace(/^\uFEFF/,''),manifest=/\.ya?ml$/i.test(file)?parseYaml(source):JSON.parse(source),root=path.dirname(file),schema=Number(manifest.schemaVersion);if(![1,2,3].includes(schema))throw new Error('Deze LocalDeck-capsuleversie wordt niet ondersteund.');if(schema===3&&manifest.kind!=='LocalDeckProjectCapsule')throw new Error('Dit is geen geldige LocalDeck Project Capsule.');const phpVersion=String(manifest.php?.version||manifest.phpVersion||this.state.settings.defaultPhpVersion),documentRoot=manifest.documentRoot?path.resolve(root,String(manifest.documentRoot)):root,profile={...DEFAULT_PROFILE,...manifest.profile,databaseEngine:manifest.database?.engine||manifest.profile?.databaseEngine||'mysql',databaseVersion:manifest.database?.version||manifest.profile?.databaseVersion||'8.4',databaseName:manifest.database?.name||manifest.profile?.databaseName,xdebugMode:manifest.php?.xdebug||manifest.profile?.xdebugMode||'off',env:{...DEFAULT_PROFILE.env,...this.publicEnvironment(manifest.env),...this.publicEnvironment(manifest.profile?.env)}},production=manifest.production?{php:String(manifest.production.php||phpVersion),database:String(manifest.production.database||`${profile.databaseEngine}:${profile.databaseVersion}`),webServer:String(manifest.production.webServer||profile.webServer)as ProjectProfile['webServer'],requiredEnv:Array.isArray(manifest.production.requiredEnv)?manifest.production.requiredEnv.map(String):Object.keys(profile.env)}:undefined;return this.addProject({name:String(manifest.name||path.basename(root)),path:root,documentRoot,domain:String(manifest.domain||this.localDomain(path.basename(root).toLowerCase())),phpVersion,secure:manifest.web?.https??manifest.secure!==false,template:manifest.template,repositoryUrl:manifest.repository?.url,branch:manifest.repository?.branch,profile,production})}

 getDatabaseTables(id:string):DatabaseTable[]{const database=this.state.databases.find(item=>item.id===id);if(!database)throw new Error('Database niet gevonden.');if(this.state.settings.simulationMode)return[];const sql=`SELECT TABLE_NAME,TABLE_ROWS,ENGINE,ROUND((DATA_LENGTH+INDEX_LENGTH)/1024/1024,2),TABLE_COLLATION FROM information_schema.TABLES WHERE TABLE_SCHEMA='${database.name.replace(/'/g,"''")}' ORDER BY TABLE_NAME`;const output=this.mysql(['-N','-e',sql]);return output.trim()?output.trim().split(/\r?\n/).map(line=>{const[name,rows,engine,size,collation]=line.split('\t');return{name,rows:Number(rows||0),engine:engine||'—',size:`${size||0} MB`,collation:collation||'—'}}):[]}
 queryDatabase(id:string,query:string):DatabaseQueryResult{const database=this.state.databases.find(item=>item.id===id);if(!database)throw new Error('Database niet gevonden.');if(query.length>20_000)throw new Error('De SQL-opdracht is te lang.');const clean=query.trim().replace(/;\s*$/,'');if(!/^(SELECT|SHOW|DESCRIBE|DESC|EXPLAIN)\b/i.test(clean)||/;|\b(INTO\s+(OUTFILE|DUMPFILE)|FOR\s+UPDATE|LOCK\s+IN\s+SHARE\s+MODE|SLEEP\s*\()\b/i.test(clean))throw new Error('De ingebouwde editor staat alleen veilige leesopdrachten toe. Gebruik phpMyAdmin voor wijzigingen.');const limited=/^SELECT\b/i.test(clean)&&!/\bLIMIT\s+\d+/i.test(clean),sql=limited?`${clean} LIMIT 200`:clean,start=Date.now();if(this.state.settings.simulationMode)return{columns:['status'],rows:[{status:'Simulatiemodus'}],durationMs:Date.now()-start,limited};const output=this.mysql(['-D',database.name,'--batch','--raw','-e',sql]),lines=output.replace(/\r/g,'').trimEnd().split('\n'),durationMs=Date.now()-start;if(!lines[0])return{columns:[],rows:[],durationMs,limited};const columns=lines[0].split('\t'),rows=lines.slice(1).map(line=>Object.fromEntries(columns.map((column,index)=>[column,line.split('\t')[index]??null])));this.log('info','Database',`Leesopdracht uitgevoerd op ${database.name}: ${rows.length} rijen in ${durationMs} ms.`);return{columns,rows,durationMs,limited}}
 getDatabaseUsers():DatabaseUser[]{if(this.state.settings.simulationMode)return[{user:'localdeck_demo',host:'127.0.0.1',plugin:'caching_sha2_password',locked:false}];const output=this.mysql(['-N','-e',"SELECT User,Host,plugin,account_locked FROM mysql.user WHERE User <> 'mysql.infoschema' AND User <> 'mysql.session' AND User <> 'mysql.sys' ORDER BY User,Host"]);return output.trim()?output.trim().split(/\r?\n/).map(line=>{const[user,host,plugin,locked]=line.split('\t');return{user,host,plugin,locked:locked==='Y'}}):[]}
 createDatabaseUser(databaseId:string,username:string,password:string){const database=this.state.databases.find(item=>item.id===databaseId),user=username.trim();if(!database)throw new Error('Database niet gevonden.');if(!/^[a-zA-Z][a-zA-Z0-9_]{0,31}$/.test(user))throw new Error('Gebruik 1–32 letters, cijfers of underscores voor de gebruiker.');if(password.length<12)throw new Error('Gebruik een wachtwoord van minimaal 12 tekens.');if(!this.state.settings.simulationMode){const escaped=password.replace(/'/g,"''");this.mysqlInput(`CREATE USER IF NOT EXISTS '${user}'@'127.0.0.1' IDENTIFIED BY '${escaped}'; ALTER USER '${user}'@'127.0.0.1' IDENTIFIED BY '${escaped}'; GRANT ALL PRIVILEGES ON \`${database.name}\`.* TO '${user}'@'127.0.0.1'; FLUSH PRIVILEGES;`)}this.log('success','Databasegebruikers',`${user}@127.0.0.1 heeft alleen rechten op ${database.name}.`);return this.snapshot()}
 deleteDatabaseUser(username:string,host='127.0.0.1'){const user=username.trim(),cleanHost=host.trim();if(user==='root')throw new Error('De ingebouwde rootgebruiker kan niet via LocalDeck worden verwijderd.');if(!/^[a-zA-Z][a-zA-Z0-9_]{0,31}$/.test(user)||!['127.0.0.1','localhost'].includes(cleanHost))throw new Error('Ongeldige lokale databasegebruiker.');if(!this.state.settings.simulationMode)this.mysql(['-e',`DROP USER IF EXISTS '${user}'@'${cleanHost}'`]);this.log('warning','Databasegebruikers',`${user}@${cleanHost} verwijderd.`);return this.snapshot()}

 saveSecret(key:string,value:string,projectId?:string){const clean=key.trim().toUpperCase();if(!/^[A-Z][A-Z0-9_]{1,63}$/.test(clean))throw new Error('Gebruik 2–64 hoofdletters, cijfers of underscores voor de sleutelnaam.');if(!value||value.length>16_384)throw new Error('De geheime waarde is leeg of te lang.');if(projectId&&!this.state.projects.some(project=>project.id===projectId))throw new Error('Project niet gevonden.');if(!this.secretCodec?.available())throw new Error('Windows-versleuteling is op dit systeem nog niet beschikbaar.');const existing=this.vaultRecords.find(item=>item.key===clean&&item.projectId===projectId),record:VaultRecord={id:existing?.id??crypto.randomUUID(),key:clean,projectId,scope:projectId?'project':'global',updatedAt:new Date().toISOString(),encryption:this.secretCodec.name,encryptedValue:this.secretCodec.encrypt(value)};this.vaultRecords=this.vaultRecords.filter(item=>item.id!==record.id);this.vaultRecords.push(record);this.saveVault();if(projectId)this.writeProjectManifest(projectId);this.log('success','Secret Vault',`${clean} versleuteld opgeslagen${projectId?' voor het project':' als globale sleutel'}.`);return this.snapshot()}
 deleteSecret(id:string){const record=this.vaultRecords.find(item=>item.id===id);if(!record)throw new Error('Geheime sleutel niet gevonden.');this.vaultRecords=this.vaultRecords.filter(item=>item.id!==id);this.saveVault();if(record.projectId&&this.state.projects.some(project=>project.id===record.projectId))this.writeProjectManifest(record.projectId);this.log('warning','Secret Vault',`${record.key} uit de kluis verwijderd.`);return this.snapshot()}

 addTask(task:Omit<ProjectTask,'id'|'lastRunAt'|'lastExitCode'>){if(!/^[a-zA-Z0-9._-]+$/.test(task.command))throw new Error('Ongeldig uitvoerbaar bestand.');const item:ProjectTask={...task,id:crypto.randomUUID()};this.state.tasks.push(item);this.persist();return this.snapshot()}
 deleteTask(id:string){if(DEFAULT_TASKS.some(task=>task.id===id))throw new Error('Ingebouwde taken kunnen niet worden verwijderd.');this.state.tasks=this.state.tasks.filter(task=>task.id!==id);this.persist();return this.snapshot()}
 async runTask(projectId:string,taskId:string){const project=this.state.projects.find(item=>item.id===projectId),task=this.state.tasks.find(item=>item.id===taskId&&(!item.projectId||item.projectId===projectId));if(!project||!task)throw new Error('Project of taak niet gevonden.');let executable=task.command,args=[...task.args];const php=path.join(this.phpDirectory(project.phpVersion),'php.exe');if(task.command==='composer'){executable=php;args=[path.join(this.dataDirectory,'composer.phar'),...args]}else if(task.command==='php')executable=php;else if(task.command==='npm'){const found=spawnSync('where.exe',['npm.cmd'],{encoding:'utf8',windowsHide:true});if(found.status!==0)throw new Error('npm is niet geïnstalleerd.');const npm=found.stdout.trim().split(/\r?\n/)[0],node=path.join(path.dirname(npm),'node.exe'),cli=path.join(path.dirname(npm),'node_modules','npm','bin','npm-cli.js');if(existsSync(node)&&existsSync(cli)){executable=node;args=[cli,...args]}else{executable=process.env.ComSpec||'cmd.exe';args=['/d','/s','/c',npm,...args]}}if(!existsSync(executable)&&!['git','node'].includes(executable))throw new Error(`${task.command} is niet geïnstalleerd.`);this.log('info','Taken',`${task.name} gestart voor ${project.name} met PHP ${project.phpVersion}.`);return new Promise<AppState>((resolve,reject)=>{const child=spawn(executable,args,{cwd:project.path,windowsHide:true,shell:false,stdio:['ignore','pipe','pipe'],env:{...process.env,...this.publicEnvironment(project.profile?.env),...this.runtimeEnvironment(project.id),LOCALDECK_PHP_VERSION:project.phpVersion}});child.stdout?.on('data',data=>this.log('info',task.name,String(data).trim()));child.stderr?.on('data',data=>this.log('warning',task.name,String(data).trim()));child.once('error',reject);child.on('exit',code=>{task.lastRunAt=new Date().toISOString();task.lastExitCode=code??-1;this.persist();if(code===0){this.log('success','Taken',`${task.name} voltooid.`);resolve(this.snapshot())}else reject(new Error(`${task.name} stopte met code ${code}.`))})})}

 scanSecurity(){const findings:SecurityFinding[]=[];const add=(finding:Omit<SecurityFinding,'id'|'resolved'>)=>findings.push({...finding,id:crypto.randomUUID(),resolved:false});if(!this.runtimeComplete())add({severity:'critical',category:'Runtime',title:'Runtime ontbreekt of is onvolledig',detail:'Installeer of herstel de onderdelen via de pagina Installatie.',fixable:false});if(!this.state.settings.requireSignedUpdates)add({severity:'warning',category:'Updates',title:'Ondertekende updates zijn nog niet verplicht',detail:'Schakel dit in zodra een officieel LocalDeck-codecertificaat beschikbaar is.',fixable:true});if(this.state.settings.webControlEnabled)add({severity:'info',category:'Netwerk',title:'Webbeheer is alleen lokaal bereikbaar',detail:`127.0.0.1:${this.state.settings.webControlPort}`,fixable:false});for(const project of this.state.projects){const certificate=this.certificatePaths(project),certificateReady=existsSync(certificate.certificateFile)&&existsSync(certificate.keyFile);if(!project.secure||!certificateReady)add({severity:'warning',category:'HTTPS',title:!project.secure?`${project.name} gebruikt geen HTTPS`:`Eigen SSL-certificaat ontbreekt voor ${project.name}`,detail:project.domain,fixable:true,projectId:project.id});if(existsSync(path.join(project.documentRoot||project.path,'.env')))add({severity:'info',category:'Project',title:`Bescherm .env voor ${project.name}`,detail:'LocalDeck blokkeert dotfiles in de gegenereerde Apache-vhost.',fixable:false,projectId:project.id});if(!existsSync(path.join(project.path,'composer.lock'))&&existsSync(path.join(project.path,'composer.json')))add({severity:'warning',category:'Dependencies',title:`Composer-lock ontbreekt voor ${project.name}`,detail:'Reproduceerbare dependencyversies zijn niet vastgelegd.',fixable:false,projectId:project.id})}this.state.securityFindings=findings;this.emit('change',this.snapshot());this.log(findings.some(item=>item.severity==='critical')?'warning':'success','Beveiliging',`${findings.length} beveiligingscontroles afgerond.`);return this.snapshot()}
 fixSecurity(id:string){const finding=this.state.securityFindings.find(item=>item.id===id);if(!finding||!finding.fixable)throw new Error('Deze bevinding kan niet automatisch worden hersteld.');if(finding.category==='Updates')this.state.settings.requireSignedUpdates=true;else if(finding.category==='HTTPS'&&finding.projectId){const project=this.state.projects.find(item=>item.id===finding.projectId);if(project)project.secure=true}else if(finding.category==='Runtime')throw new Error('Gebruik Runtime installeren om ontbrekende onderdelen veilig te herstellen.');finding.resolved=true;this.writeVhosts();this.persist();return this.scanSecurity()}

 async repairAll(){this.invalidatePortInspection();const actions:RepairAction[]=[];const record=(label:string,status:RepairAction['status'],detail:string)=>actions.push({id:crypto.randomUUID(),label,status,detail});for(const directory of['www','backups','snapshots','downloads']){mkdirSync(path.join(this.dataDirectory,directory),{recursive:true});record(`Map ${directory}`,'ok','Aanwezig')}for(const service of this.state.services){if(!service.installed){record(service.name,'warning','Onderdeel ontbreekt');continue}const info=await this.inspectPort(service.id);if(info.status==='occupied'){try{const old=service.port;this.updateServicePort(service.id,info.suggestedPort,true);record(`${service.name}-poort`,'fixed',`${old} → ${service.port}; extern conflict omzeild`)}catch(error){record(`${service.name}-poort`,'failed',error instanceof Error?error.message:String(error))}}else record(`${service.name}-poort`,'ok',info.status==='localdeck'?`${service.port} · actief via LocalDeck`:String(service.port))}try{this.configurePhpMyAdminAutoLogin();this.writeVhosts();this.writeLandingMetadata();for(const project of this.state.projects)this.writeProjectManifest(project.id);record('Configuraties','fixed','Vhosts, phpMyAdmin, metadata en projectprofielen opnieuw opgebouwd')}catch(error){record('Configuraties','failed',error instanceof Error?error.message:String(error))}this.refreshPhpExtensions();this.refreshBackups();this.refreshSnapshots();await this.runDiagnostics();this.scanSecurity();this.state.repairActions=actions;this.persist();this.log('success','Automatisch herstel',`${actions.filter(item=>item.status==='fixed').length} onderdelen hersteld.`);return this.snapshot()}

 runProjectDoctor(projectId?:string){const selected=projectId?this.state.projects.filter(project=>project.id===projectId):this.state.projects,reports:ProjectHealthReport[]=selected.map(project=>{const checks:ProjectHealthCheck[]=[],add=(id:string,label:string,status:ProjectHealthCheck['status'],detail:string,fixable=false)=>checks.push({id,label,status,detail,fixable}),root=project.path,documentRoot=project.documentRoot||root,vhosts=path.join(this.dataDirectory,'apache','conf','localdeck-vhosts.conf');add('root','Projectmap',existsSync(root)?'ok':'error',existsSync(root)?root:'De projectmap bestaat niet.');add('document-root','Document root',existsSync(documentRoot)?'ok':'error',existsSync(documentRoot)?documentRoot:'De ingestelde document root ontbreekt.',true);const entry=['index.php','index.html'].some(file=>existsSync(path.join(documentRoot,file)));add('entrypoint','Startbestand',entry?'ok':'warning',entry?'index.php of index.html gevonden.':'Geen index.php of index.html in de document root.');const php=existsSync(path.join(this.phpDirectory(project.phpVersion),'php-cgi.exe'));add('php','PHP-runtime',php?'ok':'error',php?`PHP ${project.phpVersion} is aanwezig.`:`PHP ${project.phpVersion} ontbreekt.`);const vhost=existsSync(vhosts)&&readFileSync(vhosts,'utf8').includes(`ServerName ${project.domain}`);add('vhost','Apache-vhost',vhost?'ok':'warning',vhost?'Vhost is gegenereerd.':'Vhost ontbreekt of is verouderd.',true);const localHost=project.domain.endsWith('.localhost');add('domain','Lokaal domein',localHost?'ok':project.domain.endsWith('.test')?'warning':'error',localHost?'Werkt zonder hosts-bestandswijziging.':project.domain.endsWith('.test')?'.test vereist hosts-sync of LocalDeck DNS.':'Geen ondersteund lokaal domein.',project.domain.endsWith('.test'));if(existsSync(path.join(root,'composer.json')))add('composer-lock','Composer lock',existsSync(path.join(root,'composer.lock'))?'ok':'warning',existsSync(path.join(root,'composer.lock'))?'Dependencyversies zijn vastgezet.':'composer.lock ontbreekt.');if(existsSync(path.join(root,'package.json')))add('node-lock','Node lock',readdirSync(root).some(file=>['package-lock.json','pnpm-lock.yaml','yarn.lock'].includes(file))?'ok':'warning','Controle op reproduceerbare Node-dependencies.');if(project.profile?.databaseName)add('database','Database',this.state.databases.some(item=>item.name===project.profile?.databaseName)?'ok':'warning',this.state.databases.some(item=>item.name===project.profile?.databaseName)?`${project.profile.databaseName} is bekend.`:`${project.profile.databaseName} is nog niet aangemaakt.`);if(project.secure){const certificate=this.certificatePaths(project),certificateReady=existsSync(certificate.certificateFile)&&existsSync(certificate.keyFile);add('https','HTTPS-certificaat',certificateReady?'ok':'warning',certificateReady?`Eigen certificaat voor ${project.domain} aanwezig.`:`Eigen certificaat voor ${project.domain} ontbreekt.`,true)}let writable=false;try{accessSync(root,constants.W_OK);writable=true}catch{}add('writable','Schrijfrechten',writable?'ok':'error',writable?'Projectmap is schrijfbaar.':'LocalDeck kan niet naar de projectmap schrijven.');const errors=checks.filter(check=>check.status==='error').length,warnings=checks.filter(check=>check.status==='warning').length,score=Math.max(0,100-errors*25-warnings*8),status=errors?'error':warnings?'warning':'ok';return{projectId:project.id,projectName:project.name,domain:project.domain,score,status,checkedAt:new Date().toISOString(),checks} satisfies ProjectHealthReport});this.state.projectHealth=projectId?[...this.state.projectHealth.filter(report=>report.projectId!==projectId),...reports]:reports;this.emit('change',this.snapshot());return this.snapshot()}
 repairProject(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');if(!existsSync(project.path))throw new Error('De projectmap ontbreekt en kan niet automatisch worden hersteld.');if(!existsSync(project.documentRoot||project.path)){const detected=this.detectProject(project.path);project.documentRoot=detected.documentRoot;project.template=detected.template}this.writeProjectManifest(projectId);this.writeVhosts();this.writeLandingMetadata();this.persist();this.runProjectDoctor(projectId);this.log('success','Product Doctor',`${project.name}: veilige configuratie opnieuw opgebouwd.`);return this.snapshot()}
 runProductionParity(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');this.refreshBlueprints();const capsuleFile=path.join(project.path,'.localdeck.yml');let manifest:any=this.projectManifest(project);try{if(existsSync(capsuleFile))manifest=parseYaml(readFileSync(capsuleFile,'utf8'))}catch(error){throw new Error(`Project Capsule is geen geldige YAML: ${error instanceof Error?error.message:String(error)}`)}const expected=manifest?.production??{},profile={...DEFAULT_PROFILE,...project.profile},checks:ProductionParityReport['checks']=[],add=(id:string,label:string,local:string,wanted:string,status:'ok'|'warning'|'error',detail:string)=>checks.push({id,label,local,expected:wanted,status,detail}),expectedPhp=String(expected.php||project.phpVersion),expectedDatabase=String(expected.database||`${profile.databaseEngine}:${profile.databaseVersion}`),expectedWeb=String(expected.webServer||profile.webServer),requiredEnv=Array.isArray(expected.requiredEnv)?expected.requiredEnv.map(String):[];project.production={php:expectedPhp,database:expectedDatabase,webServer:expectedWeb as ProjectProfile['webServer'],requiredEnv};add('php','PHP-versie',project.phpVersion,expectedPhp,project.phpVersion===expectedPhp?'ok':'error',project.phpVersion===expectedPhp?'Lokale en productieversie zijn gelijk.':'Pas de projectruntime of het productieprofiel aan.');const localDatabase=`${profile.databaseEngine}:${profile.databaseVersion}`;add('database','Database-engine',localDatabase,expectedDatabase,localDatabase===expectedDatabase?'ok':'error',localDatabase===expectedDatabase?'Database-engine en hoofdversie sluiten aan.':'Databasegedrag kan tussen lokaal en productie verschillen.');add('webserver','Webserver',profile.webServer,expectedWeb,profile.webServer===expectedWeb?'ok':'warning',profile.webServer===expectedWeb?'De webserver is gelijk.':'Controleer rewrites, headers en cachegedrag extra.');const envKeys=new Set([...Object.keys(profile.env||{}),...this.vaultRecords.filter(item=>!item.projectId||item.projectId===project.id).map(item=>item.key)]),missing=requiredEnv.filter((key:string)=>!envKeys.has(key));add('environment','Vereiste omgevingssleutels',`${requiredEnv.length-missing.length}/${requiredEnv.length}`,String(requiredEnv.length),missing.length?'error':'ok',missing.length?`Ontbreekt: ${missing.join(', ')}`:'Alle vereiste sleutelnamen zijn aanwezig; waarden blijven geheim.');const composerNeeded=existsSync(path.join(project.path,'composer.json')),composerLocked=existsSync(path.join(project.path,'composer.lock'));if(composerNeeded)add('composer-lock','Composer lock',composerLocked?'aanwezig':'ontbreekt','aanwezig',composerLocked?'ok':'error',composerLocked?'PHP-dependencies zijn reproduceerbaar.':'Maak composer.lock voordat je publiceert.');const nodeNeeded=existsSync(path.join(project.path,'package.json')),nodeLocked=['package-lock.json','pnpm-lock.yaml','yarn.lock'].some(file=>existsSync(path.join(project.path,file)));if(nodeNeeded)add('node-lock','Node lock',nodeLocked?'aanwezig':'ontbreekt','aanwezig',nodeLocked?'ok':'warning',nodeLocked?'Node-dependencies zijn reproduceerbaar.':'Leg de frontend-dependencies vast.');const blueprint=this.state.blueprints.find(item=>item.projectId===project.id);add('capsule-lock','Capsule-integriteit',blueprint?.locked?'geldig':'gewijzigd','geldig',blueprint?.locked?'ok':'warning',blueprint?.locked?'Project Capsule komt overeen met de lockfile.':'Schrijf de capsule opnieuw nadat je bewuste wijzigingen hebt gecontroleerd.');const errors=checks.filter(check=>check.status==='error').length,warnings=checks.filter(check=>check.status==='warning').length,report:ProductionParityReport={projectId:project.id,projectName:project.name,score:Math.max(0,100-errors*25-warnings*8),status:errors?'error':warnings?'warning':'ok',checkedAt:new Date().toISOString(),checks};this.state.productionParity=[...this.state.productionParity.filter(item=>item.projectId!==project.id),report];this.persist();this.emit('change',this.snapshot());this.log(report.status==='ok'?'success':'warning','Productiepariteit',`${project.name}: score ${report.score}/100.`);return this.snapshot()}
 async runPreflight(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const startedAt=new Date().toISOString(),steps:PreflightReport['steps']=[],add=(id:string,label:string,status:'ok'|'warning'|'error',detail:string,durationMs:number)=>steps.push({id,label,status,detail,durationMs}),run=(id:string,label:string,command:string,args:string[],required=false)=>{const started=Date.now(),result=spawnSync(command,args,{cwd:project.path,encoding:'utf8',windowsHide:true,timeout:120_000,maxBuffer:8*1024*1024,env:{...process.env,...this.publicEnvironment(project.profile?.env),...this.runtimeEnvironment(project.id)}}),missing=result.error&&'code'in result.error&&result.error.code==='ENOENT',status=missing&&!required?'warning':result.status===0?'ok':'error',detail=missing?'Hulpprogramma niet geïnstalleerd.':result.status===0?(result.stdout.trim().split(/\r?\n/).slice(-2).join(' ')||'Geslaagd.'):(result.stderr||result.stdout||result.error?.message||`Stopcode ${result.status}`).trim().slice(0,700);add(id,label,status,detail,Date.now()-started)};const php=path.join(this.phpDirectory(project.phpVersion),'php.exe');if(existsSync(php)){const phpFiles=readdirSync(project.documentRoot||project.path).filter(file=>file.toLowerCase().endsWith('.php')).slice(0,30);if(phpFiles.length)for(const file of phpFiles)run(`php-lint-${file}`,`PHP lint · ${file}`,php,['-l',path.join(project.documentRoot||project.path,file)],true);else add('php-lint','PHP syntaxcontrole','warning','Geen PHP-bestanden in de document root gevonden.',0)}else add('php-runtime','PHP-runtime','error',`PHP ${project.phpVersion} ontbreekt.`,0);if(existsSync(path.join(project.path,'composer.json'))){const composer=path.join(this.dataDirectory,'composer.phar');if(existsSync(php)&&existsSync(composer)){run('composer-validate','Composer validate',php,[composer,'validate','--no-check-publish'],true);run('composer-audit','Composer audit',php,[composer,'audit','--format=plain'],false)}else add('composer','Composercontrole','warning','Composer of de project-PHP-runtime ontbreekt.',0)}if(existsSync(path.join(project.path,'package.json'))){const npm=spawnSync('where.exe',['npm.cmd'],{encoding:'utf8',windowsHide:true});if(npm.status===0)run('npm-test','Frontend tests',npm.stdout.trim().split(/\r?\n/)[0],['test','--','--run'],false);else add('npm-test','Frontend tests','warning','npm is niet geïnstalleerd.',0)}this.runProductionParity(projectId);const parity=this.state.productionParity.find(item=>item.projectId===projectId);add('production-parity','Productiepariteit',parity?.status??'warning',parity?`${parity.score}/100`:'Niet beschikbaar.',0);const status:PreflightReport['status']=steps.some(step=>step.status==='error')?'error':steps.some(step=>step.status==='warning')?'warning':'ok',report:PreflightReport={projectId:project.id,projectName:project.name,status,startedAt,finishedAt:new Date().toISOString(),steps};this.state.preflightReports=[...this.state.preflightReports.filter(item=>item.projectId!==project.id),report];this.emit('change',this.snapshot());this.log(status==='ok'?'success':'warning','Lokale CI',`${project.name}: ${steps.filter(step=>step.status==='ok').length}/${steps.length} controles geslaagd.`);return this.snapshot()}
 async runTestLab(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const apachePort=this.state.services.find(item=>item.id==='apache')?.port??80,url=`http://127.0.0.1:${apachePort}/`,checks:TestLabReport['checks']=[],started=Date.now(),add=(id:string,label:string,status:'ok'|'warning'|'error',detail:string)=>checks.push({id,label,status,detail});let html='';try{const response=await fetch(url,{headers:{host:project.domain,'x-localdeck-testlab':'1'},redirect:'manual',signal:AbortSignal.timeout(12_000)});html=await response.text();add('http','HTTP-respons',response.status<400?'ok':'error',`${response.status} ${response.statusText}`);add('content','Pagina-inhoud',html.trim().length?'ok':'error',html.trim().length?`${html.length} tekens ontvangen.`:'Lege respons ontvangen.');const errors=[...html.matchAll(/(?:fatal error|uncaught exception|parse error|warning:)\s*([^<\r\n]*)/gi)].slice(0,5);add('php-errors','Zichtbare PHP-fouten',errors.length?'error':'ok',errors.length?errors.map(item=>item[0]).join(' · ').slice(0,600):'Geen zichtbare PHP-fouten gevonden.');const resources=[...html.matchAll(/(?:src|href)=["']([^"'#?]+)["']/gi)].map(item=>item[1]).filter(value=>!/^data:|^mailto:|^javascript:|^https?:\/\//i.test(value)).slice(0,10);let failed=0;for(const resource of resources){try{const resourceUrl=new URL(resource,url);const resourceResponse=await fetch(resourceUrl,{headers:{host:project.domain},signal:AbortSignal.timeout(5_000)});if(resourceResponse.status>=400)failed++}catch{failed++}}add('assets','Lokale assets',failed?'warning':'ok',resources.length?`${resources.length-failed}/${resources.length} gecontroleerde assets bereikbaar.`:'Geen lokale assets gevonden.')}catch(error){add('http','HTTP-respons','error',error instanceof Error?error.message:String(error));add('content','Pagina-inhoud','error','Geen pagina ontvangen.')}const status:TestLabReport['status']=checks.some(check=>check.status==='error')?'error':checks.some(check=>check.status==='warning')?'warning':'ok',report:TestLabReport={projectId:project.id,projectName:project.name,url:`${project.secure?'https':'http'}://${project.domain}`,status,checkedAt:new Date().toISOString(),durationMs:Date.now()-started,checks};this.state.testLabReports=[...this.state.testLabReports.filter(item=>item.projectId!==project.id),report];this.emit('change',this.snapshot());this.log(status==='ok'?'success':'warning','Testlab',`${project.name}: ${checks.filter(check=>check.status==='ok').length}/${checks.length} controles geslaagd.`);return report}
 setTestLabScreenshot(projectId:string,file:string){const report=this.state.testLabReports.find(item=>item.projectId===projectId);if(!report)throw new Error('Voer eerst het Testlab uit.');report.screenshot=file;this.emit('change',this.snapshot());return this.snapshot()}
 detectDevDrive(){const drive=path.parse(this.projectsDirectory).root.replace(/\\$/,''),supported=process.platform==='win32';if(!supported){this.state.devDrive={supported:false,drive,isDevDrive:false,status:'unsupported',detail:'Dev Drive is alleen op Windows beschikbaar.',securityMode:'Niet van toepassing',checkedAt:new Date().toISOString()};return this.snapshot()}const result=spawnSync('fsutil.exe',['devdrv','query',drive],{encoding:'utf8',windowsHide:true,timeout:8000}),output=`${result.stdout}\n${result.stderr}`.trim(),isDevDrive=result.status===0&&!/not a dev drive|is geen dev drive/i.test(output);let securityMode='Windows Defender standaardbeleid';const defender=spawnSync('powershell.exe',['-NoLogo','-NoProfile','-NonInteractive','-WindowStyle','Hidden','-Command','(Get-MpPreference -ErrorAction SilentlyContinue).PerformanceModeStatus'],{encoding:'utf8',windowsHide:true,timeout:5000});if(defender.status===0&&defender.stdout.trim())securityMode=`Defender-status: ${defender.stdout.trim()}`;this.state.devDrive={supported:true,drive,isDevDrive,status:isDevDrive?'active':'available',detail:isDevDrive?'De projectmap staat op een Windows Dev Drive.':'De projectmap staat niet op een Dev Drive. LocalDeck wijzigt Defender-instellingen nooit automatisch.',securityMode,checkedAt:new Date().toISOString()};this.emit('change',this.snapshot());return this.snapshot()}
 refreshComponentPacks(force=false){if(this.componentPacksLoaded&&!force)return this.snapshot();const root=path.join(this.dataDirectory,'components'),installed:ComponentPack[]=[];if(existsSync(root))for(const id of readdirSync(root)){const componentRoot=path.join(root,id);if(!statSync(componentRoot).isDirectory())continue;for(const version of readdirSync(componentRoot)){const directory=path.join(componentRoot,version),manifestFile=path.join(directory,'manifest.json'),receiptFile=path.join(directory,'verification.json');if(!existsSync(manifestFile))continue;try{const manifestBytes=readFileSync(manifestFile),manifest=JSON.parse(manifestBytes.toString('utf8').replace(/^\uFEFF/,'')),receipt=existsSync(receiptFile)?JSON.parse(readFileSync(receiptFile,'utf8').replace(/^\uFEFF/,'')):undefined,manifestHash=createHash('sha256').update(manifestBytes).digest('hex').toUpperCase(),filesValid=Array.isArray(manifest.files)&&manifest.files.every((item:any)=>{const relative=String(item.path||'').replace(/\//g,path.sep),target=path.resolve(directory,relative),prefix=path.resolve(directory)+path.sep;return target.startsWith(prefix)&&existsSync(target)&&createHash('sha256').update(readFileSync(target)).digest('hex').toUpperCase()===String(item.sha256||'').toUpperCase()}),verified=Boolean(receipt?.verified&&String(receipt.manifestSha256).toUpperCase()===manifestHash&&filesValid);installed.push({id:String(manifest.id||id),name:String(manifest.name||id),description:String(manifest.description||'Ondertekend offline componentpakket.'),category:manifest.category||'tool',status:verified?'installed':'blocked',requiresAdmin:Boolean(manifest.requiresAdmin),license:String(manifest.license||'Onbekend'),version:String(manifest.version||version),publisher:String(receipt?.publisher||''),verified,path:directory})}catch{}}}const installedIds=new Set(installed.map(item=>item.id));this.state.componentPacks=[...installed,...COMPONENT_PACKS.filter(item=>!installedIds.has(item.id))];this.componentPacksLoaded=true;return this.snapshot()}
 registerComponentPack(pack:ComponentPack){if(!pack.verified||pack.status!=='installed')throw new Error('Alleen een geverifieerd, geïnstalleerd componentpakket kan worden geregistreerd.');this.state.componentPacks=[pack,...this.state.componentPacks.filter(item=>item.id!==pack.id)];this.persist();this.log('success','Componentpack',`${pack.name} ${pack.version||''} is ondertekend gecontroleerd en geïnstalleerd.`);return this.snapshot()}
 stageAtomicUpdate(version:string,file:string){if(!/^\d+\.\d+\.\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(version))throw new Error('Ongeldige updateversie.');if(!existsSync(file))throw new Error('Het gecontroleerde updatebestand ontbreekt.');this.state.atomicUpdate={status:'staged',currentVersion:APP_VERSION,lastKnownGoodVersion:this.state.atomicUpdate.lastKnownGoodVersion||APP_VERSION,stagedVersion:version,stagedFile:file,checkedAt:new Date().toISOString(),detail:'Update is gecontroleerd en apart klaargezet. De huidige versie blijft beschikbaar voor rollback.'};this.persist();return this.snapshot()}
 markVersionHealthy(){this.state.atomicUpdate={status:'healthy',currentVersion:APP_VERSION,lastKnownGoodVersion:APP_VERSION,checkedAt:new Date().toISOString(),detail:'De actieve versie heeft de start- en runtimecontrole doorstaan.'};this.persist();return this.snapshot()}
 assessReleaseReadiness(checkBinarySignature=false){const checks:ReleaseReadiness['checks']=[],add=(id:string,label:string,status:'ok'|'warning'|'error',detail:string)=>checks.push({id,label,status,detail});const required=this.state.runtimePackages.filter(item=>item.required),verified=required.filter(item=>item.verified);add('runtime','Offline runtime',required.length&&required.every(item=>item.installed)?'ok':'error',`${required.filter(item=>item.installed).length}/${required.length||this.state.offlineRuntime.requiredPackages} verplichte pakketten aanwezig.`);add('hashes','Pakketintegriteit',required.length&&verified.length===required.length?'ok':'warning',required.length&&verified.length===required.length?'Alle SHA-256-hashes gecontroleerd.':'Voer Pakketten controleren uit voor de definitieve release.');add('signed-updates','Updatebeleid',this.state.settings.requireSignedUpdates?'ok':'error',this.state.settings.requireSignedUpdates?'Ondertekening is verplicht.':'Ondertekende updates zijn niet verplicht.');add('publisher','Vertrouwde uitgever',this.state.settings.trustedUpdatePublisher.trim()?'ok':'warning',this.state.settings.trustedUpdatePublisher.trim()||'Vul vóór publicatie de exacte certificaatuitgever in.');const signature=checkBinarySignature&&process.platform==='win32'?spawnSync('powershell.exe',['-NoLogo','-NoProfile','-NonInteractive','-WindowStyle','Hidden','-Command',`(Get-AuthenticodeSignature -LiteralPath '${process.execPath.replace(/'/g,"''")}').Status`],{encoding:'utf8',windowsHide:true}):undefined,signed=signature?.status===0&&signature.stdout.trim()==='Valid';add('binary-signature','Programmaundertekening',signed?'ok':'warning',signed?'De actieve executable heeft een geldige Authenticode-handtekening.':checkBinarySignature?'De ontwikkelbuild is niet ondertekend; de publicatiestraat weigert dit bij Finalize.':'Klik Opnieuw beoordelen om de actieve executable te controleren.');const errors=checks.filter(check=>check.status==='error').length,warnings=checks.filter(check=>check.status==='warning').length,score=Math.max(0,100-errors*30-warnings*10),status=errors?'error':warnings?'warning':'ok';this.state.releaseReadiness={score,status,checkedAt:new Date().toISOString(),checks};this.emit('change',this.snapshot());return this.snapshot()}

 refreshOfflineRuntime(){const imported=path.join(this.dataDirectory,'downloads');mkdirSync(imported,{recursive:true});const expected=['apache.zip','php.zip','php-8.2.zip','php-8.3.zip','php-8.4.zip','mysql.zip','phpmyadmin.zip','mailpit.zip','redis.zip','WinSW.NET4.exe','composer.phar','vc_redist.x64.exe','mkcert.exe'],bundled=this.bundledPackagesDirectory&&existsSync(this.bundledPackagesDirectory)?this.bundledPackagesDirectory:undefined,bundledCount=bundled?expected.filter(file=>existsSync(path.join(bundled,file))).length:0,importedCount=expected.filter(file=>existsSync(path.join(imported,file))).length,directory=bundledCount===expected.length?bundled!:imported,packages=Math.max(bundledCount,importedCount);this.state.offlineRuntime={status:packages===expected.length?'ready':packages?'incomplete':'not-prepared',directory,packages,requiredPackages:expected.length,lastCheckedAt:new Date().toISOString()};this.refreshRuntimePackages(false);return this.snapshot()}
 refreshRuntimePackages(verifyHashes=true){const directory=this.state.offlineRuntime.directory||this.bundledPackagesDirectory||path.join(this.dataDirectory,'downloads'),manifestFile=path.join(directory,'offline-runtime.json'),previous=this.state.runtimePackages;let manifest:{packages?:Array<{id?:string;displayName?:string;name:string;version?:string;sha256:string;bytes:number;required?:boolean;source?:string}>}={};try{manifest=JSON.parse(readFileSync(manifestFile,'utf8').replace(/^\uFEFF/,''))}catch{}this.state.runtimePackages=(manifest.packages??[]).map(item=>{const id=item.id||path.parse(item.name).name,file=path.join(directory,item.name),installed=existsSync(file),sizeOk=installed&&statSync(file).size===Number(item.bytes),result=verifyHashes&&installed?spawnSync('certutil.exe',['-hashfile',file,'SHA256'],{encoding:'utf8',windowsHide:true,maxBuffer:1024*1024}):undefined,actualSha256=result?.status===0?(result.stdout.match(/\b[A-Fa-f0-9]{64}\b/)?.[0]||'').toUpperCase():previous.find(saved=>saved.id===id)?.actualSha256,verified=Boolean(sizeOk&&actualSha256===String(item.sha256).toUpperCase());return{id,name:item.displayName||item.id||item.name,version:item.version||'vastgezet',file:item.name,sha256:String(item.sha256).toUpperCase(),actualSha256,bytes:Number(item.bytes)||0,installed,verified,required:item.required!==false,detail:!installed?'Ontbreekt':!sizeOk?'Bestandsgrootte wijkt af':verifyHashes&&!verified?'SHA-256 wijkt af':verified?'SHA-256 gecontroleerd':'Grootte en manifest bekend'} satisfies RuntimePackageInfo});for(const component of this.state.components??[]){const exact=this.state.runtimePackages.find(item=>item.id===component.id);if(exact)component.version=exact.version}for(const service of this.state.services){const packageId=service.id==='mail'?'mailpit':service.id==='php'?`php-${this.state.settings.defaultPhpVersion}`:service.id,exact=this.state.runtimePackages.find(item=>item.id===packageId);if(exact)service.version=exact.version}return this.snapshot()}
 importOfflineRuntime(source:string){if(!existsSync(source))throw new Error('Offline pakketmap bestaat niet.');const target=path.join(this.dataDirectory,'downloads'),allowed=/\.(zip|exe|phar|json)$/i;mkdirSync(target,{recursive:true});let copied=0;for(const file of readdirSync(source)){if(!allowed.test(file))continue;copyFileSync(path.join(source,file),path.join(target,file));copied++}if(!copied)throw new Error('Geen LocalDeck-runtimepakketten gevonden in deze map.');this.refreshOfflineRuntime();this.log('success','Offline runtime',`${copied} pakketten geïmporteerd.`);return this.snapshot()}
 exportOfflineRuntime(target:string){this.refreshOfflineRuntime();const source=this.state.offlineRuntime.directory||path.join(this.dataDirectory,'downloads'),allowed=/\.(zip|exe|phar|json)$/i;mkdirSync(target,{recursive:true});let copied=0;if(existsSync(source))for(const file of readdirSync(source)){if(!allowed.test(file))continue;copyFileSync(path.join(source,file),path.join(target,file));copied++}if(!copied)throw new Error('Er zijn geen ingebouwde runtimepakketten om te exporteren.');this.log('success','Offline runtime',`${copied} pakketten geëxporteerd.`);return this.snapshot()}

 importLegacy(rootDirectory:string,kind:'xampp'|'wamp'|'laragon'){const root=path.resolve(rootDirectory),marker=kind==='xampp'?'xampp-control.exe':kind==='wamp'?'wampmanager.exe':'laragon.exe',candidates=kind==='xampp'?[path.join(root,'htdocs')]:kind==='wamp'?[path.join(root,'www')]:[path.join(root,'www')];if(!existsSync(path.join(root,marker)))throw new Error(`Dit lijkt geen geldige ${kind}-map te zijn.`);let added=0;for(const projectsRoot of candidates){if(!existsSync(projectsRoot))continue;for(const name of readdirSync(projectsRoot)){const projectPath=path.join(projectsRoot,name);if(!statSync(projectPath).isDirectory())continue;const files=readdirSync(projectPath);if(!files.some(file=>['index.php','composer.json','wp-config.php'].includes(file.toLowerCase())))continue;let slug=name.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||`${kind}-${added+1}`,domain=this.localDomain(slug),suffix=2;while(this.state.projects.some(project=>project.domain===domain))domain=this.localDomain(`${slug}-${suffix++}`);const publicRoot=existsSync(path.join(projectPath,'public','index.php'))?path.join(projectPath,'public'):projectPath;this.state.projects.push(normalizeProject({id:crypto.randomUUID(),name,domain,path:projectPath,documentRoot:publicRoot,phpVersion:this.state.settings.defaultPhpVersion,secure:true,createdAt:new Date().toISOString(),template:existsSync(path.join(projectPath,'wp-config.php'))?'wordpress':existsSync(path.join(projectPath,'artisan'))?'laravel':existsSync(path.join(projectPath,'bin','console'))?'symfony':'custom'}));added++}}this.writeVhosts();for(const project of this.state.projects.slice(-added))this.writeProjectManifest(project.id);this.runProjectDoctor();this.persist();this.log(added?'success':'warning','Migratie',added?`${added} projecten uit ${kind} geïmporteerd.`:`Geen geschikte projecten in ${kind} gevonden.`);return this.snapshot()}

 togglePlugin(id:string){const plugin=this.state.plugins.find(item=>item.id===id);if(!plugin)throw new Error('Plugin niet gevonden.');plugin.enabled=!plugin.enabled;this.persist();this.log('info','Catalogus',`${plugin.name} ${plugin.enabled?'ingeschakeld':'uitgeschakeld'}.`);return this.snapshot()}
 collectServiceLogs(){const sources=[['Apache',path.join(this.dataDirectory,'apache-error.log')],['MySQL',path.join(this.dataDirectory,'mysql-data',`${process.env.COMPUTERNAME||''}.err`)],['PHP',path.join(this.dataDirectory,'php-error.log')]]as const;for(const[source,file]of sources){if(!existsSync(file))continue;const lines=readFileSync(file,'utf8').replace(/\r/g,'').split('\n').filter(Boolean).slice(-80);for(const line of lines.reverse())if(!this.state.logs.some(item=>item.source===source&&item.message===line.slice(0,3000)))this.state.logs.unshift({id:crypto.randomUUID(),at:new Date().toISOString(),level:/error|fatal|denied/i.test(line)?'error':/warn/i.test(line)?'warning':'info',source,message:line.slice(0,3000)})}this.state.logs=this.state.logs.slice(0,500);this.emit('change',this.snapshot());return this.snapshot()}
 async testMailConfiguration(){const mode=this.state.settings.mailMode,host=mode==='capture'?'127.0.0.1':this.state.settings.smtpHost,port=this.state.settings.smtpPort;if(mode==='capture'){const mail=this.state.services.find(service=>service.id==='mail');if(!mail?.installed&&!this.state.settings.simulationMode)throw new Error('Mailpit is nog niet geïnstalleerd.')}if(!host||!port)throw new Error('Vul een SMTP-host en poort in.');if(!this.state.settings.simulationMode)await new Promise<void>((resolve,reject)=>{const socket=createConnection({host,port}),finish=(error?:Error)=>{socket.destroy();error?reject(error):resolve()};socket.setTimeout(2500,()=>finish(new Error(`Geen SMTP-verbinding met ${host}:${port}.`)));socket.once('connect',()=>finish());socket.once('error',error=>finish(new Error(`SMTP-test mislukt: ${error.message}`)))});this.log('success','E-mail',`${mode==='capture'?'Mailpit SMTP':mode==='local-server'?'Lokale mailserver':'SMTP-relay'} is bereikbaar op ${host}:${port}.`);return this.snapshot()}

 refreshBlueprints(){this.state.blueprints=this.state.projects.map(project=>{const file=path.join(project.path,'.localdeck.yml'),lockFile=path.join(project.path,'.localdeck.lock.json'),source=existsSync(file)?readFileSync(file,'utf8'):`# LocalDeck Project Capsule — veilig deelbaar, zonder geheime waarden\n${stringifyYaml(this.projectManifest(project),{indent:2,lineWidth:110})}`,hash=createHash('sha256').update(source).digest('hex');let locked=false,lockHash='';try{lockHash=String(JSON.parse(readFileSync(lockFile,'utf8')).blueprintHash||'');locked=lockHash===hash}catch{}const warnings:string[]=[];if(!existsSync(file))warnings.push('Capsule ontbreekt');if(!existsSync(path.join(project.path,'composer.lock'))&&existsSync(path.join(project.path,'composer.json')))warnings.push('composer.lock ontbreekt');if(project.profile?.databaseEngine!=='mysql')warnings.push(`${project.profile?.databaseEngine} is voorbereid maar nog niet in de native runtime geïnstalleerd`);return{projectId:project.id,schemaVersion:3,file,lockFile,locked,hash,updatedAt:existsSync(file)?statSync(file).mtime.toISOString():project.createdAt,summary:`PHP ${project.phpVersion} · ${project.profile?.webServer||'apache'} · ${project.profile?.databaseEngine||'mysql'} ${project.profile?.databaseVersion||'8.4'}`,warnings} satisfies ProjectBlueprint});return this.snapshot()}
 refreshComponents(){const system=(command:string)=>{const result=spawnSync('where.exe',[command],{encoding:'utf8',windowsHide:true});return result.status===0?result.stdout.trim().split(/\r?\n/)[0]:undefined},runtimeManifest=existsSync(path.join(this.dataDirectory,'runtime.json'));const component=(item:Omit<RuntimeComponent,'installed'|'active'|'verified'>,file?:string,active=false):RuntimeComponent=>{const installed=file?existsSync(file):false;return{...item,path:file,installed,active,verified:installed&&(runtimeManifest||item.category==='tool')}};this.state.components=[component({id:'apache',name:'Apache',category:'webserver',version:'2.4.68',required:true,description:'Standaard Windows-webserver.'},path.join(this.dataDirectory,'apache','bin','httpd.exe'),true),...['8.2','8.3','8.4','8.5'].map(version=>component({id:`php-${version}`,name:`PHP ${version}`,category:'language',version,required:version===this.state.settings.defaultPhpVersion,description:'Geïsoleerde FastCGI-pool per project.'},path.join(this.phpDirectory(version),'php.exe'),this.state.projects.some(project=>project.phpVersion===version)||this.state.settings.defaultPhpVersion===version)),component({id:'mysql',name:'MySQL',category:'database',version:'8.4.10',required:true,description:'Standaard relationele database.'},path.join(this.dataDirectory,'mysql','bin','mysqld.exe'),true),component({id:'mariadb',name:'MariaDB',category:'database',version:'11.x',required:false,description:'Optionele compatibele database-engine.'},path.join(this.dataDirectory,'mariadb','bin','mariadbd.exe')),component({id:'postgresql',name:'PostgreSQL',category:'database',version:'17.x',required:false,description:'Optionele PostgreSQL-engine.'},path.join(this.dataDirectory,'postgresql','bin','postgres.exe')),component({id:'phpmyadmin',name:'phpMyAdmin',category:'tool',version:'5.2.3',required:true,description:'Webgebaseerd MySQL-beheer.'},path.join(this.dataDirectory,'phpmyadmin','index.php'),true),component({id:'mailpit',name:'Mailpit',category:'service',version:'actueel',required:true,description:'Lokale SMTP-capture en inbox.'},path.join(this.dataDirectory,'mailpit','mailpit.exe'),true),component({id:'redis',name:'Redis',category:'service',version:'5.0.14.1',required:true,description:'Cache, sessies en queues.'},path.join(this.dataDirectory,'redis','redis-server.exe'),true),component({id:'composer',name:'Composer',category:'tool',version:'stable',required:true,description:'PHP-dependencybeheer.'},path.join(this.dataDirectory,'composer.phar'),true),component({id:'git',name:'Git',category:'tool',version:'system',required:false,description:'Klonen en branchomgevingen.'},system('git.exe')),component({id:'node',name:'Node.js',category:'language',version:'system',required:false,description:'Frontend builds en npm-taken.'},system('node.exe')),component({id:'vscode',name:'Visual Studio Code',category:'tool',version:'system',required:false,description:'Projecteditor.'},system('code.cmd'))];return this.snapshot()}
 refreshResources(){const items:{id:string;name:string;pid?:number;memoryMb:number}[]=[];const add=(id:string,name:string,pid?:number)=>{let memoryMb=0;if(pid){const result=spawnSync('tasklist.exe',['/fi',`PID eq ${pid}`,'/fo','csv','/nh'],{encoding:'utf8',windowsHide:true});const match=result.stdout.match(/"([\d.,]+)\s+K"/i);if(match)memoryMb=Math.round(Number(match[1].replace(/[.,]/g,''))/1024)}items.push({id,name,pid,memoryMb})};for(const service of this.state.services)if(service.pid)add(service.id,service.name,service.pid);for(const[version,child]of this.phpProcesses)if(child.pid&&!items.some(item=>item.pid===child.pid))add(`php-${version}`,`PHP ${version}`,child.pid);const appMemoryMb=Math.round(process.memoryUsage().rss/1024/1024),serviceMemoryMb=items.reduce((sum,item)=>sum+item.memoryMb,0);this.state.resourceUsage={capturedAt:new Date().toISOString(),appMemoryMb,serviceMemoryMb,totalMemoryMb:appMemoryMb+serviceMemoryMb,processes:items};return this.snapshot()}
 touchProject(id:string){const project=this.state.projects.find(item=>item.id===id);if(project){project.lastOpenedAt=new Date().toISOString();this.persist()}return this.snapshot()}
 async applyResourcePolicy(){this.refreshResources();const minutes=this.state.settings.idleStopMinutes;if(!this.state.settings.resourceSaver||!minutes||this.state.settings.runtimeMode!=='application'||!this.state.services.some(service=>service.status==='running'))return this.snapshot();const cutoff=Date.now()-minutes*60_000,recent=this.state.projects.some(project=>new Date(project.lastOpenedAt||project.createdAt).getTime()>cutoff);if(!recent){await this.toggleAll(false);this.log('info','Eco-modus',`Services automatisch gestopt na ${minutes} minuten zonder projectactiviteit.`)}return this.snapshot()}
 private detectProject(projectPath:string){const exists=(...parts:string[])=>existsSync(path.join(projectPath,...parts));const template:NonNullable<Project['template']>=exists('wp-config.php')||exists('wp-content')?'wordpress':exists('artisan')?'laravel':exists('bin','console')?'symfony':exists('web','core')||exists('core','lib','Drupal.php')?'drupal':'custom',documentRoot=['laravel','symfony','drupal'].includes(template)&&exists('public','index.php')?path.join(projectPath,'public'):projectPath;return{template,documentRoot,composer:exists('composer.json'),node:exists('package.json')}}
 async cloneProject(repositoryUrl:string,parentDirectory:string,name?:string){const url=repositoryUrl.trim();if(!/^(https:\/\/|ssh:\/\/|git@)[^\s]+$/i.test(url))throw new Error('Gebruik een geldige HTTPS- of SSH-Git-URL.');if(!existsSync(parentDirectory))throw new Error('De gekozen doelmap bestaat niet.');const repositoryName=(name?.trim()||path.basename(url).replace(/\.git$/i,'')).replace(/[^a-zA-Z0-9._-]+/g,'-'),target=path.join(parentDirectory,repositoryName);if(existsSync(target))throw new Error('De doelmap bestaat al.');const git=spawnSync('where.exe',['git.exe'],{encoding:'utf8',windowsHide:true});if(git.status!==0){if(!this.state.settings.simulationMode)throw new Error('Git is niet geïnstalleerd. Installeer Git via de catalogus.');mkdirSync(target,{recursive:true});writeFileSync(path.join(target,'index.php'),'<?php echo "LocalDeck Git-simulatie";');}else{this.log('info','Git',`${url} klonen…`);const result=spawnSync(git.stdout.trim().split(/\r?\n/)[0],['clone','--depth','1','--',url,target],{encoding:'utf8',windowsHide:true,maxBuffer:16*1024*1024});if(result.status!==0)throw new Error(result.stderr||'Git clone is mislukt.')}const detected=this.detectProject(target),slug=repositoryName.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||'project',state=this.addProject({name:repositoryName,path:target,documentRoot:detected.documentRoot,domain:this.localDomain(slug),phpVersion:this.state.settings.defaultPhpVersion,secure:true,template:detected.template,repositoryUrl:url,branch:'main'});this.log('success','Git',`${repositoryName} is gekloond en als LocalDeck-project toegevoegd.`);return state}
 async bootstrapProject(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const detected=this.detectProject(project.path),databaseName=project.profile?.databaseName||project.name.toLowerCase().replace(/[^a-z0-9]+/g,'_').replace(/^\d/,'db_$&').slice(0,48)||'localdeck';if(!this.state.databases.some(database=>database.name===databaseName)){if(!this.state.settings.simulationMode&&this.state.services.find(service=>service.id==='mysql')?.status!=='running')throw new Error('Start MySQL voordat je dit project automatisch inricht.');this.createDatabase(databaseName)}project.template=detected.template;project.documentRoot=detected.documentRoot;project.profile={...DEFAULT_PROFILE,...project.profile,databaseName,env:{...DEFAULT_PROFILE.env,...this.publicEnvironment(project.profile?.env),DATABASE_URL:`mysql://root@127.0.0.1:${this.state.services.find(service=>service.id==='mysql')?.port??3306}/${databaseName}`,DB_DATABASE:databaseName,MAIL_HOST:'127.0.0.1',MAIL_PORT:String(this.state.settings.smtpPort)}};const publicEnv=this.publicEnvironment(project.profile.env);writeFileSync(path.join(project.path,'.env.localdeck'),`${Object.entries(publicEnv).map(([key,value])=>`${key}=${value}`).join('\r\n')}\r\n# Geheime waarden worden tijdens processen veilig uit Windows Secret Vault toegevoegd.\r\n`);this.writeProjectManifest(projectId);if(!this.state.settings.simulationMode&&detected.composer)await this.runComposer(projectId,'install');if(!this.state.settings.simulationMode&&detected.node){const task=this.state.tasks.find(item=>item.id==='npm-install');if(task)await this.runTask(projectId,task.id)}this.persist();this.log('success','Project setup',`${project.name} is via de Project Capsule ingericht zonder geheime waarden of migraties automatisch uit te voeren.`);return this.snapshot()}
 writeIdeIntegration(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const vscode=path.join(project.path,'.vscode');mkdirSync(vscode,{recursive:true});const readJson=(file:string,fallback:any)=>{try{return JSON.parse(readFileSync(file,'utf8'))}catch{return fallback}},tasksFile=path.join(vscode,'tasks.json'),tasks=readJson(tasksFile,{version:'2.0.0',tasks:[]});tasks.version='2.0.0';tasks.tasks=[...(Array.isArray(tasks.tasks)?tasks.tasks:[]).filter((item:any)=>!String(item?.label||'').startsWith('LocalDeck:')),{label:'LocalDeck: preflight',type:'process',command:process.execPath,args:['--localdeck-cli=preflight',`--project=${project.id}`,`--localdeck-runtime-dir=${this.dataDirectory}`],problemMatcher:[]},{label:'LocalDeck: status',type:'process',command:process.execPath,args:['--localdeck-cli=status',`--localdeck-runtime-dir=${this.dataDirectory}`],problemMatcher:[]}];writeFileSync(tasksFile,JSON.stringify(tasks,null,2));const settingsFile=path.join(vscode,'settings.json'),settings=readJson(settingsFile,{});settings['php.validate.executablePath']=path.join(this.phpDirectory(project.phpVersion),'php.exe');settings['localdeck.projectId']=project.id;settings['localdeck.domain']=project.domain;writeFileSync(settingsFile,JSON.stringify(settings,null,2));const launchFile=path.join(vscode,'launch.json'),launch=readJson(launchFile,{version:'0.2.0',configurations:[]});launch.version='0.2.0';launch.configurations=[...(Array.isArray(launch.configurations)?launch.configurations:[]).filter((item:any)=>item?.name!=='LocalDeck: wacht op Xdebug'),{name:'LocalDeck: wacht op Xdebug',type:'php',request:'launch',port:9003,pathMappings:{[project.documentRoot||project.path]:'${workspaceFolder}'}}];writeFileSync(launchFile,JSON.stringify(launch,null,2));const idea=path.join(project.path,'.idea','runConfigurations');mkdirSync(idea,{recursive:true});const escape=(value:string)=>value.replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;'),cliArgs=`--localdeck-cli=preflight --project=${project.id} --localdeck-runtime-dir=${this.dataDirectory}`;writeFileSync(path.join(idea,'LocalDeck_Preflight.xml'),`<component name="ProjectRunConfigurationManager">\n  <configuration default="false" name="LocalDeck Preflight" type="ShConfigurationType">\n    <option name="SCRIPT_TEXT" value="&quot;${escape(process.execPath)}&quot; ${escape(cliArgs)}" />\n    <option name="INDEPENDENT_SCRIPT_PATH" value="true" />\n    <option name="SCRIPT_PATH" value="" />\n    <option name="SCRIPT_OPTIONS" value="" />\n    <option name="INDEPENDENT_SCRIPT_WORKING_DIRECTORY" value="true" />\n    <option name="SCRIPT_WORKING_DIRECTORY" value="$PROJECT_DIR$" />\n    <method v="2" />\n  </configuration>\n</component>\n`);this.log('success','IDE-integratie',`${project.name}: VS Code-taken, Xdebug en PhpStorm-runconfiguratie bijgewerkt.`);return this.snapshot()}
 private cloneDatabase(sourceName:string,targetName:string){const source=this.state.databases.find(item=>item.name===sourceName);if(!source)return false;if(!this.state.databases.some(item=>item.name===targetName))this.createDatabase(targetName,source.charset,source.collation);if(this.state.settings.simulationMode)return true;const port=String(this.state.services.find(service=>service.id==='mysql')?.port??3306),defaults=`--defaults-file=${path.join(this.dataDirectory,'my.ini')}`,env={...process.env,MYSQL_PWD:this.mysqlPassword()},dump=spawnSync(path.join(this.dataDirectory,'mysql','bin','mysqldump.exe'),[defaults,'-u','root','-h','127.0.0.1','-P',port,'--single-transaction','--routines','--events',sourceName],{encoding:'buffer',windowsHide:true,maxBuffer:256*1024*1024,env});if(dump.status!==0)throw new Error(`Databasebranch kon niet worden gekopieerd: ${String(dump.stderr)}`);const restore=spawnSync(path.join(this.dataDirectory,'mysql','bin','mysql.exe'),[defaults,'-u','root','-h','127.0.0.1','-P',port,targetName],{input:dump.stdout,encoding:'buffer',windowsHide:true,maxBuffer:256*1024*1024,env});if(restore.status!==0)throw new Error(`Databasebranch kon niet worden hersteld: ${String(restore.stderr)}`);return true}
 createBranchEnvironment(projectId:string,branch:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const clean=branch.trim();if(!/^[a-zA-Z0-9._/-]{1,80}$/.test(clean)||clean.startsWith('-')||clean.includes('..'))throw new Error('Gebruik een geldige Git-branchnaam.');if(this.state.branchEnvironments.some(item=>item.projectId===projectId&&item.branch===clean))throw new Error('Voor deze branch bestaat al een omgeving.');const slug=clean.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'').slice(0,40),target=path.join(this.dataDirectory,'branches',project.id,slug);if(existsSync(target))throw new Error('De branchmap bestaat al.');if(this.state.settings.simulationMode){mkdirSync(target,{recursive:true});writeFileSync(path.join(target,'index.php'),`<?php echo ${JSON.stringify(`${project.name} · ${clean}`)};`)}else{const git=spawnSync('where.exe',['git.exe'],{encoding:'utf8',windowsHide:true});if(git.status!==0)throw new Error('Git is niet geïnstalleerd.');mkdirSync(path.dirname(target),{recursive:true});const executable=git.stdout.trim().split(/\r?\n/)[0],local=spawnSync(executable,['-C',project.path,'show-ref','--verify','--quiet',`refs/heads/${clean}`],{windowsHide:true}),remote=spawnSync(executable,['-C',project.path,'show-ref','--verify','--quiet',`refs/remotes/origin/${clean}`],{windowsHide:true}),args=local.status===0?['-C',project.path,'worktree','add',target,clean]:remote.status===0?['-C',project.path,'worktree','add','-b',clean,target,`origin/${clean}`]:['-C',project.path,'worktree','add','-b',clean,target];const result=spawnSync(executable,args,{encoding:'utf8',windowsHide:true,maxBuffer:8*1024*1024});if(result.status!==0)throw new Error(result.stderr||'Git worktree kon niet worden gemaakt.')}const detected=this.detectProject(target),domain=`${slug}.${project.domain}`,databaseName=`${project.profile?.databaseName||project.name.toLowerCase().replace(/[^a-z0-9]+/g,'_')}_${slug.replace(/-/g,'_')}`.slice(0,63),child=this.addProject({name:`${project.name} · ${clean}`,path:target,documentRoot:detected.documentRoot,domain,phpVersion:project.phpVersion,secure:project.secure,template:detected.template,repositoryUrl:project.repositoryUrl,branch:clean,parentProjectId:project.id,profile:{...project.profile,databaseName,httpPort:undefined,httpsPort:undefined,env:{...this.publicEnvironment(project.profile?.env),DB_DATABASE:databaseName,DATABASE_URL:`mysql://root@127.0.0.1:${this.state.services.find(service=>service.id==='mysql')?.port??3306}/${databaseName}`}}as ProjectProfile}).projects.find(item=>item.path===target)!;const environment:BranchEnvironment={id:crypto.randomUUID(),projectId,childProjectId:child.id,branch:clean,path:target,domain,databaseName,databaseCloned:false,httpPort:child.profile!.httpPort!,httpsPort:child.profile!.httpsPort!,status:'creating',createdAt:new Date().toISOString()};this.state.branchEnvironments.push(environment);try{environment.databaseCloned=this.cloneDatabase(project.profile?.databaseName||'',databaseName);environment.status='ready';this.writeProjectManifest(child.id);this.log('success','Database Branching',`${clean} is beschikbaar op ${domain}${environment.databaseCloned?` met een kopie van ${project.profile?.databaseName}`:' met een leeg databaseprofiel'}.`)}catch(error){environment.status='error';environment.error=error instanceof Error?error.message:String(error);this.log('error','Database Branching',environment.error)}this.persist();return this.snapshot()}
 removeBranchEnvironment(id:string,keepDatabase=true){const environment=this.state.branchEnvironments.find(item=>item.id===id);if(!environment)throw new Error('Branchomgeving niet gevonden.');const root=path.resolve(path.join(this.dataDirectory,'branches')),target=path.resolve(environment.path);if(target!==root&&!target.startsWith(`${root}${path.sep}`))throw new Error('De branchmap valt buiten de beveiligde LocalDeck-branchmap.');const parent=this.state.projects.find(item=>item.id===environment.projectId);if(!this.state.settings.simulationMode&&parent){const git=spawnSync('where.exe',['git.exe'],{encoding:'utf8',windowsHide:true});if(git.status===0)spawnSync(git.stdout.trim().split(/\r?\n/)[0],['-C',parent.path,'worktree','remove','--force',target],{encoding:'utf8',windowsHide:true})}if(existsSync(target))rmSync(target,{recursive:true,force:true});this.state.projects=this.state.projects.filter(item=>item.id!==environment.childProjectId);if(!keepDatabase){const database=this.state.databases.find(item=>item.name===environment.databaseName);if(database)this.deleteDatabase(database.id)}this.state.branchEnvironments=this.state.branchEnvironments.filter(item=>item.id!==id);this.writeVhosts();this.persist();this.log('warning','Database Branching',`${environment.branch} verwijderd${keepDatabase?'; database bewaard':'; database verwijderd'}.`);return this.snapshot()}
 getFrameworkActions(projectId:string):FrameworkAction[]{const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const action=(id:string,name:string,description:string,command:string,args:string[],destructive=false,requiredFile?:string):FrameworkAction=>({id,projectId,name,description,command,args,destructive,available:!requiredFile||existsSync(path.join(project.path,requiredFile))}),items=[action('php-version','PHP-versie','Controleer de actieve projectruntime.','php',['-v']),action('composer-audit','Composer audit','Controleer PHP-dependencies op bekende kwetsbaarheden.','composer',['audit','--format=plain'])];if(project.template==='laravel')items.push(action('laravel-about','Laravel about','Toon framework- en omgevingsinformatie.','php',['artisan','about'],false,'artisan'),action('laravel-cache','Caches legen','Leeg Laravel configuratie-, route- en viewcaches.','php',['artisan','optimize:clear'],false,'artisan'),action('laravel-migrate','Migraties uitvoeren','Voer nog openstaande databasemigraties uit.','php',['artisan','migrate'],true,'artisan'));if(project.template==='symfony')items.push(action('symfony-about','Symfony about','Toon framework- en kernelinformatie.','php',['bin/console','about'],false,'bin/console'),action('symfony-cache','Cache legen','Bouw de Symfony-cache opnieuw op.','php',['bin/console','cache:clear'],false,'bin/console'));if(project.template==='drupal')items.push(action('drupal-status','Drupal status','Toon Drush-statusinformatie.','php',['vendor/bin/drush','status'],false,'vendor/bin/drush'),action('drupal-cache','Drupal cache rebuild','Bouw de Drupal-cache opnieuw op.','php',['vendor/bin/drush','cr'],false,'vendor/bin/drush'));if(project.template==='wordpress')items.push(action('wordpress-info','WordPress core-versie','Lees de geïnstalleerde WordPress-versie.','php',['vendor/bin/wp','core','version'],false,'vendor/bin/wp'),action('wordpress-cache','WordPress cache flush','Leeg de WordPress-objectcache.','php',['vendor/bin/wp','cache','flush'],false,'vendor/bin/wp'));if(existsSync(path.join(project.path,'package.json')))items.push(action('npm-test','Frontend tests','Voer het npm test-script uit.','npm',['test','--','--run']));return items}
 async runFrameworkAction(projectId:string,actionId:string){const action=this.getFrameworkActions(projectId).find(item=>item.id===actionId);if(!action||!action.available)throw new Error('Deze frameworkactie is niet beschikbaar.');const task:ProjectTask={id:`framework-${crypto.randomUUID()}`,projectId,name:action.name,command:action.command,args:action.args,category:'framework',safe:!action.destructive};this.state.tasks.push(task);try{return await this.runTask(projectId,task.id)}finally{this.state.tasks=this.state.tasks.filter(item=>item.id!==task.id);this.persist()}}
 setProjectXdebug(projectId:string,mode:ProjectProfile['xdebugMode']){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');if(!['off','debug','profile','trace','coverage'].includes(mode))throw new Error('Ongeldige Xdebug-modus.');const directory=this.phpDirectory(project.phpVersion),ini=path.join(directory,'php.ini'),dll=path.join(directory,'ext','php_xdebug.dll'),output=path.join(this.dataDirectory,'xdebug',project.id);mkdirSync(output,{recursive:true});if(!this.state.settings.simulationMode&&!existsSync(dll))throw new Error(`Xdebug ontbreekt voor PHP ${project.phpVersion}.`);if(existsSync(ini)){let content=readFileSync(ini,'utf8').replace(/^\s*zend_extension\s*=.*xdebug.*$/gmi,'').replace(/^\s*xdebug\.(mode|start_with_request|output_dir|client_host|client_port)\s*=.*$/gmi,'');if(mode!=='off')content=`${content.trimEnd()}\r\nzend_extension="${dll.replace(/\\/g,'/')}"\r\nxdebug.mode=${mode}\r\nxdebug.start_with_request=trigger\r\nxdebug.client_host=127.0.0.1\r\nxdebug.client_port=9003\r\nxdebug.output_dir="${output.replace(/\\/g,'/')}"\r\n`;writeFileSync(ini,content)}project.profile={...DEFAULT_PROFILE,...project.profile,xdebugMode:mode};this.writeProjectManifest(projectId);this.persist();this.log('success','Xdebug Workbench',`${project.name}: ${mode==='off'?'uitgeschakeld':`${mode} triggerklaar op IDE-poort 9003`}.`);return this.snapshot()}
 collectInspector(){const events:InspectorEvent[]=[];const add=(event:Omit<InspectorEvent,'id'|'traceId'|'spanId'>)=>{const seed=`${event.at}|${event.projectId||''}|${event.kind}|${event.title}|${event.detail}`,hash=createHash('sha256').update(seed).digest('hex');events.push({...event,id:crypto.randomUUID(),traceId:hash.slice(0,32),spanId:hash.slice(32,48),service:event.service||event.kind})};for(const project of this.state.projects){const access=path.join(this.dataDirectory,'logs',`${project.id}-access.log`),error=path.join(this.dataDirectory,'logs',`${project.id}-error.log`);if(existsSync(access))for(const line of readFileSync(access,'utf8').split(/\r?\n/).filter(Boolean).slice(-80)){const match=line.match(/"(GET|POST|PUT|PATCH|DELETE|HEAD)\s+([^\s]+)[^"]*"\s+(\d{3})\s+(\d+|-)(?:\s+(\d+))?$/),durationMs=match?.[5]?Math.round(Number(match[5])/1000):undefined;add({at:new Date().toISOString(),projectId:project.id,kind:'request',severity:match&&Number(match[3])>=500?'error':match&&Number(match[3])>=400?'warning':'info',title:match?`${match[1]} ${match[2]} · ${match[3]}${durationMs!==undefined?` · ${durationMs} ms`:''}`:'HTTP-request',detail:line,durationMs,service:'apache'})}if(existsSync(error))for(const line of readFileSync(error,'utf8').split(/\r?\n/).filter(Boolean).slice(-40))add({at:new Date().toISOString(),projectId:project.id,kind:'php',severity:/fatal|error/i.test(line)?'error':'warning',title:`${project.name} runtimefout`,detail:line,service:`php-${project.phpVersion}`})}for(const log of this.state.logs.slice(0,120)){const kind:InspectorEvent['kind']=/sql|database|mysql/i.test(log.source)?'sql':/queue|worker/i.test(log.source)?'queue':/taak|composer|npm|artisan|symfony|drupal|wordpress/i.test(log.source)?'task':/beveilig/i.test(log.source)?'security':'php';const durationMs=kind==='sql'?Number(log.message.match(/(\d+) ms\.?$/)?.[1]||'')||undefined:undefined;add({at:log.at,kind,severity:log.level==='error'?'error':log.level==='warning'?'warning':'info',title:log.source,detail:log.message,durationMs,service:kind})}this.state.inspectorEvents=events.sort((a,b)=>b.at.localeCompare(a.at)).slice(0,400);this.emit('change',this.snapshot());return this.snapshot()}
 compareDatabaseSchemas(leftId:string,rightId:string):DatabaseSchemaDiff{const left=this.state.databases.find(item=>item.id===leftId),right=this.state.databases.find(item=>item.id===rightId);if(!left||!right)throw new Error('Kies twee geldige databases.');const leftTables=this.getDatabaseTables(leftId),rightTables=this.getDatabaseTables(rightId),leftMap=new Map(leftTables.map(table=>[table.name,table])),rightMap=new Map(rightTables.map(table=>[table.name,table])),onlyLeft=[...leftMap.keys()].filter(name=>!rightMap.has(name)),onlyRight=[...rightMap.keys()].filter(name=>!leftMap.has(name)),changed=[...leftMap.keys()].filter(name=>rightMap.has(name)).flatMap(name=>{const a=leftMap.get(name)!,b=rightMap.get(name)!;return a.engine!==b.engine||a.collation!==b.collation?[{table:name,left:`${a.engine} · ${a.collation}`,right:`${b.engine} · ${b.collation}`}]:[]});return{left:left.name,right:right.name,onlyLeft,onlyRight,changed}}
 generateTestData(databaseId:string,table:string,count:number){const database=this.state.databases.find(item=>item.id===databaseId),clean=table.trim();if(!database)throw new Error('Database niet gevonden.');if(!/^[a-zA-Z0-9_]{1,64}$/.test(clean))throw new Error('Ongeldige tabelnaam.');if(!Number.isInteger(count)||count<1||count>100)throw new Error('Genereer per keer tussen 1 en 100 rijen.');if(this.state.settings.simulationMode){this.log('success','Testdata',`${count} gesimuleerde rijen voor ${database.name}.${clean}.`);return this.snapshot()}const description=this.mysql(['-D',database.name,'-N','-e',`SHOW COLUMNS FROM \`${clean}\``]).trim().split(/\r?\n/).map(line=>line.split('\t')),required=description.filter(([,type,nullable,key,defaultValue,extra])=>!String(extra).includes('auto_increment')&&nullable==='NO'&&defaultValue==='NULL').map(([name,type])=>({name,type}));for(let index=1;index<=count;index++){const values=required.map(column=>/int|decimal|float|double|bit/i.test(column.type)?String(index):/date|time|year/i.test(column.type)?'NOW()':`'LocalDeck-${index}'`),columns=required.map(column=>`\`${column.name}\``).join(','),sql=required.length?`INSERT INTO \`${clean}\` (${columns}) VALUES (${values.join(',')})`:`INSERT INTO \`${clean}\` () VALUES ()`;this.mysql(['-D',database.name,'-e',sql])}this.log('success','Testdata',`${count} rijen toegevoegd aan ${database.name}.${clean}.`);return this.snapshot()}
 anonymizeDatabase(databaseId:string,createBackup=true){const database=this.state.databases.find(item=>item.id===databaseId);if(!database)throw new Error('Database niet gevonden.');if(createBackup)this.backupDatabase(databaseId);if(this.state.settings.simulationMode){this.log('success','Privacy',`${database.name} gesimuleerd geanonimiseerd${createBackup?'; back-up gemaakt':''}.`);return this.snapshot()}const columns=this.mysql(['-N','-e',`SELECT TABLE_NAME,COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA='${database.name.replace(/'/g,"''")}' AND COLUMN_NAME REGEXP 'email|e_mail|phone|telephone|mobile|first_name|last_name|fullname|address|postcode|postal|token|api_key'`]).trim().split(/\r?\n/).filter(Boolean).map(line=>line.split('\t'));for(const[table,column]of columns){if(!/^[a-zA-Z0-9_]+$/.test(table)||!/^[a-zA-Z0-9_]+$/.test(column))continue;const expression=/email|e_mail/i.test(column)?`CONCAT('localdeck+',LEFT(SHA2(CAST(\`${column}\` AS CHAR),256),12),'@example.test')`:/phone|telephone|mobile/i.test(column)?`CONCAT('06',LEFT(SHA2(CAST(\`${column}\` AS CHAR),256),8))`:/token|api_key/i.test(column)?`LEFT(SHA2(CONCAT('localdeck-',CAST(\`${column}\` AS CHAR)),256),32)`:`CONCAT('Test-',LEFT(SHA2(CAST(\`${column}\` AS CHAR),256),10))`;this.mysql(['-D',database.name,'-e',`UPDATE \`${table}\` SET \`${column}\`=${expression} WHERE \`${column}\` IS NOT NULL`])}this.log('success','Privacy',`${columns.length} persoonsgegevens- en sleutelkolommen in ${database.name} geanonimiseerd${createBackup?' na automatische back-up':''}.`);return this.snapshot()}
 registerShare(session:ShareSession){this.state.shares=this.state.shares.filter(item=>item.status==='active'&&new Date(item.expiresAt)>new Date());this.state.shares.push(session);this.emit('change',this.snapshot());this.log('success','Delen',`Tijdelijke LAN-link actief tot ${new Date(session.expiresAt).toLocaleTimeString('nl-NL')}.`);return this.snapshot()}
 stopShare(id:string){const session=this.state.shares.find(item=>item.id===id);if(session)session.status='stopped';this.emit('change',this.snapshot());return this.snapshot()}
 expireShares(){const now=Date.now();for(const session of this.state.shares)if(session.status==='active'&&new Date(session.expiresAt).getTime()<=now)session.status='expired';return this.snapshot()}

 beginOperation(kind:OperationRecord['kind'],title:string,options:{projectId?:string;targetId?:string;canRetry?:boolean;metadata?:Record<string,string>;steps?:string[]}={}){const now=new Date().toISOString(),operation:OperationRecord={id:crypto.randomUUID(),kind,title,status:'running',progress:2,message:'Voorbereiden…',projectId:options.projectId,targetId:options.targetId,startedAt:now,updatedAt:now,canRetry:options.canRetry??true,canCancel:true,metadata:options.metadata,steps:(options.steps??[]).map((label,index)=>({id:`step-${index+1}`,label,status:'pending',detail:'Wacht op uitvoering.'}))};this.state.actionOperations.unshift(operation);this.state.actionOperations=this.state.actionOperations.slice(0,100);this.persist();this.emit('change',this.snapshot());return operation.id}
 updateOperation(id:string,progress:number,message:string,step?:{index:number;status:'running'|'ok'|'warning'|'error'|'cancelled';detail:string}){const operation=this.state.actionOperations.find(item=>item.id===id);if(!operation)return this.snapshot();operation.progress=Math.max(0,Math.min(100,Math.round(progress)));operation.message=message.slice(0,1200);operation.updatedAt=new Date().toISOString();if(step&&operation.steps[step.index]){const target=operation.steps[step.index];target.status=step.status;target.detail=step.detail.slice(0,1200);if(step.status==='running')target.startedAt??=operation.updatedAt;if(['ok','warning','error','cancelled'].includes(step.status))target.finishedAt=operation.updatedAt}this.persist();this.emit('change',this.snapshot());return this.snapshot()}
 completeOperation(id:string,message:string,status:'completed'|'failed'|'cancelled'='completed'){const operation=this.state.actionOperations.find(item=>item.id===id);if(!operation)return this.snapshot();operation.status=status;operation.progress=status==='completed'?100:operation.progress;operation.message=message.slice(0,1200);operation.updatedAt=new Date().toISOString();operation.finishedAt=operation.updatedAt;operation.canCancel=false;for(const step of operation.steps)if(step.status==='pending'||step.status==='running'){step.status=status==='completed'?'ok':status==='cancelled'?'cancelled':'error';step.detail=message.slice(0,1200);step.finishedAt=operation.updatedAt}this.persist();this.emit('change',this.snapshot());return this.snapshot()}
 cancelOperation(id:string){const operation=this.state.actionOperations.find(item=>item.id===id);if(!operation||!operation.canCancel||!['queued','running'].includes(operation.status))throw new Error('Deze actie kan niet meer worden geannuleerd.');this.cancelledOperations.add(id);const worker=operation.targetId&&this.workerProcesses.get(operation.targetId);if(worker)this.terminateProcess(worker);return this.completeOperation(id,'Actie door de gebruiker geannuleerd.','cancelled')}
 dismissOperation(id:string){const operation=this.state.actionOperations.find(item=>item.id===id);if(operation&&operation.status==='running')throw new Error('Een actieve actie kan niet worden verwijderd.');this.state.actionOperations=this.state.actionOperations.filter(item=>item.id!==id);this.persist();this.emit('change',this.snapshot());return this.snapshot()}
 clearCompletedOperations(){this.state.actionOperations=this.state.actionOperations.filter(item=>item.status==='running'||item.status==='queued');this.persist();this.emit('change',this.snapshot());return this.snapshot()}
 operationCancelled(id:string){return this.cancelledOperations.has(id)}
 beginSafeChange(kind:SafeChangeRecord['kind'],targetId:string,label:string,before:Record<string,unknown>,after:Record<string,unknown>){const operationId=this.beginOperation('safe-change',label,{targetId,canRetry:false,metadata:{changeKind:kind},steps:['Momentopname vastleggen','Wijziging vooraf controleren','Wijziging toepassen','Gezondheid controleren']}),change:SafeChangeRecord={id:crypto.randomUUID(),operationId,kind,targetId,label,before,after,status:'checking',createdAt:new Date().toISOString()};this.state.safeChanges.unshift(change);this.state.safeChanges=this.state.safeChanges.slice(0,100);this.persist();return change}
 finishSafeChange(id:string,success:boolean,message:string){const change=this.state.safeChanges.find(item=>item.id===id);if(!change)return this.snapshot();change.status=success?'applied':'failed';change.completedAt=new Date().toISOString();change.error=success?undefined:message;this.completeOperation(change.operationId,message,success?'completed':'failed');this.persist();return this.snapshot()}
 markSafeChangeRolledBack(id:string){const change=this.state.safeChanges.find(item=>item.id===id);if(!change)throw new Error('Veilige wijziging niet gevonden.');change.status='rolled-back';change.rolledBackAt=new Date().toISOString();this.persist();this.log('warning','Safe Change Engine',`${change.label} is teruggedraaid.`);return this.snapshot()}
 getSafeChange(id:string){return this.state.safeChanges.find(item=>item.id===id)}

 requiredServices(id:ServiceId){return SERVICE_DEPENDENCIES.find(item=>item.serviceId===id)?.requires??[]}
 dependentServices(id:ServiceId){return SERVICE_DEPENDENCIES.find(item=>item.serviceId===id)?.usedBy??[]}
 expandServiceSelection(ids:ServiceId[]){const selected=new Set<ServiceId>();const add=(id:ServiceId)=>{for(const required of this.requiredServices(id))add(required);selected.add(id)};for(const id of ids)add(id);return(['mysql','php','apache','phpmyadmin','redis','mail']as ServiceId[]).filter(id=>selected.has(id))}
 saveStartProfile(input:{id?:string;name:string;description?:string;services:ServiceId[];projectIds?:string[]}){const name=input.name.trim();if(!name)throw new Error('Geef het startprofiel een naam.');if(!input.services.length)throw new Error('Kies minimaal één service.');if(input.services.some(id=>!this.state.services.some(service=>service.id===id)))throw new Error('Het startprofiel bevat een onbekende service.');const services=this.expandServiceSelection(input.services),existing=input.id?this.state.startProfiles.find(item=>item.id===input.id):undefined;if(existing?.builtIn)throw new Error('Ingebouwde startprofielen kunnen niet worden overschreven.');const profile:StartProfile={id:existing?.id??crypto.randomUUID(),name,description:(input.description||'Eigen startprofiel').trim(),services,projectIds:(input.projectIds??[]).filter(id=>this.state.projects.some(project=>project.id===id)),builtIn:false,active:existing?.active??false,updatedAt:new Date().toISOString()};this.state.startProfiles=this.state.startProfiles.filter(item=>item.id!==profile.id);this.state.startProfiles.push(profile);this.persist();this.log('success','Startprofielen',`${profile.name} opgeslagen met ${services.length} services.`);return this.snapshot()}
 deleteStartProfile(id:string){const profile=this.state.startProfiles.find(item=>item.id===id);if(!profile)throw new Error('Startprofiel niet gevonden.');if(profile.builtIn)throw new Error('Een ingebouwd profiel kan niet worden verwijderd.');this.state.startProfiles=this.state.startProfiles.filter(item=>item.id!==id);this.persist();return this.snapshot()}
 activateStartProfile(id:string){const profile=this.state.startProfiles.find(item=>item.id===id);if(!profile)throw new Error('Startprofiel niet gevonden.');for(const item of this.state.startProfiles)item.active=item.id===id;profile.updatedAt=new Date().toISOString();this.persist();this.log('success','Startprofielen',`${profile.name} is actief.`);return this.snapshot()}
 refreshRuntimeLifecycle(){this.state.runtimeLifecycle=this.state.phpVersions.map(item=>{const usedBy=this.state.projects.filter(project=>project.phpVersion===item.version).map(project=>project.name),status:'current'|'security'|'legacy'=item.version==='8.2'||item.version==='8.3'?'security':'current',detail=item.version==='8.2'?'Oudste ondersteunde lijn: plan tijdig een projectupgrade.':item.version==='8.3'?'Stabiele beveiligingslijn voor bestaande projecten.':'Actuele lijn voor nieuwe en gemigreerde projecten.';return{version:item.version,status,usedBy,installed:item.installed,detail}});return this.snapshot()}
 private async portReachableAt(host:string,port:number){return new Promise<boolean>(resolve=>{let finished=false;const socket=createConnection({host,port}),finish=(reachable:boolean)=>{if(finished)return;finished=true;socket.destroy();resolve(reachable)};socket.setTimeout(800,()=>finish(false));socket.once('connect',()=>finish(true));socket.once('error',()=>finish(false))})}
 async inspectAllBindings(){this.invalidatePortInspection();const now=new Date().toISOString(),definitions:Array<Omit<PortBindingInfo,'status'|'ownerPid'|'ownerName'|'ownerPath'|'checkedAt'>>=[];const add=(id:string,label:string,port:number,role:PortBindingInfo['role'],reservedBy:string,serviceId?:ServiceId,protocol:PortBindingInfo['protocol']='TCP',family:PortBindingInfo['family']='IPv4',address=family==='IPv4'?'127.0.0.1':'::1')=>definitions.push({id,label,port,role,reservedBy,serviceId,protocol,family,address});for(const service of this.state.services){add(`${service.id}-tcp4`,service.name,service.port,'primary',service.name,service.id);add(`${service.id}-tcp6`,`${service.name} · IPv6`,service.port,'primary',service.name,service.id,'TCP','IPv6')}for(const phpRuntime of this.state.phpVersions)if(phpRuntime.version!==this.state.settings.defaultPhpVersion&&this.state.projects.some(project=>project.phpVersion===phpRuntime.version))add(`php-${phpRuntime.version}`,`PHP ${phpRuntime.version} pool`,this.phpPort(phpRuntime.version),'secondary',`PHP ${phpRuntime.version}`,'php');add('mail-smtp','Mailpit SMTP',this.state.settings.smtpPort,'secondary','Mailpit','mail');if(this.state.settings.webControlEnabled)add('web-control','Webbeheer',this.state.settings.webControlPort,'control','LocalDeck Webbeheer');if(this.state.settings.dnsMode==='local-dns'){add('dns-tcp','Lokale DNS',53,'control','LocalDeck DNS',undefined,'TCP');add('dns-udp','Lokale DNS',53,'control','LocalDeck DNS',undefined,'UDP')}for(const project of this.state.projects){if(project.profile?.httpPort)add(`project-${project.id}-http`,`${project.name} HTTP`,project.profile.httpPort,'project',project.name,'apache');if(project.secure&&project.profile?.httpsPort)add(`project-${project.id}-https`,`${project.name} HTTPS`,project.profile.httpsPort,'project',project.name,'apache')}const results:PortBindingInfo[]=[];for(const definition of definitions){const owner=definition.protocol==='TCP'&&!this.state.settings.simulationMode?this.windowsPortOwner(definition.port):undefined,reachable=definition.protocol==='TCP'?await this.portReachableAt(definition.address,definition.port):false,managed=Boolean(definition.serviceId&&this.state.services.find(item=>item.id===definition.serviceId)?.status==='running'),status:PortBindingInfo['status']=managed&&reachable?'localdeck':reachable||owner?'occupied':'reserved';results.push({...definition,status,ownerPid:owner?.pid,ownerName:owner?.name,ownerPath:owner?.path,checkedAt:now})}this.state.portBindings=results;this.emit('change',this.snapshot());return this.snapshot()}

 private saveEngineeringReport(report:Omit<EngineeringReport,'id'|'createdAt'>){const saved:EngineeringReport={...report,id:crypto.randomUUID(),createdAt:new Date().toISOString()};this.state.engineeringReports.unshift(saved);this.state.engineeringReports=this.state.engineeringReports.slice(0,100);this.persist();this.emit('change',this.snapshot());return saved}
 private reportStatus(checks:EngineeringCheck[]):{status:'ok'|'warning'|'error';score:number}{const errors=checks.filter(item=>item.status==='error').length,warnings=checks.filter(item=>item.status==='warning').length,total=Math.max(1,checks.length),score=Math.max(0,Math.round(((total-errors-warnings*.35)/total)*100));return{status:errors?'error':warnings?'warning':'ok',score}}
 async runDependencyGuard(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const operationId=this.beginOperation('dependency-guard',`Dependency Guard · ${project.name}`,{projectId,steps:['Lockfiles inventariseren','Composer valideren','Kwetsbaarheden controleren','Rapport opslaan']}),checks:EngineeringCheck[]=[];try{this.updateOperation(operationId,15,'Lockfiles controleren…',{index:0,status:'running',detail:'composer.lock en package-lock.json worden lokaal gelezen.'});const composerJson=existsSync(path.join(project.path,'composer.json')),composerLock=existsSync(path.join(project.path,'composer.lock')),packageJson=existsSync(path.join(project.path,'package.json')),packageLock=existsSync(path.join(project.path,'package-lock.json'));checks.push({id:'composer-lock',label:'Composer lockfile',status:!composerJson||composerLock?'ok':'error',detail:!composerJson?'Geen Composer-project.':composerLock?'composer.lock is aanwezig.':'composer.lock ontbreekt.'},{id:'node-lock',label:'Node lockfile',status:!packageJson||packageLock?'ok':'warning',detail:!packageJson?'Geen Node-project.':packageLock?'package-lock.json is aanwezig.':'package-lock.json ontbreekt.'});this.updateOperation(operationId,35,'Lockfiles geïnventariseerd.',{index:0,status:'ok',detail:`Composer ${composerLock?'vastgezet':'niet vastgezet'} · Node ${packageLock?'vastgezet':'niet vastgezet'}.`});if(composerJson){this.updateOperation(operationId,48,'Composer-manifest valideren…',{index:1,status:'running',detail:'composer validate --no-check-publish'});const php=path.join(this.phpDirectory(project.phpVersion),'php.exe'),composer=path.join(this.dataDirectory,'composer.phar');if(existsSync(php)&&existsSync(composer)){const validate=spawnSync(php,[composer,'validate','--no-check-publish'],{cwd:project.path,encoding:'utf8',windowsHide:true,timeout:120_000,maxBuffer:8*1024*1024}),valid=validate.status===0;checks.push({id:'composer-validate',label:'Composer validate',status:valid?'ok':'error',detail:(valid?validate.stdout:validate.stderr||validate.stdout).trim().slice(-700)||`Stopcode ${validate.status}`});this.updateOperation(operationId,62,'Composer-validatie afgerond.',{index:1,status:valid?'ok':'error',detail:valid?'Manifest is geldig.':'Composer meldt validatiefouten.'});this.updateOperation(operationId,70,'Composer audit uitvoeren…',{index:2,status:'running',detail:'Bekende advisories in de dependencyset controleren.'});const audit=spawnSync(php,[composer,'audit','--format=plain','--no-interaction'],{cwd:project.path,encoding:'utf8',windowsHide:true,timeout:120_000,maxBuffer:8*1024*1024}),auditOk=audit.status===0;checks.push({id:'composer-audit',label:'Composer audit',status:auditOk?'ok':audit.status===1?'error':'warning',detail:(audit.stdout||audit.stderr).trim().slice(-900)||`Stopcode ${audit.status}`});this.updateOperation(operationId,86,'Composer-audit afgerond.',{index:2,status:auditOk?'ok':audit.status===1?'error':'warning',detail:auditOk?'Geen bekende advisories.':'Controleer het auditresultaat.'})}else{checks.push({id:'composer-runtime',label:'Composer runtime',status:'warning',detail:'PHP of de ingebouwde Composer ontbreekt.'});this.updateOperation(operationId,78,'Composer niet beschikbaar.',{index:1,status:'warning',detail:'PHP of Composer ontbreekt.'});this.updateOperation(operationId,82,'Audit overgeslagen.',{index:2,status:'warning',detail:'Composer is niet beschikbaar.'})}}else{this.updateOperation(operationId,82,'Geen Composer-project.',{index:1,status:'ok',detail:'Niet van toepassing.'});this.updateOperation(operationId,84,'Geen Composer-audit nodig.',{index:2,status:'ok',detail:'Niet van toepassing.'})}const outcome=this.reportStatus(checks),report=this.saveEngineeringReport({kind:'dependency-guard',projectId,projectName:project.name,title:`Dependency Guard · ${project.name}`,status:outcome.status,score:outcome.score,summary:outcome.status==='ok'?'Dependencies zijn reproduceerbaar en gecontroleerd.':'Dependencycontrole vraagt aandacht.',checks,artifacts:[]});this.updateOperation(operationId,98,'Rapport opgeslagen.',{index:3,status:'ok',detail:`Score ${report.score}/100.`});this.completeOperation(operationId,`Dependency Guard voltooid met score ${report.score}/100.`);return this.snapshot()}catch(error){const message=error instanceof Error?error.message:String(error);this.completeOperation(operationId,message,'failed');throw error}}
 generateSbom(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const operationId=this.beginOperation('sbom',`SBOM · ${project.name}`,{projectId,steps:['Dependencybestanden lezen','Componenten normaliseren','CycloneDX schrijven']}),components:Array<{type:string;name:string;version:string;licenses?:Array<{license:{id?:string;name?:string}}>}>=[];try{this.updateOperation(operationId,20,'Composer- en Node-lockfiles lezen…',{index:0,status:'running',detail:'Alleen lokale lockfiles worden verwerkt.'});const composerLock=path.join(project.path,'composer.lock');if(existsSync(composerLock)){const parsed=JSON.parse(readFileSync(composerLock,'utf8'));for(const item of[...(parsed.packages??[]),...(parsed['packages-dev']??[])])components.push({type:'library',name:String(item.name),version:String(item.version),licenses:(Array.isArray(item.license)?item.license:[]).map((id:string)=>({license:{id}}))})}const packageLock=path.join(project.path,'package-lock.json');if(existsSync(packageLock)){const parsed=JSON.parse(readFileSync(packageLock,'utf8'));if(parsed.packages)for(const[key,item]of Object.entries(parsed.packages)as Array<[string,{name?:string;version?:string;license?:string}]>){if(!key||!item.version)continue;components.push({type:'library',name:item.name||path.basename(key),version:item.version,licenses:item.license?[{license:{id:item.license}}]:undefined})}}this.updateOperation(operationId,52,`${components.length} componenten gevonden.`,{index:0,status:'ok',detail:'Lockfiles zijn zonder netwerkverkeer gelezen.'});const unique=[...new Map(components.map(item=>[`${item.name}@${item.version}`,item])).values()];this.updateOperation(operationId,72,'Componenten normaliseren…',{index:1,status:'running',detail:`${unique.length} unieke dependencies.`});const directory=path.join(this.dataDirectory,'reports','sbom');mkdirSync(directory,{recursive:true});const file=path.join(directory,`${project.id}-${Date.now()}.cdx.json`),document={bomFormat:'CycloneDX',specVersion:'1.5',serialNumber:`urn:uuid:${crypto.randomUUID()}`,version:1,metadata:{timestamp:new Date().toISOString(),tools:{components:[{type:'application',name:'LocalDeck',version:APP_VERSION}]},component:{type:'application',name:project.name,version:'local'}},components:unique};this.updateOperation(operationId,88,'CycloneDX-document schrijven…',{index:1,status:'ok',detail:'Namen, versies en bekende licenties genormaliseerd.'});writeFileSync(file,JSON.stringify(document,null,2));const unknown=unique.filter(item=>!item.licenses?.length).length,checks:EngineeringCheck[]=[{id:'components',label:'Componentinventaris',status:unique.length?'ok':'warning',detail:`${unique.length} unieke componenten gevonden.`},{id:'licenses',label:'Licentiedekking',status:unknown?'warning':'ok',detail:unknown?`${unknown} componenten hebben geen licentie in de lockfile.`:'Alle componenten vermelden een licentie.'}],outcome=this.reportStatus(checks),report=this.saveEngineeringReport({kind:'sbom',projectId,projectName:project.name,title:`SBOM & licenties · ${project.name}`,status:outcome.status,score:outcome.score,summary:`CycloneDX 1.5 met ${unique.length} componenten.`,checks,artifacts:[file],metrics:{components:unique.length,unknownLicenses:unknown}});this.updateOperation(operationId,98,'SBOM opgeslagen.',{index:2,status:'ok',detail:file});this.completeOperation(operationId,`SBOM met ${unique.length} componenten aangemaakt.`);return report}catch(error){const message=error instanceof Error?error.message:String(error);this.completeOperation(operationId,message,'failed');throw error}}
 runDriftDetector(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const operationId=this.beginOperation('drift',`Team Drift Detector · ${project.name}`,{projectId,steps:['Capsule vergelijken','Runtime vergelijken','Afwijkingen rapporteren']}),checks:EngineeringCheck[]=[];try{const manifestFile=path.join(project.path,'.localdeck.json'),lockFile=path.join(project.path,'.localdeck.lock.json');this.updateOperation(operationId,30,'Project Capsule vergelijken…',{index:0,status:'running',detail:'Werkmap wordt met het vastgezette profiel vergeleken.'});if(!existsSync(manifestFile)||!existsSync(lockFile))checks.push({id:'capsule',label:'Project Capsule',status:'error',detail:'Capsule of LocalDeck-lockfile ontbreekt.'});else{const manifest=JSON.parse(readFileSync(manifestFile,'utf8')),lock=JSON.parse(readFileSync(lockFile,'utf8')),current=this.projectManifest(project),currentYaml=stringifyYaml(current,{indent:2,lineWidth:110}),hash=createHash('sha256').update(currentYaml).digest('hex'),lockedHash=String(lock.blueprintHash||'');checks.push({id:'capsule',label:'Capsule-profiel',status:manifest.php?.version===project.phpVersion?'ok':'error',detail:manifest.php?.version===project.phpVersion?`PHP ${project.phpVersion} komt overeen.`:`Capsule ${manifest.php?.version||'?'} versus lokaal ${project.phpVersion}.`},{id:'lock-hash',label:'Blueprint-lock',status:hash===lockedHash?'ok':'warning',detail:hash===lockedHash?'Profielhash is gelijk.':'Het actieve profiel wijkt af van de lockfile; schrijf de capsule opnieuw als dit bewust is.'})}this.updateOperation(operationId,62,'Capsulecontrole afgerond.',{index:0,status:checks.some(item=>item.status==='error')?'error':checks.some(item=>item.status==='warning')?'warning':'ok',detail:'Projectprofiel en lockfile zijn vergeleken.'});this.updateOperation(operationId,78,'Runtime vergelijken…',{index:1,status:'running',detail:'PHP, webserver en databaseversie controleren.'});const lifecycle=this.state.runtimeLifecycle.find(item=>item.version===project.phpVersion);checks.push({id:'runtime',label:'Projectruntime',status:lifecycle?.installed?'ok':'error',detail:lifecycle?.installed?`PHP ${project.phpVersion} is lokaal aanwezig.`:`PHP ${project.phpVersion} ontbreekt.`},{id:'runtime-policy',label:'Runtimebeleid',status:lifecycle?.status==='current'?'ok':'warning',detail:lifecycle?.detail||'Onbekende runtimelijn.'});this.updateOperation(operationId,88,'Runtimecontrole afgerond.',{index:1,status:checks.some(item=>item.id.startsWith('runtime')&&item.status!=='ok')?'warning':'ok',detail:'Gebruikte PHP-lijn gecontroleerd.'});const outcome=this.reportStatus(checks),report=this.saveEngineeringReport({kind:'drift',projectId,projectName:project.name,title:`Team Drift · ${project.name}`,status:outcome.status,score:outcome.score,summary:outcome.status==='ok'?'Werkmap, capsule en runtime zijn gelijk.':'Er zijn verschillen met het gedeelde projectprofiel.',checks,artifacts:[manifestFile,lockFile].filter(existsSync)});this.updateOperation(operationId,98,'Driftrapport gereed.',{index:2,status:outcome.status,detail:`Score ${report.score}/100.`});this.completeOperation(operationId,report.summary);return this.snapshot()}catch(error){const message=error instanceof Error?error.message:String(error);this.completeOperation(operationId,message,'failed');throw error}}
 async runMigrationLab(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const operationId=this.beginOperation('migration',`Migration Lab · ${project.name}`,{projectId,steps:['Framework herkennen','Migratiestatus lezen','Risico rapporteren']}),checks:EngineeringCheck[]=[];try{this.updateOperation(operationId,25,'Framework en migraties herkennen…',{index:0,status:'running',detail:'Er worden geen migraties uitgevoerd.'});const php=path.join(this.phpDirectory(project.phpVersion),'php.exe');let command:string[]|undefined,label='Migratiebestanden';if(existsSync(path.join(project.path,'artisan'))){command=['artisan','migrate:status','--no-interaction'];label='Laravel migrate:status'}else if(existsSync(path.join(project.path,'bin','console'))){command=['bin/console','doctrine:migrations:status','--no-interaction'];label='Doctrine migrations:status'}const folders=['database/migrations','migrations'].map(relative=>path.join(project.path,relative)).filter(existsSync),files=folders.flatMap(folder=>readdirSync(folder).filter(file=>/\.(php|sql)$/i.test(file)));checks.push({id:'migration-files',label:'Migratiebestanden',status:files.length?'ok':'warning',detail:files.length?`${files.length} migratiebestanden gevonden.`:'Geen bekende migratiemap gevonden.'});this.updateOperation(operationId,52,'Frameworkcontrole afgerond.',{index:0,status:'ok',detail:command?label:'Generieke bestandscontrole.'});if(command&&existsSync(php)){this.updateOperation(operationId,68,'Alleen-lezen migratiestatus uitvoeren…',{index:1,status:'running',detail:label});const result=spawnSync(php,command,{cwd:project.path,encoding:'utf8',windowsHide:true,timeout:120_000,maxBuffer:8*1024*1024,env:{...process.env,...this.publicEnvironment(project.profile?.env),...this.runtimeEnvironment(project.id)}}),ok=result.status===0;checks.push({id:'migration-status',label,status:ok?'ok':'warning',detail:(result.stdout||result.stderr).trim().slice(-1200)||`Stopcode ${result.status}`});this.updateOperation(operationId,82,'Migratiestatus gelezen.',{index:1,status:ok?'ok':'warning',detail:ok?'Geen wijzigingsopdracht uitgevoerd.':'Status kon niet volledig worden gelezen.'})}else{checks.push({id:'migration-status',label:'Migratiestatus',status:'warning',detail:'Geen ondersteund frameworkcommando of PHP-runtime gevonden.'});this.updateOperation(operationId,80,'Statuscontrole beperkt.',{index:1,status:'warning',detail:'Alleen migratiebestanden zijn geïnventariseerd.'})}checks.push({id:'safe-mode',label:'Veilige modus',status:'ok',detail:'Migration Lab heeft geen schemawijzigingen uitgevoerd.'});const outcome=this.reportStatus(checks),report=this.saveEngineeringReport({kind:'migration',projectId,projectName:project.name,title:`Migration Lab · ${project.name}`,status:outcome.status,score:outcome.score,summary:`Alleen-lezen controle van ${files.length} migraties.`,checks,artifacts:folders});this.updateOperation(operationId,96,'Risicoanalyse gereed.',{index:2,status:outcome.status,detail:report.summary});this.completeOperation(operationId,report.summary);return this.snapshot()}catch(error){const message=error instanceof Error?error.message:String(error);this.completeOperation(operationId,message,'failed');throw error}}
 async runRehearsal(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const operationId=this.beginOperation('rehearsal',`Rehearsal Mode · ${project.name}`,{projectId,steps:['Geïsoleerde kopie maken','Dependencies en syntax controleren','Migraties inventariseren','Testopdracht uitvoeren','Kopie opruimen']}),root=path.join(this.dataDirectory,'rehearsals',operationId),checks:EngineeringCheck[]=[];try{mkdirSync(path.dirname(root),{recursive:true});this.updateOperation(operationId,12,'Geïsoleerde projectkopie maken…',{index:0,status:'running',detail:'Bronproject en database worden niet gewijzigd.'});cpSync(project.path,root,{recursive:true,filter:source=>{const relative=path.relative(project.path,source).replace(/\\/g,'/');return!/(^|\/)(\.git|node_modules|storage\/logs|var\/cache)(\/|$)/i.test(relative)}});this.updateOperation(operationId,28,'Projectkopie gereed.',{index:0,status:'ok',detail:root});if(this.operationCancelled(operationId))throw new Error('Geannuleerd.');this.updateOperation(operationId,38,'Lockfiles en PHP-syntax controleren…',{index:1,status:'running',detail:'Alle controles draaien uitsluitend in de kopie.'});const composer=existsSync(path.join(root,'composer.json')),composerLock=existsSync(path.join(root,'composer.lock')),packageJson=existsSync(path.join(root,'package.json')),packageLock=existsSync(path.join(root,'package-lock.json'));checks.push({id:'locks',label:'Reproduceerbare dependencies',status:(!composer||composerLock)&&(!packageJson||packageLock)?'ok':'error',detail:`Composer ${composerLock?'lock aanwezig':composer?'lock ontbreekt':'n.v.t.'}; Node ${packageLock?'lock aanwezig':packageJson?'lock ontbreekt':'n.v.t.'}.`});const php=path.join(this.phpDirectory(project.phpVersion),'php.exe'),documentRoot=project.documentRoot?path.join(root,path.relative(project.path,project.documentRoot)):root,phpFiles=existsSync(documentRoot)?readdirSync(documentRoot).filter(file=>file.endsWith('.php')).slice(0,30):[],lintFailures:string[]=[];if(existsSync(php))for(const file of phpFiles){const result=spawnSync(php,['-l',path.join(documentRoot,file)],{encoding:'utf8',windowsHide:true,timeout:20_000});if(result.status!==0)lintFailures.push(file)}checks.push({id:'php-lint',label:'PHP-syntax in kopie',status:!existsSync(php)?'error':lintFailures.length?'error':phpFiles.length?'ok':'warning',detail:!existsSync(php)?`PHP ${project.phpVersion} ontbreekt.`:lintFailures.length?`Fouten: ${lintFailures.join(', ')}`:`${phpFiles.length} bestanden gecontroleerd.`});this.updateOperation(operationId,58,'Dependency- en syntaxcontrole gereed.',{index:1,status:checks.some(item=>item.status==='error')?'error':'ok',detail:`${phpFiles.length} PHP-bestanden gecontroleerd.`});const migrationFolders=['database/migrations','migrations'].map(relative=>path.join(root,relative)).filter(existsSync),migrations=migrationFolders.flatMap(folder=>readdirSync(folder).filter(file=>/\.(php|sql)$/i.test(file)));checks.push({id:'migrations',label:'Migratie-inventaris',status:migrations.length?'ok':'warning',detail:migrations.length?`${migrations.length} migraties in de geïsoleerde kopie gevonden.`:'Geen bekende migratiemap gevonden.'});this.updateOperation(operationId,70,'Migraties geïnventariseerd.',{index:2,status:migrations.length?'ok':'warning',detail:'Er is niets op de projectdatabase toegepast.'});if(this.operationCancelled(operationId))throw new Error('Geannuleerd.');this.updateOperation(operationId,78,'Veilige testopdracht zoeken…',{index:3,status:'running',detail:'Alleen tests uit reeds aanwezige dependencies.'});let testStatus:'ok'|'warning'|'error'='warning',testDetail='Geen lokaal testprogramma gevonden.';const phpunit=path.join(root,'vendor','bin','phpunit');if(existsSync(php)&&existsSync(phpunit)){const result=spawnSync(php,[phpunit],{cwd:root,encoding:'utf8',windowsHide:true,timeout:180_000,maxBuffer:8*1024*1024,env:{...process.env,...this.publicEnvironment(project.profile?.env),APP_ENV:'testing',LOCALDECK_REHEARSAL:'1'}});testStatus=result.status===0?'ok':'error';testDetail=(result.stdout||result.stderr).trim().slice(-1000)||`Stopcode ${result.status}`}checks.push({id:'tests',label:'Tests in kopie',status:testStatus,detail:testDetail});this.updateOperation(operationId,90,'Teststap afgerond.',{index:3,status:testStatus,detail:testDetail});const outcome=this.reportStatus(checks),report=this.saveEngineeringReport({kind:'rehearsal',projectId,projectName:project.name,title:`Rehearsal · ${project.name}`,status:outcome.status,score:outcome.score,summary:`Geïsoleerde repetitie: ${checks.filter(item=>item.status==='ok').length}/${checks.length} controles geslaagd; bronproject bleef ongewijzigd.`,checks,artifacts:[],metrics:{phpFiles:phpFiles.length,migrations:migrations.length}});rmSync(root,{recursive:true,force:true});this.updateOperation(operationId,98,'Geïsoleerde kopie verwijderd.',{index:4,status:'ok',detail:'Tijdelijke bestanden zijn opgeruimd.'});this.completeOperation(operationId,report.summary);return this.snapshot()}catch(error){if(existsSync(root))rmSync(root,{recursive:true,force:true});const message=error instanceof Error?error.message:String(error),cancelled=this.operationCancelled(operationId)||message==='Geannuleerd.';this.completeOperation(operationId,cancelled?'Rehearsal geannuleerd; tijdelijke kopie is verwijderd.':message,cancelled?'cancelled':'failed');if(!cancelled)throw error;return this.snapshot()}finally{this.cancelledOperations.delete(operationId)}}
 async runReleaseGate(projectId:string){const project=this.state.projects.find(item=>item.id===projectId);if(!project)throw new Error('Project niet gevonden.');const operationId=this.beginOperation('release-gate',`Release Gate · ${project.name}`,{projectId,steps:['Preflight uitvoeren','Dependencyrapport lezen','Drift beoordelen','SBOM controleren','Gatebesluit vastleggen']}),checks:EngineeringCheck[]=[];try{this.updateOperation(operationId,12,'Lokale CI uitvoeren…',{index:0,status:'running',detail:'PHP, Composer, tests en productiepariteit.'});await this.runPreflight(projectId);const preflight=this.state.preflightReports.find(item=>item.projectId===projectId)!;checks.push({id:'preflight',label:'Lokale CI',status:preflight.status,detail:`${preflight.steps.filter(item=>item.status==='ok').length}/${preflight.steps.length} stappen geslaagd.`});this.updateOperation(operationId,35,'Preflight afgerond.',{index:0,status:preflight.status,detail:checks.at(-1)!.detail});await this.runDependencyGuard(projectId);const dependency=this.state.engineeringReports.find(item=>item.kind==='dependency-guard'&&item.projectId===projectId)!;checks.push({id:'dependencies',label:'Dependency Guard',status:dependency.status,detail:`Score ${dependency.score}/100.`});this.updateOperation(operationId,52,'Dependencies beoordeeld.',{index:1,status:dependency.status,detail:dependency.summary});this.runDriftDetector(projectId);const drift=this.state.engineeringReports.find(item=>item.kind==='drift'&&item.projectId===projectId)!;checks.push({id:'drift',label:'Team Drift',status:drift.status,detail:`Score ${drift.score}/100.`});this.updateOperation(operationId,68,'Projectdrift beoordeeld.',{index:2,status:drift.status,detail:drift.summary});this.generateSbom(projectId);const sbom=this.state.engineeringReports.find(item=>item.kind==='sbom'&&item.projectId===projectId)!;checks.push({id:'sbom',label:'SBOM & licenties',status:sbom.status,detail:sbom.summary});this.updateOperation(operationId,82,'SBOM gecontroleerd.',{index:3,status:sbom.status,detail:sbom.summary});const performance=this.state.engineeringReports.find(item=>item.kind==='performance'&&item.projectId===projectId),browser=this.state.engineeringReports.find(item=>item.kind==='browser-matrix'&&item.projectId===projectId);checks.push({id:'performance',label:'Performancebaseline',status:performance?.status??'warning',detail:performance?.summary||'Nog geen performancebaseline vastgelegd.'},{id:'browser',label:'Browsermatrix',status:browser?.status??'warning',detail:browser?.summary||'Nog geen browsermatrix uitgevoerd.'});const outcome=this.reportStatus(checks),blocked=checks.some(item=>item.status==='error'),report=this.saveEngineeringReport({kind:'release-gate',projectId,projectName:project.name,title:`Release Gate · ${project.name}`,status:blocked?'error':outcome.status,score:outcome.score,summary:blocked?'Release geblokkeerd: los foutcontroles eerst op.':outcome.status==='warning'?'Voorwaardelijk gereed: bekijk waarschuwingen.':'Releasegereed.',checks,artifacts:sbom.artifacts});this.updateOperation(operationId,96,'Gatebesluit vastgelegd.',{index:4,status:report.status,detail:report.summary});this.completeOperation(operationId,`${report.summary} Score ${report.score}/100.`);return this.snapshot()}catch(error){const message=error instanceof Error?error.message:String(error);this.completeOperation(operationId,message,'failed');throw error}}
 recordExternalReport(kind:'performance'|'browser-matrix'|'mail-quality',input:{projectId?:string;title:string;summary:string;checks:EngineeringCheck[];artifacts?:string[];metrics?:Record<string,number|string|boolean>},operationId?:string){const project=input.projectId?this.state.projects.find(item=>item.id===input.projectId):undefined,outcome=this.reportStatus(input.checks),report=this.saveEngineeringReport({kind,projectId:input.projectId,projectName:project?.name,title:input.title,status:outcome.status,score:outcome.score,summary:input.summary,checks:input.checks,artifacts:input.artifacts??[],metrics:input.metrics});if(operationId)this.completeOperation(operationId,`${report.summary} Score ${report.score}/100.`);return this.snapshot()}
 importApiWorkspace(file:string){const source=readFileSync(file,'utf8').replace(/^\uFEFF/,''),document=/\.ya?ml$/i.test(file)?parseYaml(source):JSON.parse(source),paths=document.paths;if(!paths||typeof paths!=='object')throw new Error('Het OpenAPI-document bevat geen paths-object.');const endpoints:ApiWorkspace['endpoints']=[];for(const[route,definition]of Object.entries(paths)as Array<[string,Record<string,any>]>){for(const method of['get','post','put','patch','delete','head','options']as const){const operation=definition?.[method];if(!operation)continue;let requestBody='';const schema=operation.requestBody?.content?.['application/json']?.schema;if(schema?.example)requestBody=JSON.stringify(schema.example,null,2);else if(operation.requestBody?.content?.['application/json']?.example)requestBody=JSON.stringify(operation.requestBody.content['application/json'].example,null,2);endpoints.push({id:crypto.randomUUID(),method:method.toUpperCase()as ApiWorkspace['endpoints'][number]['method'],path:route,summary:String(operation.summary||operation.operationId||`${method.toUpperCase()} ${route}`),requestBody})}}if(!endpoints.length)throw new Error('Er zijn geen ondersteunde OpenAPI-endpoints gevonden.');const server=Array.isArray(document.servers)?document.servers[0]?.url:undefined,name=String(document.info?.title||path.basename(file,path.extname(file))),existing=this.state.apiWorkspaces.find(item=>item.sourceFile===file),now=new Date().toISOString(),workspace:ApiWorkspace={id:existing?.id??crypto.randomUUID(),name,baseUrl:String(server||'http://localhost'),sourceFile:file,endpoints,createdAt:existing?.createdAt??now,updatedAt:now};this.state.apiWorkspaces=this.state.apiWorkspaces.filter(item=>item.id!==workspace.id);this.state.apiWorkspaces.push(workspace);this.persist();this.log('success','API Studio',`${workspace.name}: ${endpoints.length} OpenAPI-endpoints geïmporteerd.`);return this.snapshot()}
 saveApiWorkspace(input:{id?:string;name:string;baseUrl:string;projectId?:string}){const name=input.name.trim();if(!name)throw new Error('Geef de API-werkruimte een naam.');let baseUrl:string;try{const parsed=new URL(input.baseUrl);if(!['http:','https:'].includes(parsed.protocol))throw new Error();baseUrl=parsed.toString().replace(/\/$/,'')}catch{throw new Error('Gebruik een geldige http- of https-basis-URL.')}const existing=input.id?this.state.apiWorkspaces.find(item=>item.id===input.id):undefined,now=new Date().toISOString(),workspace:ApiWorkspace={id:existing?.id??crypto.randomUUID(),name,baseUrl,projectId:input.projectId,endpoints:existing?.endpoints??[],sourceFile:existing?.sourceFile,createdAt:existing?.createdAt??now,updatedAt:now};this.state.apiWorkspaces=this.state.apiWorkspaces.filter(item=>item.id!==workspace.id);this.state.apiWorkspaces.push(workspace);this.persist();return this.snapshot()}
 deleteApiWorkspace(id:string){this.state.apiWorkspaces=this.state.apiWorkspaces.filter(item=>item.id!==id);this.state.apiRuns=this.state.apiRuns.filter(item=>item.workspaceId!==id);this.persist();return this.snapshot()}
 recordApiRun(run:Omit<ApiRun,'id'|'ranAt'>){const saved:ApiRun={...run,id:crypto.randomUUID(),ranAt:new Date().toISOString()};this.state.apiRuns.unshift(saved);this.state.apiRuns=this.state.apiRuns.slice(0,100);this.persist();this.log(saved.error?'warning':'success','API Studio',`${saved.method} ${saved.url} · ${saved.error||saved.status||'geen status'} · ${saved.durationMs} ms.`);return this.snapshot()}
 apiFixture(workspaceId:string,endpointId:string){const workspace=this.state.apiWorkspaces.find(item=>item.id===workspaceId),endpoint=workspace?.endpoints.find(item=>item.id===endpointId);if(!workspace||!endpoint)throw new Error('API-endpoint niet gevonden.');if(endpoint.requestBody)return endpoint.requestBody;return JSON.stringify({id:crypto.randomUUID(),name:'LocalDeck testitem',email:'developer@example.test',active:true,created_at:new Date().toISOString()},null,2)}
 createWebhookEndpoint(name:string){const clean=name.trim();if(!clean)throw new Error('Geef de webhook een naam.');const endpoint:WebhookEndpoint={id:crypto.randomUUID(),name:clean,path:`/hooks/${randomBytes(9).toString('hex')}`,secret:randomBytes(24).toString('base64url'),enabled:true,createdAt:new Date().toISOString()};this.state.webhookEndpoints.push(endpoint);this.persist();this.log('success','Webhook Catcher',`${endpoint.name} is lokaal beschikbaar.`);return this.snapshot()}
 deleteWebhookEndpoint(id:string){this.state.webhookEndpoints=this.state.webhookEndpoints.filter(item=>item.id!==id);this.state.webhookEvents=this.state.webhookEvents.filter(item=>item.endpointId!==id);this.persist();return this.snapshot()}
 recordWebhook(pathname:string,method:string,headers:Record<string,string>,body:string){const endpoint=this.state.webhookEndpoints.find(item=>item.enabled&&item.path===pathname);if(!endpoint)return undefined;const event:WebhookEvent={id:crypto.randomUUID(),endpointId:endpoint.id,method:method.slice(0,12),headers:Object.fromEntries(Object.entries(headers).filter(([key])=>!/authorization|cookie/i.test(key)).slice(0,80)),body:body.slice(0,256_000),receivedAt:new Date().toISOString(),replayCount:0};this.state.webhookEvents.unshift(event);this.state.webhookEvents=this.state.webhookEvents.slice(0,200);this.persist();this.emit('change',this.snapshot());this.log('success','Webhook Catcher',`${endpoint.name}: ${event.method} ontvangen (${event.body.length} tekens).`);return event}
 markWebhookReplayed(id:string){const event=this.state.webhookEvents.find(item=>item.id===id);if(!event)throw new Error('Webhookevent niet gevonden.');event.replayCount++;event.lastReplayAt=new Date().toISOString();this.persist();return this.snapshot()}
 saveWorker(input:{id?:string;projectId:string;name:string;command:string;args:string[];autoRestart?:boolean}){const project=this.state.projects.find(item=>item.id===input.projectId);if(!project)throw new Error('Project niet gevonden.');const name=input.name.trim(),command=input.command.trim();if(!name||!command||/[&|<>\r\n]/.test(command))throw new Error('Gebruik een veilige naam en één uitvoerbaar bestand zonder shellconstructies.');if(input.args.some(argument=>/[\r\n]/.test(argument)))throw new Error('Workerargumenten mogen geen regeleinden bevatten.');const existing=input.id?this.state.workers.find(item=>item.id===input.id):undefined;if(existing?.status==='running')throw new Error('Stop de worker voordat je hem wijzigt.');const worker:WorkerProcessInfo={id:existing?.id??crypto.randomUUID(),projectId:project.id,name,command,args:input.args.slice(0,30),status:'stopped',logs:existing?.logs??[],autoRestart:input.autoRestart??existing?.autoRestart??false,restartCount:existing?.restartCount??0};this.state.workers=this.state.workers.filter(item=>item.id!==worker.id);this.state.workers.push(worker);this.persist();return this.snapshot()}
 private workerExecutable(project:Project,worker:WorkerProcessInfo){let executable=worker.command,args=[...worker.args];const php=path.join(this.phpDirectory(project.phpVersion),'php.exe');if(worker.command==='php')executable=php;else if(worker.command==='composer'){executable=php;args=[path.join(this.dataDirectory,'composer.phar'),...args]}else if(worker.command==='npm'){const found=spawnSync('where.exe',['npm.cmd'],{encoding:'utf8',windowsHide:true});if(found.status!==0)throw new Error('npm is niet geïnstalleerd.');executable=found.stdout.trim().split(/\r?\n/)[0]}else if(!path.isAbsolute(executable)){const found=spawnSync('where.exe',[executable],{encoding:'utf8',windowsHide:true});if(found.status!==0)throw new Error(`${worker.command} is niet geïnstalleerd.`);executable=found.stdout.trim().split(/\r?\n/)[0]}if(!existsSync(executable))throw new Error(`${worker.command} is niet geïnstalleerd.`);return{executable,args}}
 async startWorker(id:string){const worker=this.state.workers.find(item=>item.id===id),project=worker&&this.state.projects.find(item=>item.id===worker.projectId);if(!worker||!project)throw new Error('Worker of project niet gevonden.');if(worker.status==='running')return this.snapshot();const operationId=this.beginOperation('worker',`Worker starten · ${worker.name}`,{projectId:project.id,targetId:worker.id,steps:['Opdracht controleren','Proces starten','Proces bewaken']}),launch=()=>{const{executable,args}=this.workerExecutable(project,worker),child=spawn(executable,args,{cwd:project.path,windowsHide:true,shell:false,stdio:['ignore','pipe','pipe'],env:{...process.env,...this.publicEnvironment(project.profile?.env),...this.runtimeEnvironment(project.id),LOCALDECK_WORKER:'1'}});this.workerProcesses.set(worker.id,child);worker.status='starting';worker.startedAt=new Date().toISOString();worker.error=undefined;this.updateOperation(operationId,45,'Proces gestart; gezondheid controleren…',{index:1,status:'running',detail:`${worker.command} ${worker.args.join(' ')}`});const append=(value:unknown)=>{worker.logs.unshift(String(value).trim().slice(0,4000));worker.logs=worker.logs.slice(0,100);this.emit('change',this.snapshot())};child.stdout?.on('data',append);child.stderr?.on('data',append);child.once('spawn',()=>{worker.status='running';worker.pid=child.pid;worker.restartCount=0;this.updateOperation(operationId,88,'Worker draait.',{index:1,status:'ok',detail:`PID ${child.pid}`});this.updateOperation(operationId,96,'Procesbewaking actief.',{index:2,status:'ok',detail:worker.autoRestart?'Automatisch herstarten staat aan.':'Handmatig herstarten.'});this.completeOperation(operationId,`${worker.name} draait verborgen met PID ${child.pid}.`);this.persist()});child.once('error',error=>{worker.status='error';worker.error=error.message;this.completeOperation(operationId,error.message,'failed');this.persist()});child.on('exit',code=>{if(this.workerProcesses.get(worker.id)!==child)return;this.workerProcesses.delete(worker.id);delete worker.pid;worker.status=code===0?'stopped':'error';worker.stoppedAt=new Date().toISOString();worker.error=code&&code!==0?`Stopcode ${code}`:undefined;this.persist();this.emit('change',this.snapshot());if(code&&worker.autoRestart&&worker.restartCount<3){worker.restartCount++;setTimeout(()=>{if(worker.status==='error')try{launch()}catch(error){worker.error=error instanceof Error?error.message:String(error)}},1200)}})};this.updateOperation(operationId,20,'Workeropdracht controleren…',{index:0,status:'running',detail:'Uitvoerbaar bestand en losse argumenten, zonder shell.'});try{if(this.state.settings.simulationMode){this.updateOperation(operationId,32,'Opdrachtstructuur is veilig.',{index:0,status:'ok',detail:worker.command});worker.status='running';worker.pid=Math.floor(3000+Math.random()*20000);worker.startedAt=new Date().toISOString();this.updateOperation(operationId,90,'Gesimuleerde worker draait.',{index:1,status:'ok',detail:`PID ${worker.pid}`});this.updateOperation(operationId,96,'Procesbewaking actief.',{index:2,status:'ok',detail:'Simulatiemodus.'});this.completeOperation(operationId,`${worker.name} draait in simulatiemodus.`);this.persist();return this.snapshot()}this.workerExecutable(project,worker);this.updateOperation(operationId,32,'Opdracht is veilig uitvoerbaar.',{index:0,status:'ok',detail:worker.command});launch();return this.snapshot()}catch(error){const message=error instanceof Error?error.message:String(error);this.completeOperation(operationId,message,'failed');throw error}}
 stopWorker(id:string){const worker=this.state.workers.find(item=>item.id===id);if(!worker)throw new Error('Worker niet gevonden.');const child=this.workerProcesses.get(id);if(child){this.workerProcesses.delete(id);this.terminateProcess(child)}worker.status='stopped';delete worker.pid;worker.stoppedAt=new Date().toISOString();this.persist();this.log('warning','Workers',`${worker.name} is gestopt.`);return this.snapshot()}
 deleteWorker(id:string){const worker=this.state.workers.find(item=>item.id===id);if(worker?.status==='running')this.stopWorker(id);this.state.workers=this.state.workers.filter(item=>item.id!==id);this.persist();return this.snapshot()}
 saveReplayCapture(input:Omit<ReplayCapture,'id'|'createdAt'>){let url:string;try{url=new URL(input.url).toString()}catch{throw new Error('Gebruik een geldige replay-URL.')}const capture:ReplayCapture={...input,url,id:crypto.randomUUID(),createdAt:new Date().toISOString(),headers:Object.fromEntries(Object.entries(input.headers).filter(([key])=>!/authorization|cookie/i.test(key)))};this.state.replayCaptures.unshift(capture);this.state.replayCaptures=this.state.replayCaptures.slice(0,100);this.persist();return this.snapshot()}
 markReplayCapture(id:string,result:{status?:number;durationMs:number}){const capture=this.state.replayCaptures.find(item=>item.id===id);if(!capture)throw new Error('Replay-opname niet gevonden.');capture.status=result.status;capture.durationMs=result.durationMs;capture.lastReplayedAt=new Date().toISOString();this.persist();return this.snapshot()}
 deleteReplayCapture(id:string){this.state.replayCaptures=this.state.replayCaptures.filter(item=>item.id!==id);this.persist();return this.snapshot()}
 inspectComponentPack(file:string){const operationId=this.beginOperation('component-pack','Componentpack inspecteren',{steps:['Archiefstructuur lezen','Manifest valideren','Veiligheidsrapport opslaan']}),result=spawnSync('tar.exe',['-tf',file],{encoding:'utf8',windowsHide:true,timeout:20_000,maxBuffer:4*1024*1024});if(result.status!==0){this.completeOperation(operationId,(result.stderr||'Componentpack kon niet worden gelezen.').trim(),'failed');throw new Error(result.stderr||'Componentpack kon niet worden gelezen.')}const entries=result.stdout.split(/\r?\n/).filter(Boolean),unsafe=entries.filter(entry=>path.isAbsolute(entry)||entry.split(/[\\/]+/).includes('..')),checks:EngineeringCheck[]=[{id:'archive',label:'Archiefstructuur',status:unsafe.length?'error':'ok',detail:unsafe.length?`${unsafe.length} onveilige paden gevonden.`:`${entries.length} bestanden gecontroleerd zonder uitpakken.`},{id:'manifest',label:'Packmanifest',status:entries.some(entry=>/(^|\/)manifest\.json$/i.test(entry))?'ok':'error',detail:entries.some(entry=>/(^|\/)manifest\.json$/i.test(entry))?'manifest.json aanwezig.':'manifest.json ontbreekt.'},{id:'signature',label:'Installatiehandtekening',status:'warning',detail:'Definitieve Authenticodecontrole vindt plaats bij expliciete installatie.'}],outcome=this.reportStatus(checks),report=this.saveEngineeringReport({kind:'component-pack',title:`Componentpack · ${path.basename(file)}`,status:outcome.status,score:outcome.score,summary:'Pack is alleen-lezen geïnspecteerd; er is niets uitgepakt of geïnstalleerd.',checks,artifacts:[file],metrics:{entries:entries.length}});this.updateOperation(operationId,95,'Inspectie afgerond.',{index:0,status:checks[0].status,detail:checks[0].detail});this.updateOperation(operationId,97,'Manifest beoordeeld.',{index:1,status:checks[1].status,detail:checks[1].detail});this.updateOperation(operationId,99,'Veiligheidsrapport opgeslagen.',{index:2,status:outcome.status,detail:`Score ${report.score}/100.`});this.completeOperation(operationId,report.summary);return this.snapshot()}
 retryOperation(id:string){const operation=this.state.actionOperations.find(item=>item.id===id);if(!operation||!operation.canRetry||!operation.projectId)throw new Error('Deze actie kan niet automatisch opnieuw worden uitgevoerd.');if(operation.kind==='rehearsal')return this.runRehearsal(operation.projectId);if(operation.kind==='release-gate')return this.runReleaseGate(operation.projectId);if(operation.kind==='dependency-guard')return this.runDependencyGuard(operation.projectId);if(operation.kind==='drift')return Promise.resolve(this.runDriftDetector(operation.projectId));if(operation.kind==='migration')return this.runMigrationLab(operation.projectId);if(operation.kind==='sbom'){this.generateSbom(operation.projectId);return Promise.resolve(this.snapshot())}throw new Error('Voor dit actietype is opnieuw uitvoeren niet beschikbaar.')}

 setUpdate(update:Partial<UpdateState>){this.state.update={...this.state.update,...update,currentVersion:APP_VERSION};this.emit('change',this.snapshot());return this.snapshot()}
 dismissUpdate(){this.state.update={status:'idle',currentVersion:APP_VERSION};this.emit('change',this.snapshot());return this.snapshot()}
 updateSettings(patch:Partial<AppState['settings']>){if(patch.language!==undefined&&!['nl','en'].includes(patch.language))throw new Error('Kies Nederlands of Engels als interfacetaal.');if(patch.webControlPort!==undefined&&(!Number.isInteger(patch.webControlPort)||patch.webControlPort<1024||patch.webControlPort>65535))throw new Error('De webbeheerpoort moet tussen 1024 en 65535 liggen.');if(patch.smtpPort!==undefined&&(!Number.isInteger(patch.smtpPort)||patch.smtpPort<1||patch.smtpPort>65535))throw new Error('De SMTP-poort moet tussen 1 en 65535 liggen.');if(patch.mailMaxMessages!==undefined&&(!Number.isInteger(patch.mailMaxMessages)||patch.mailMaxMessages<50||patch.mailMaxMessages>100000))throw new Error('Bewaar tussen 50 en 100.000 e-mails.');if(patch.mailMaxAgeDays!==undefined&&(!Number.isInteger(patch.mailMaxAgeDays)||patch.mailMaxAgeDays<0||patch.mailMaxAgeDays>3650))throw new Error('De e-mailretentie moet tussen 0 en 3650 dagen liggen.');if(patch.localDomainSuffix!==undefined&&!['localhost','test'].includes(patch.localDomainSuffix))throw new Error('Kies .localhost of .test als lokaal domein.');if(patch.idleStopMinutes!==undefined&&(!Number.isInteger(patch.idleStopMinutes)||patch.idleStopMinutes<0||patch.idleStopMinutes>1440))throw new Error('De inactiviteitsduur moet tussen 0 en 1440 minuten liggen.');if(patch.defaultPhpVersion!==undefined){if(!this.state.phpVersions.some(item=>item.version===patch.defaultPhpVersion))throw new Error('Deze PHP-versie wordt niet ondersteund.');if(this.state.services.find(item=>item.id==='php')?.status==='running')throw new Error('Stop PHP eerst voordat je de standaardversie wijzigt.');if(!this.state.settings.simulationMode&&!existsSync(path.join(this.phpDirectory(patch.defaultPhpVersion),'php-cgi.exe')))throw new Error(`PHP ${patch.defaultPhpVersion} is nog niet geïnstalleerd.`)}if(patch.updateChannel!==undefined&&patch.updateFeedUrl===undefined&&[OFFICIAL_STABLE_UPDATE_FEED,OFFICIAL_BETA_UPDATE_FEED].includes(this.state.settings.updateFeedUrl))patch={...patch,updateFeedUrl:patch.updateChannel==='beta'?OFFICIAL_BETA_UPDATE_FEED:OFFICIAL_STABLE_UPDATE_FEED};this.state.settings={...this.state.settings,...patch,dataDirectory:this.dataDirectory,projectsDirectory:this.projectsDirectory,folderMode:this.folderMode};this.state.phpVersions.forEach(v=>v.active=v.version===this.state.settings.defaultPhpVersion);const phpService=this.state.services.find(service=>service.id==='php');if(phpService)phpService.version=this.state.settings.defaultPhpVersion;if(patch.defaultPhpVersion){this.refreshPhpExtensions();this.refreshComponents();this.writeVhosts()}if(patch.language)this.writeLandingMetadata();this.assessReleaseReadiness();this.persist();this.log('info','Instellingen','Instellingen opgeslagen.');return this.snapshot()}
 clearLogs(){this.state.logs=[];this.log('info','Systeem','Logboek gewist.');return this.snapshot()}
}
