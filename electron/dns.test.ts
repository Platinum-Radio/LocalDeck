import{describe,expect,it}from'vitest';import{buildDnsResponse}from'./dns.js';
function query(name:string,type=1){const labels=name.split('.').flatMap(label=>[label.length,...Buffer.from(label)]),buffer=Buffer.alloc(12+labels.length+1+4);buffer.writeUInt16BE(0x1234,0);buffer.writeUInt16BE(0x0100,2);buffer.writeUInt16BE(1,4);Buffer.from(labels).copy(buffer,12);let offset=12+labels.length;buffer[offset++]=0;buffer.writeUInt16BE(type,offset);buffer.writeUInt16BE(1,offset+2);return buffer}
describe('LocalDeck DNS',()=>{it('antwoordt met 127.0.0.1 voor bekende .test-domeinen',()=>{const response=buildDnsResponse(query('demo.test'),new Set(['demo.test']));expect(response?.readUInt16BE(6)).toBe(1);expect([...response!.subarray(-4)]).toEqual([127,0,0,1])});it('geeft NXDOMAIN voor onbekende domeinen',()=>{const response=buildDnsResponse(query('anders.test'),new Set(['demo.test']));expect(response?.readUInt16BE(2)&15).toBe(3)})});
